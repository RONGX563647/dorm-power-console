# 前端学习文档 - 宿舍智能用电管理系统

> 本文档以实际项目为例，系统讲解前端开发的基础语法与高难点技术

## 目录

- [一、基础篇](#一基础篇)
  - [1.1 Vue 3 核心语法](#11-vue-3-核心语法)
  - [1.2 TypeScript 基础](#12-typescript-基础)
  - [1.3 组件化开发](#13-组件化开发)
  - [1.4 路由管理](#14-路由管理)
  - [1.5 状态管理](#15-状态管理)

- [二、进阶篇](#二进阶篇)
  - [2.1 API 请求封装](#21-api-请求封装)
  - [2.2 表单验证](#22-表单验证)
  - [2.3 数据可视化](#23-数据可视化)
  - [2.4 响应式设计](#24-响应式设计)

- [三、高难点篇](#三高难点篇)
  - [3.1 权限控制](#31-权限控制)
  - [3.2 WebSocket 实时通信](#32-websocket-实时通信)
  - [3.3 性能优化](#33-性能优化)
  - [3.4 错误处理](#34-错误处理)
  - [3.5 类型安全](#35-类型安全)

---

## 一、基础篇

### 1.1 Vue 3 核心语法

#### `<script setup>` 语法糖

项目中所有组件都使用了 `<script setup>` 语法糖，这是 Vue 3 Composition API 的简化写法：

```vue
<script setup lang="ts">
import { ref, computed, onMounted } from 'vue'

// 响应式数据
const count = ref(0)

// 计算属性
const doubleCount = computed(() => count.value * 2)

// 生命周期钩子
onMounted(() => {
  console.log('组件已挂载')
})

// 方法
const increment = () => {
  count.value++
}
</script>
```

**项目实例：[DevicesView.vue:180-381](frontend/src/views/DevicesView.vue#L180-L381)**

```vue
<script setup lang="ts">
import { ref, computed, onMounted, onUnmounted } from 'vue'

const loading = ref(false)  // 响应式状态
const devices = ref<Device[]>([])  // 带类型注解的响应式数组

// 计算属性
const onlineCount = computed(() => devices.value.filter(d => d.online).length)

// 生命周期
onMounted(() => {
  loadDevices()  // 组件挂载时加载数据
})

onUnmounted(() => {
  // 组件卸载时清理资源
  pollingManager.stop()
})
</script>
```

#### 模板语法

**条件渲染：**
```vue
<template>
  <div v-if="statusFilter === 'online'">
    显示在线设备
  </div>
  <div v-else>
    显示所有设备
  </div>
</template>
```

**列表渲染：**
```vue
<template>
  <div v-for="device in filteredDevices" :key="device.id">
    {{ device.name }}
  </div>
</template>
```

**事件处理：**
```vue
<template>
  <button @click="handleDelete(device)">删除</button>
  <input v-model="keyword" @input="handleSearch" />
</template>
```

**插槽（Slots）：**
```vue
<template>
  <a-button>
    <template #icon>
      <PlusOutlined />
    </template>
    添加设备
  </a-button>
</template>
```

### 1.2 TypeScript 基础

#### 类型定义

**接口定义：**
```typescript
// frontend/src/types/index.ts
export interface Device {
  id: string
  name: string
  room: string
  online: boolean
  type: 'strip' | 'socket'  // 联合类型
  lastSeen: string
}

export interface User {
  id?: string  // 可选属性
  username: string
  email: string
  role: 'admin' | 'user'  // 枚举类型
}
```

**泛型函数：**
```typescript
export async function get<T = any>(url: string, config?: any): Promise<T> {
  return instance.get(url, config)
}

// 使用时指定类型
const devices = await get<Device[]>('/api/devices')
```

**项目实例：[request.ts:64-82](frontend/src/utils/request.ts#L64-L82)**

```typescript
export async function get<T = any>(url: string, config?: any): Promise<T> {
  return instance.get(url, config)
}

export async function post<T = any>(url: string, data?: any, config?: any): Promise<T> {
  return instance.post(url, data, config)
}
```

### 1.3 组件化开发

#### Ant Design Vue 组件库

项目使用了 Ant Design Vue 组件库：

```vue
<template>
  <!-- 表格组件 -->
  <a-table :columns="columns" :data-source="devices" :loading="loading" />

  <!-- 模态框 -->
  <a-modal v-model:visible="visible" title="添加设备" @ok="handleOk">
    <a-form :model="form">
      <a-form-item label="设备名称">
        <a-input v-model:value="form.name" />
      </a-form-item>
    </a-form>
  </a-modal>

  <!-- 消息提示 -->
  <a-button @click="showMessage">提示</a-button>
</template>

<script setup lang="ts">
import { message } from 'ant-design-vue'

const showMessage = () => {
  message.success('操作成功')
  message.error('操作失败')
  message.warning('警告信息')
}
</script>
```

**项目实例：[DevicesView.vue:63-128](frontend/src/views/DevicesView.vue#L63-L128)**

```vue
<a-table
  :columns="columns"
  :data-source="filteredDevices"
  :loading="loading"
  :row-key="(record: Device) => record.id"
  :row-selection="{
    selectedRowKeys: state.selectedRowKeys,
    onChange: (keys: any[]) => {
      state.selectedRowKeys = keys
    }
  }"
>
  <template #bodyCell="{ column, record }">
    <template v-if="column.key === 'name'">
      {{ record.name }}
    </template>
  </template>
</a-table>
```

#### 自定义组件

创建可复用的自定义组件：

```vue
<!-- ButtonWithIcon.vue -->
<template>
  <a-button :type="type" @click="handleClick">
    <template #icon>
      <component :is="icon" />
    </template>
    <slot></slot>
  </a-button>
</template>

<script setup lang="ts">
import { computed } from 'vue'

const props = defineProps<{
  type?: 'primary' | 'default' | 'danger'
  icon: string
}>()

const emit = defineEmits<{
  click: []
}>()

const handleClick = () => {
  emit('click')
}
</script>
```

### 1.4 路由管理

#### Vue Router 配置

**基础配置：[router/index.ts:1-228](frontend/src/router/index.ts#L1-L228)**

```typescript
import { createRouter, createWebHistory } from 'vue-router'

const router = createRouter({
  history: createWebHistory(import.meta.env.BASE_URL),
  routes: [
    {
      path: '/',
      name: 'home',
      component: () => import('@/views/HomeView.vue'),
      meta: { requiresAuth: false }
    },
    {
      path: '/app',
      component: AppLayout,
      meta: { requiresAuth: true },
      children: [
        {
          path: 'dashboard',
          name: 'dashboard',
          component: () => import('@/views/DashboardView.vue')
        },
        {
          path: 'devices',
          name: 'devices',
          component: () => import('@/views/DevicesView.vue')
        }
      ]
    }
  ]
})
```

#### 路由守卫

**全局前置守卫：[router/index.ts:233-243](frontend/src/router/index.ts#L233-L243)**

```typescript
router.beforeEach((to, from, next) => {
  const authStore = useAuthStore()

  // 需要认证的路由
  if (to.meta.requiresAuth && !authStore.isAuthenticated) {
    next('/login')
  }
  // 已登录用户不能访问登录页
  else if ((to.path === '/login' || to.path === '/register') && authStore.isAuthenticated) {
    next('/app/dashboard')
  }
  else {
    next()
  }
})
```

#### 路由导航

```vue
<script setup lang="ts">
import { useRouter, useRoute } from 'vue-router'

const router = useRouter()
const route = useRoute()

// 编程式导航
const goToDevices = () => {
  router.push('/app/devices')
}

// 带参数导航
const goToDeviceDetail = (id: string) => {
  router.push(`/app/devices/${id}`)
}

// 查询参数
const searchDevices = (keyword: string) => {
  router.push({
    path: '/app/devices',
    query: { keyword }
  })
}

// 获取当前路由参数
const deviceId = route.params.id
const keyword = route.query.keyword
</script>
```

### 1.5 状态管理

#### Pinia Store

**认证状态管理：[stores/auth.ts:1-49](frontend/src/stores/auth.ts#L1-L49)**

```typescript
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'

export const useAuthStore = defineStore('auth', () => {
  // 响应式状态
  const token = ref<string | null>(localStorage.getItem('dorm_auth_token'))

  const user = ref<User | null>(() => {
    const cached = localStorage.getItem('dorm_auth_user')
    return cached ? JSON.parse(cached) : null
  })

  // 计算属性
  const isAuthenticated = computed(() => !!token.value)

  // Actions
  async function login(account: string, password: string) {
    const data = await authApi.login({ account, password })
    token.value = data.token
    user.value = data.user

    // 持久化到 localStorage
    localStorage.setItem('dorm_auth_token', data.token)
    localStorage.setItem('dorm_auth_user', JSON.stringify(data.user))
  }

  function logout() {
    token.value = null
    user.value = null
    localStorage.removeItem('dorm_auth_token')
    localStorage.removeItem('dorm_auth_user')
  }

  return {
    token,
    user,
    isAuthenticated,
    login,
    logout
  }
})
```

**使用 Store：**

```vue
<script setup lang="ts">
import { useAuthStore } from '@/stores/auth'

const authStore = useAuthStore()

// 访问状态
const isAuthenticated = authStore.isAuthenticated
const user = authStore.user

// 调用 actions
await authStore.login('admin', 'password')
authStore.logout()

// 在模板中使用
<template>
  <div v-if="authStore.isAuthenticated">
    欢迎, {{ authStore.user?.username }}
  </div>
</template>
```

---

## 二、进阶篇

### 2.1 API 请求封装

#### Axios 拦截器

**请求拦截器：[request.ts:13-24](frontend/src/utils/request.ts#L13-L24)**

```typescript
instance.interceptors.request.use(
  (config) => {
    const authStore = useAuthStore()
    // 自动添加认证令牌
    if (authStore.token) {
      config.headers.Authorization = `Bearer ${authStore.token}`
    }
    return config
  },
  (error) => {
    return Promise.reject(error)
  }
)
```

**响应拦截器：[request.ts:26-62](frontend/src/utils/request.ts#L26-L62)**

```typescript
instance.interceptors.response.use(
  (response) => {
    // 直接返回 data，避免每次都写 response.data
    return response.data
  },
  (error) => {
    // 统一错误处理
    if (error.response) {
      const status = error.response.status
      const data = error.response.data

      switch (status) {
        case 401:  // 未授权
          authStore.logout()
          message.error('登录已过期，请重新登录')
          window.location.href = '/login'
          break
        case 403:  // 禁止访问
          message.error('没有权限访问该资源')
          break
        case 404:  // 资源不存在
          message.error('请求的资源不存在')
          break
        case 500:  // 服务器错误
          message.error(data?.message || '服务器错误')
          break
        default:
          message.error(data?.message || `请求失败 (${status})`)
      }
    } else if (error.request) {
      message.error('网络错误，请检查网络连接')
    }

    return Promise.reject(error)
  }
)
```

#### API 模块化

**设备 API：[api/index.ts:86-146](frontend/src/utils/request.ts#L86-L146)**

```typescript
export const deviceApi = {
  // 获取设备列表
  getDevices: () => get<Device[]>('/api/devices'),

  // 获取房间设备
  getRoomDevices: (room: string) => get<Device[]>(`/api/devices/room/${room}`),

  // 创建设备
  createDevice: (data: Partial<Device>) => post<Device>('/api/devices', data),

  // 更新设备
  updateDevice: (deviceId: string, data: Partial<Device>) =>
    put<Device>(`/api/devices/${deviceId}`, data),

  // 删除设备
  deleteDevice: (deviceId: string) => del(`/api/devices/${deviceId}`),

  // 批量删除
  batchDeleteDevices: (deviceIds: string[]) =>
    del(`/api/devices/batch`, { data: deviceIds }),

  // 获取设备历史数据
  getDeviceHistory: (deviceId: string, hours: number = 24) =>
    get<DeviceHistory>(`/api/devices/${deviceId}/history`, { params: { hours } })
}
```

**使用 API：**

```vue
<script setup lang="ts">
import { deviceApi } from '@/api'

// 获取设备列表
const devices = await deviceApi.getDevices()

// 创建设备
await deviceApi.createDevice({
  id: 'device-001',
  name: '测试设备',
  room: '101'
})

// 删除设备
await deviceApi.deleteDevice('device-001')
</script>
```

### 2.2 表单验证

#### Ant Design Vue 表单验证

```vue
<template>
  <a-modal v-model:visible="visible" @ok="handleOk">
    <a-form
      ref="formRef"
      :model="form"
      :rules="rules"
      layout="vertical"
    >
      <a-form-item label="用户名" name="username">
        <a-input v-model:value="form.username" />
      </a-form-item>

      <a-form-item label="密码" name="password">
        <a-input-password v-model:value="form.password" />
      </a-form-item>

      <a-form-item label="邮箱" name="email">
        <a-input v-model:value="form.email" />
      </a-form-item>
    </a-form>
  </a-modal>
</template>

<script setup lang="ts">
import { ref, reactive } from 'vue'

const formRef = ref()
const visible = ref(false)

const form = reactive({
  username: '',
  password: '',
  email: ''
})

const rules = {
  username: [
    { required: true, message: '请输入用户名', trigger: 'blur' },
    { min: 3, max: 20, message: '长度在3到20个字符', trigger: 'blur' }
  ],
  password: [
    { required: true, message: '请输入密码', trigger: 'blur' },
    {
      pattern: /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d).{8,}$/,
      message: '密码至少8位，包含大小写字母和数字',
      trigger: 'blur'
    }
  ],
  email: [
    { required: true, message: '请输入邮箱', trigger: 'blur' },
    { type: 'email', message: '请输入正确的邮箱格式', trigger: 'blur' }
  ]
}

const handleOk = async () => {
  try {
    // 验证表单
    await formRef.value.validate()
    // 表单验证通过，执行提交逻辑
    console.log('表单数据:', form)
  } catch (error) {
    console.log('验证失败:', error)
  }
}
</script>
```

### 2.3 响应式设计

#### CSS 媒体查询

**项目实例：[AppLayout.vue:1120-1140](frontend/src/components/AppLayout.vue#L1120-L1140)**

```css
@media (max-width: 768px) {
  .cyber-content {
    margin: 16px;
  }

  .content-wrapper {
    padding: 16px;
  }

  .cyber-header {
    padding: 0 16px;
  }

  .user-info {
    display: none;  /* 小屏幕隐藏用户信息 */
  }

  .cyber-breadcrumb {
    display: none;  /* 小屏幕隐藏面包屑 */
  }
}
```

#### 使用计算属性实现响应式逻辑

```vue
<script setup lang="ts">
import { computed } from 'vue'

const screenWidth = ref(window.innerWidth)

// 监听窗口大小变化
window.addEventListener('resize', () => {
  screenWidth.value = window.innerWidth
})

// 根据屏幕宽度计算不同的值
const isMobile = computed(() => screenWidth.value < 768)
const isTablet = computed(() => screenWidth.value >= 768 && screenWidth.value < 1024)
const isDesktop = computed(() => screenWidth.value >= 1024)

// 动态样式
const containerStyle = computed(() => ({
  padding: isMobile.value ? '10px' : '20px',
  fontSize: isMobile.value ? '14px' : '16px'
}))
</script>
```

---

## 三、高难点篇

### 3.1 权限控制

#### 路由级权限控制

**路由守卫实现：[router/index.ts:233-243](frontend/src/router/index.ts#L233-L243)**

```typescript
router.beforeEach((to, from, next) => {
  const authStore = useAuthStore()

  // 检查路由是否需要认证
  if (to.meta.requiresAuth && !authStore.isAuthenticated) {
    next('/login')
  }
  else if ((to.path === '/login' || to.path === '/register') && authStore.isAuthenticated) {
    next('/app/dashboard')
  }
  else {
    next()
  }
})
```

#### 组件级权限控制

```vue
<template>
  <div>
    <!-- 根据权限显示/隐藏按钮 -->
    <a-button
      v-if="hasPermission('device:delete')"
      type="danger"
      @click="handleDelete"
    >
      删除设备
    </a-button>

    <!-- 根据角色显示内容 -->
    <div v-if="isAdmin">
      <h3>管理员专属内容</h3>
      <p>这里是管理员才能看到的内容</p>
    </div>
  </div>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import { useAuthStore } from '@/stores/auth'

const authStore = useAuthStore()

// 判断是否是管理员
const isAdmin = computed(() => authStore.user?.role === 'admin')

// 权限检查函数
const hasPermission = (permission: string): boolean => {
  // 实际项目中应该从后端获取用户权限列表
  const userPermissions = ['device:view', 'device:create', 'device:delete']
  return userPermissions.includes(permission)
}
</script>
```

### 3.2 WebSocket 实时通信

#### 基础 WebSocket 实现

```typescript
// services/websocket.ts
export class WebSocketService {
  private ws: WebSocket | null = null
  private url: string
  private reconnectTimer: number | null = null
  private reconnectAttempts = 0
  private maxReconnectAttempts = 5

  constructor(url: string) {
    this.url = url
  }

  // 连接
  connect() {
    this.ws = new WebSocket(this.url)

    this.ws.onopen = () => {
      console.log('WebSocket 连接成功')
      this.reconnectAttempts = 0
    }

    this.ws.onmessage = (event) => {
      const data = JSON.parse(event.data)
      this.handleMessage(data)
    }

    this.ws.onerror = (error) => {
      console.error('WebSocket 错误:', error)
    }

    this.ws.onclose = () => {
      console.log('WebSocket 连接关闭')
      this.reconnect()
    }
  }

  // 处理消息
  private handleMessage(data: any) {
    // 根据消息类型处理
    switch (data.type) {
      case 'DEVICE_STATUS':
        // 更新设备状态
        break
      case 'ALERT':
        // 显示告警
        break
    }
  }

  // 重连机制
  private reconnect() {
    if (this.reconnectAttempts < this.maxReconnectAttempts) {
      this.reconnectAttempts++
      const delay = Math.min(1000 * Math.pow(2, this.reconnectAttempts), 30000)

      this.reconnectTimer = window.setTimeout(() => {
        console.log(`尝试重连 (${this.reconnectAttempts}/${this.maxReconnectAttempts})`)
        this.connect()
      }, delay)
    }
  }

  // 发送消息
  send(data: any) {
    if (this.ws?.readyState === WebSocket.OPEN) {
      this.ws.send(JSON.stringify(data))
    }
  }

  // 断开连接
  disconnect() {
    if (this.reconnectTimer) {
      clearTimeout(this.reconnectTimer)
    }
    if (this.ws) {
      this.ws.close()
      this.ws = null
    }
  }
}
```

#### 在组件中使用

```vue
<script setup lang="ts">
import { onMounted, onUnmounted } from 'vue'
import { WebSocketService } from '@/services/websocket'

const wsService = new WebSocketService('ws://localhost:8080/ws')

onMounted(() => {
  wsService.connect()
})

onUnmounted(() => {
  wsService.disconnect()
})
</script>
```

### 3.3 性能优化

#### 1. 虚拟滚动（大数据列表）

```vue
<template>
  <div class="virtual-list" ref="listRef">
    <div
      class="virtual-list-container"
      :style="{ height: containerHeight + 'px' }"
    >
      <div
        class="virtual-list-placeholder"
        :style="{ height: totalHeight + 'px' }"
      ></div>
      <div
        class="virtual-list-content"
        :style="{ transform: `translateY(${offset}px)` }"
      >
        <div
          v-for="item in visibleItems"
          :key="item.id"
          class="list-item"
        >
          {{ item.name }}
        </div>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, computed, onMounted } from 'vue'

const listRef = ref<HTMLElement | null>(null)
const scrollTop = ref(0)
const ITEM_HEIGHT = 50  // 每项高度
const VISIBLE_COUNT = 20  // 可见数量

const totalHeight = computed(() => items.value.length * ITEM_HEIGHT)
const containerHeight = computed(() => VISIBLE_COUNT * ITEM_HEIGHT)
const startIndex = computed(() => Math.floor(scrollTop.value / ITEM_HEIGHT))
const endIndex = computed(() => Math.min(startIndex.value + VISIBLE_COUNT, items.value.length))
const offset = computed(() => startIndex.value * ITEM_HEIGHT)
const visibleItems = computed(() => items.value.slice(startIndex.value, endIndex.value))

const handleScroll = (e: Event) => {
  scrollTop.value = (e.target as HTMLElement).scrollTop
}

onMounted(() => {
  if (listRef.value) {
    listRef.value.addEventListener('scroll', handleScroll)
  }
})
</script>
```

#### 2. 防抖和节流

```typescript
// utils/debounce-throttle.ts

// 防抖：频繁触发的函数在停止触发后延迟执行
export function debounce<T extends (...args: any[]) => any>(
  func: T,
  delay: number
): (...args: Parameters<T>) => void {
  let timer: number | null = null

  return (...args: Parameters<T>) => {
    if (timer) {
      clearTimeout(timer)
    }
    timer = window.setTimeout(() => {
      func(...args)
    }, delay)
  }
}

// 节流：频繁触发的函数按固定时间间隔执行
export function throttle<T extends (...args: any[]) => any>(
  func: T,
  delay: number
): (...args: Parameters<T>) => void {
  let lastCall = 0

  return (...args: Parameters<T>) => {
    const now = Date.now()
    if (now - lastCall >= delay) {
      lastCall = now
      func(...args)
    }
  }
}
```

**使用示例：**

```vue
<script setup lang="ts">
import { debounce, throttle } from '@/utils/debounce-throttle'

// 搜索框防抖
const handleSearch = debounce((keyword: string) => {
  // 执行搜索逻辑
  searchDevices(keyword)
}, 500)

// 滚动节流
const handleScroll = throttle(() => {
  // 执行滚动逻辑
  loadMoreData()
}, 200)
</script>
```

#### 3. 路由懒加载

```typescript
// router/index.ts
const routes = [
  {
    path: '/app/dashboard',
    name: 'dashboard',
    component: () => import('@/views/DashboardView.vue')
  },
  {
    path: '/app/devices',
    name: 'devices',
    component: () => import('@/views/DevicesView.vue')
  }
]
```

#### 4. 图片懒加载

```vue
<template>
  <img
    v-lazy="imageUrl"
    alt="设备图片"
    class="device-image"
  />
</template>

<script setup lang="ts">
// 自定义指令
const vLazy = {
  mounted(el: HTMLElement, binding: any) {
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            el.setAttribute('src', binding.value)
            observer.unobserve(el)
          }
        })
      },
      { threshold: 0.1 }
    )

    observer.observe(el)
  }
}
</script>
```

### 3.4 错误处理

#### 全局错误处理

```typescript
// main.ts
import { createApp } from 'vue'

const app = createApp(App)

// 全局错误处理器
app.config.errorHandler = (err, instance, info) => {
  console.error('全局错误:', err)
  console.error('错误信息:', info)

  // 上报错误到监控系统
  reportError(err, info)

  // 显示友好的错误提示
  message.error('系统发生错误，请稍后重试')
}

// 未捕获的 Promise 错误
window.addEventListener('unhandledrejection', (event) => {
  console.error('未捕获的 Promise 错误:', event.reason)
  event.preventDefault()
})
```

#### 组件错误边界

```vue
<script setup lang="ts">
import { onErrorCaptured } from 'vue'

// 捕获子组件的错误
onErrorCaptured((err, instance, info) => {
  console.error('捕获到子组件错误:', err)
  console.error('错误信息:', info)

  // 显示错误 UI
  showErrorUI(err)

  // 返回 false 阻止错误继续向上传播
  return false
})
</script>
```

#### API 错误处理

**已在 [request.ts](frontend/src/utils/request.ts) 中实现全局响应拦截器**

### 3.5 类型安全

#### 类型守卫

```typescript
// 类型守卫函数
function isDevice(obj: any): obj is Device {
  return obj &&
    typeof obj.id === 'string' &&
    typeof obj.name === 'string' &&
    typeof obj.room === 'string'
}

// 使用
function processDevice(device: any) {
  if (isDevice(device)) {
    // TypeScript 知道 device 是 Device 类型
    console.log(device.name)
  }
}
```

#### 枚举类型

```typescript
// 定义枚举
enum DeviceStatus {
  ONLINE = 'online',
  OFFLINE = 'offline',
  MAINTENANCE = 'maintenance'
}

// 使用
const status: DeviceStatus = DeviceStatus.ONLINE

// 带字符串值的枚举
enum CommandState {
  PENDING = 'pending',
  SUCCESS = 'success',
  FAILED = 'failed',
  TIMEOUT = 'timeout',
  CANCELLED = 'cancelled'
}
```

#### 工具类型

```typescript
// Partial<T> - 将所有属性变为可选
type PartialDevice = Partial<Device>

// Required<T> - 将所有属性变为必填
type RequiredDevice = Required<Device>

// Readonly<T> - 将所有属性变为只读
type ReadonlyDevice = Readonly<Device>

// Pick<T, K> - 从 T 中选取指定属性
type DeviceSummary = Pick<Device, 'id' | 'name' | 'online'>

// Omit<T, K> - 从 T 中排除指定属性
type DeviceWithoutId = Omit<Device, 'id'>

// Record<K, T> - 构造一个类型，属性名为 K，属性值类型为 T
type DeviceMap = Record<string, Device>

// 交叉类型 &
type AdminUser = User & { permissions: string[] }

// 联合类型 |
type Status = 'active' | 'inactive' | 'pending'
```

---

## 四、实战案例分析

### 案例 1：设备管理页面

**技术要点：**
- 表格展示与筛选
- 模态框表单
- 批量操作
- 实时数据更新

**核心代码分析：[DevicesView.vue](frontend/src/views/DevicesView.vue)**

```vue
<script setup lang="ts">
// 1. 响应式状态管理
const devices = ref<Device[]>([])
const powerMap = ref<Record<string, number>>({})
const keyword = ref('')
const statusFilter = ref<'all' | 'online' | 'offline'>('all')

// 2. 计算属性实现数据过滤
const filteredDevices = computed(() => {
  return devices.value
    .filter(d => {
      const k = keyword.value.trim().toLowerCase()
      if (k) {
        return d.name.toLowerCase().includes(k) ||
               d.room.toLowerCase().includes(k) ||
               d.id.toLowerCase().includes(k)
      }
      return true
    })
    .filter(d => {
      if (statusFilter.value === 'all') return true
      if (statusFilter.value === 'online') return d.online
      return !d.online
    })
    .sort((a, b) => new Date(b.lastSeen).getTime() - new Date(a.lastSeen).getTime())
})

// 3. 异步数据加载
const loadDevices = async () => {
  try {
    loading.value = true
    const devs = await deviceApi.getDevices()
    devices.value = devs

    // 批量获取设备状态
    const statusMap = await deviceStatusService.getBatchDeviceStatus(devs.map(d => d.id))
    powerMap.value = Object.fromEntries(
      Object.entries(statusMap).map(([id, status]) => [id, status?.total_power_w ?? 0])
    )
  } catch (error: any) {
    message.error('加载设备失败')
  } finally {
    loading.value = false
  }
}

// 4. 定时轮询更新数据
const pollingManager = createPollingManager(loadDevices, 10000)

onMounted(() => {
  loadDevices()
  pollingManager.start()  // 每10秒自动刷新
})

onUnmounted(() => {
  pollingManager.stop()  // 组件卸载时停止轮询
})
</script>
```

### 案例 2：认证流程

**技术要点：**
- Token 认证
- 路由守卫
- 状态持久化
- 请求拦截器

**核心代码：**

1. **登录：[LoginView.vue](frontend/src/views/LoginView.vue)**
```vue
<script setup lang="ts">
const handleLogin = async () => {
  try {
    await authStore.login(username.value, password.value)
    router.push('/app/dashboard')
    message.success('登录成功')
  } catch (error) {
    message.error('登录失败')
  }
}
</script>
```

2. **Token 持久化：[stores/auth.ts](frontend/src/stores/auth.ts)**
```typescript
const token = ref<string | null>(localStorage.getItem('dorm_auth_token'))
```

3. **请求拦截器自动添加 Token：[request.ts](frontend/src/utils/request.ts)**
```typescript
instance.interceptors.request.use((config) => {
  const authStore = useAuthStore()
  if (authStore.token) {
    config.headers.Authorization = `Bearer ${authStore.token}`
  }
  return config
})
```

4. **Token 过期处理：[request.ts](frontend/src/utils/request.ts)**
```typescript
case 401:
  authStore.logout()
  message.error('登录已过期，请重新登录')
  window.location.href = '/login'
```

---

## 五、开发技巧与最佳实践

### 1. 代码组织

```
frontend/
├── src/
│   ├── api/           # API 接口定义
│   ├── assets/        # 静态资源
│   ├── components/    # 公共组件
│   ├── router/        # 路由配置
│   ├── stores/        # 状态管理
│   ├── types/         # TypeScript 类型定义
│   ├── utils/         # 工具函数
│   ├── views/         # 页面组件
│   └── App.vue
```

### 2. 代码规范

- 使用 TypeScript 严格模式
- 组件名使用 PascalCase
- 变量和函数名使用 camelCase
- 常量使用 UPPER_SNAKE_CASE
- 使用 ESLint + Prettier 保证代码风格一致

### 3. 调试技巧

```typescript
// 1. 使用 console.table 查看数组/对象
console.table(devices.value)

// 2. 使用 debugger 断点
debugger

// 3. 使用 Vue DevTools
// 安装 Vue DevTools 浏览器插件

// 4. 使用 performance API
console.time('loadDevices')
await loadDevices()
console.timeEnd('loadDevices')
```

### 4. 常见问题解决

**问题 1：响应式数据不更新**
```typescript
// 错误：直接修改数组索引
items[0].name = 'new name'

// 正确：使用 Vue.set 或重新赋值
items.value = [...items.value]
items.value[0].name = 'new name'
```

**问题 2：异步数据获取**
```typescript
// 错误：在 setup 顶层直接调用异步函数
const data = await fetchData()

// 正确：在 onMounted 中调用
onMounted(async () => {
  const data = await fetchData()
})
```

**问题 3：循环引用**
```typescript
// 在类型定义文件中避免循环引用
// 使用 Pick 或 Omit 来减少依赖
```

---

## 六、学习资源推荐

### 官方文档
- [Vue 3 官方文档](https://cn.vuejs.org/)
- [TypeScript 官方文档](https://www.typescriptlang.org/zh/)
- [Pinia 官方文档](https://pinia.vuejs.org/zh/)
- [Vue Router 官方文档](https://router.vuejs.org/zh/)
- [Ant Design Vue 官方文档](https://www.antdv.com/)

### 在线课程
- Vue Mastery
- Vue School
- TypeScript 官方教程

### 工具推荐
- VSCode + Volar 插件
- Vue DevTools
- TypeScript Playground
- Postman（API 测试）

---

## 七、总结

本文档通过宿舍智能用电管理系统这个实际项目，全面讲解了前端开发的核心技术：

1. **基础语法**：Vue 3 Composition API、TypeScript、组件化开发
2. **进阶技术**：路由管理、状态管理、API 封装、响应式设计
3. **高难知识点**：权限控制、WebSocket、性能优化、错误处理、类型安全

学习建议：
1. 先掌握基础语法，理解响应式原理
2. 通过实际项目练习，加深理解
3. 关注性能和代码质量
4. 持续学习新技术，跟上行业发展

**记住：最好的学习方式是动手实践！**
