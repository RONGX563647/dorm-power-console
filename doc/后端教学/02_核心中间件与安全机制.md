# 第二章：核心中间件与安全机制

> 本章深入讲解宿舍电力管理系统的核心中间件实现，包括Spring Security安全框架、JWT令牌认证、AOP切面编程、限流机制等。通过学习，你将掌握企业级应用的安全防护和横切关注点处理技术。

---

## 教学目标与难点

### 学习目标

1. 能解释一次请求进入系统后，如何依次经过 CORS、Security、JWT、AOP、异常处理等组件。
2. 能读懂 `SecurityConfig`、`JwtAuthenticationFilter`、`JwtUtil`、`ApiAspect` 的职责分工。
3. 能说明项目里限流和缓存的当前实现，以及它们各自适合解决什么问题。
4. 能发现当前实现中的可改进点，而不是把所有代码都视为最终最佳实践。

### 教学难点

- 学生容易把“JWT 认证成功”误解为“所有权限都自动正确”。
- 学生容易只看到注解，不理解过滤器链和切面是如何在运行期生效的。
- 学生容易把“推荐方案”与“当前项目现状”混为一谈，例如把 Redis、Caffeine、完整 RBAC 都当成已完全落地。

---

## 目录

1. [Spring Security安全框架](#1-spring-security安全框架)
2. [JWT令牌认证机制](#2-jwt令牌认证机制)
3. [AOP切面编程](#3-aop切面编程)
4. [限流机制实现](#4-限流机制实现)
5. [缓存管理](#5-缓存管理)

---

## 1. Spring Security安全框架

### 1.1 安全框架概述

Spring Security是一个强大且高度可定制的认证和访问控制框架。它是保护基于Spring应用程序的事实标准。

**核心功能**：

- **认证 (Authentication)**：验证用户身份（你是谁？）
- **授权 (Authorization)**：验证用户权限（你能做什么？）
- **防护 (Protection)**：防止常见攻击（CSRF、Session Fixation等）

### 1.2 安全配置详解

#### SecurityConfig.java 完整解析

```java
@Configuration
@EnableWebSecurity
@EnableMethodSecurity(prePostEnabled = true)
public class SecurityConfig {

    @Autowired
    private JwtAuthenticationFilter jwtAuthenticationFilter;

    @Autowired
    private CorsConfigurationSource corsConfigurationSource;

    /**
     * 配置安全过滤器链
     * 这是Spring Security的核心配置
     */
    @Bean
    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {
        http
            .cors(cors -> cors.configurationSource(corsConfigurationSource))
            .csrf(AbstractHttpConfigurer::disable)
            .authorizeHttpRequests(authorize -> authorize
                .requestMatchers(HttpMethod.OPTIONS, "/**").permitAll()
                .requestMatchers(
                    "/health",
                    "/actuator/**",
                    "/api/auth/**",
                    "/api/agent/**",
                    "/api/simulator/**",
                    "/ws",
                    "/swagger-ui/**",
                    "/swagger-ui.html",
                    "/v3/api-docs/**",
                    "/v3/api-docs.yaml"
                ).permitAll()
                .requestMatchers("/api/rbac/**").hasRole("ADMIN")
                .requestMatchers("/api/admin/**").hasRole("ADMIN")
                .requestMatchers("/api/users/**").authenticated()
                .requestMatchers("/api/devices/**").authenticated()
                .requestMatchers("/api/telemetry/**").authenticated()
                .requestMatchers("/api/commands/**").authenticated()
                .requestMatchers("/api/groups/**").authenticated()
                .requestMatchers("/api/alerts/**").authenticated()
                .requestMatchers("/api/tasks/**").authenticated()
                .requestMatchers("/api/billing/**").authenticated()
                .requestMatchers("/api/dorm/**").authenticated()
                .requestMatchers("/api/students/**").authenticated()
                .requestMatchers("/api/rooms/**").authenticated()
                .requestMatchers("/api/notifications/**").authenticated()
                .anyRequest().authenticated()
            )
            .addFilterBefore(jwtAuthenticationFilter, UsernamePasswordAuthenticationFilter.class);

        return http.build();
    }
}
```

这段代码有两个教学上必须强调的点：

1. **当前项目已经按 JWT 无状态思路实现认证**，但 `SecurityConfig` 里并没有显式设置 `SessionCreationPolicy.STATELESS`。也就是说，课堂上要区分“当前实现”与“推荐补强项”。
2. **路径授权是项目级最直观的安全边界**。学生第一次读安全配置，不要先陷入底层原理，而要先能看懂哪些接口公开、哪些接口需要登录、哪些接口必须管理员才能访问。

#### 授权规则详解

| 规则                             | 说明                 | 示例           |
| -------------------------------- | -------------------- | -------------- |
| `permitAll()`                    | 完全公开，无需认证   | 登录、健康检查 |
| `denyAll()`                      | 完全拒绝             | 未实现接口     |
| `authenticated()`                | 需要认证（任何用户） | 个人资料       |
| `hasRole("ADMIN")`               | 需要指定角色         | 管理员后台     |
| `hasAuthority("device:read")`    | 需要指定权限         | 设备读取       |
| `hasAnyRole("ADMIN", "USER")`    | 满足任一角色         | 多角色访问     |
| `hasIpAddress("192.168.1.0/24")` | IP白名单             | 内网访问       |

### 1.3 过滤器链执行顺序

```
HTTP请求
    ↓
1. WebAsyncManagerIntegrationFilter - 异步处理集成
    ↓
2. SecurityContextPersistenceFilter - 安全上下文持久化
    ↓
3. HeaderWriterFilter - 添加安全响应头
    ↓
4. CorsFilter - CORS跨域处理 ★
    ↓
5. LogoutFilter - 登出处理
    ↓
6. JwtAuthenticationFilter - JWT令牌验证 ★（自定义）
    ↓
7. UsernamePasswordAuthenticationFilter - 表单登录
    ↓
8. AuthorizationFilter - 授权检查 ★
    ↓
9. FilterSecurityInterceptor - 安全拦截器
    ↓
... 其他过滤器
    ↓
到达Controller
```

### 1.4 CORS跨域配置

```java
@Configuration
public class CorsConfig {

    @Bean
    public CorsConfigurationSource corsConfigurationSource() {
        CorsConfiguration config = new CorsConfiguration();

        // 允许的来源（生产环境应限制为具体域名）
        // 注意：allowCredentials=true时不能使用"*"
        config.setAllowedOrigins(Arrays.asList(
            "http://localhost:3000",
            "http://localhost:5173"  // Vite开发服务器
        ));

        // 允许的HTTP方法
        config.setAllowedMethods(Arrays.asList(
            "GET", "POST", "PUT", "DELETE", "OPTIONS", "PATCH"
        ));

        // 允许的请求头
        config.setAllowedHeaders(Arrays.asList(
            "Authorization",   // JWT令牌
            "Content-Type",    // 请求体类型
            "X-Requested-With",
            "Accept",
            "Origin"
        ));

        // 是否允许携带Cookie
        config.setAllowCredentials(true);

        // 预检请求缓存时间（秒）
        config.setMaxAge(3600L);

        // 允许暴露的响应头
        config.setExposedHeaders(Arrays.asList(
            "Authorization",
            "Content-Disposition"
        ));

        // 注册配置到所有路径
        UrlBasedCorsConfigurationSource source = new UrlBasedCorsConfigurationSource();
        source.registerCorsConfiguration("/**", config);

        return source;
    }
}
```

**CORS预检请求流程**：

```
浏览器发送复杂请求（如带Authorization头的POST）
    ↓
浏览器自动发送OPTIONS预检请求
    ↓
服务器返回CORS响应头
    ├─ Access-Control-Allow-Origin
    ├─ Access-Control-Allow-Methods
    ├─ Access-Control-Allow-Headers
    └─ Access-Control-Max-Age
    ↓
浏览器检查响应头
    ├─ 允许 → 发送实际请求
    └─ 拒绝 → 报错，不发送实际请求
```

---

## 2. JWT令牌认证机制

### 2.1 JWT令牌结构

JWT（JSON Web Token）由三部分组成，用点号分隔：

```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJhZG1pbiIsInJvbGUiOiJBRE1JTiIsImlhdCI6MTcwOTYwMDAwMCwiZXhwIjoxNzA5Njg2NDAwfQ.SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c
│                              │                                                                  │
└─────── Header ───────────────┴──────────────────────── Payload ────────────────────────────────┴────── Signature ──────
```

#### Header（头部）

```json
{
  "alg": "HS256", // 签名算法：HMAC-SHA256
  "typ": "JWT" // Token类型
}
```

#### Payload（载荷）

```json
{
  "sub": "admin", // 主题（用户名）
  "role": "ADMIN", // 角色
  "iat": 1709600000, // 签发时间（Unix时间戳）
  "exp": 1709686400 // 过期时间（24小时后）
}
```

#### Signature（签名）

```
HMACSHA256(
  base64UrlEncode(header) + "." + base64UrlEncode(payload),
  secret  // 服务器密钥
)
```

### 2.2 JWT工具类实现

```java
@Component
public class JwtUtil {

    @Value("${security.jwt.secret}")
    private String secret;  // Base64编码的密钥

    @Value("${security.jwt.expiration:86400000}")  // 默认24小时
    private long expiration;

    @Autowired
    private TokenBlacklist tokenBlacklist;

    /**
     * 生成JWT令牌
     *
     * @param username 用户名
     * @param role 角色
     * @return JWT字符串
     */
    public String generateToken(String username, String role) {
        Date now = new Date();
        Date expiryDate = new Date(now.getTime() + expiration);

        return Jwts.builder()
                .setSubject(username)                    // 主题（用户名）
                .claim("username", username)             // 自定义声明
                .claim("role", role)                     // 角色声明
                .setIssuedAt(now)                        // 签发时间
                .setExpiration(expiryDate)               // 过期时间
                .signWith(getSigningKey(), SignatureAlgorithm.HS256)  // HMAC-SHA256签名
                .compact();                              // 压缩为字符串
    }

    /**
     * 验证令牌有效性
     *
     * @param token JWT令牌
     * @return 是否有效
     */
    public boolean validateToken(String token) {
        // 1. 检查黑名单（已登出的令牌）
        if (tokenBlacklist.isBlacklisted(token)) {
            return false;
        }

        try {
            // 2. 解析并验证签名
            Jwts.parserBuilder()
                    .setSigningKey(getSigningKey())
                    .build()
                    .parseClaimsJws(token);
            return true;
        } catch (ExpiredJwtException e) {
            logger.warn("JWT令牌已过期: {}", e.getMessage());
        } catch (UnsupportedJwtException e) {
            logger.warn("不支持的JWT令牌: {}", e.getMessage());
        } catch (MalformedJwtException e) {
            logger.warn("无效的JWT令牌: {}", e.getMessage());
        } catch (SignatureException e) {
            logger.warn("JWT签名验证失败: {}", e.getMessage());
        } catch (IllegalArgumentException e) {
            logger.warn("JWT令牌为空: {}", e.getMessage());
        }
        return false;
    }

    /**
     * 从令牌获取用户名
     */
    public String getUsernameFromToken(String token) {
        Claims claims = Jwts.parserBuilder()
                .setSigningKey(getSigningKey())
                .build()
                .parseClaimsJws(token)
                .getBody();
        return claims.getSubject();
    }

    /**
     * 从令牌获取角色
     */
    public String getRoleFromToken(String token) {
        Claims claims = Jwts.parserBuilder()
                .setSigningKey(getSigningKey())
                .build()
                .parseClaimsJws(token)
                .getBody();
        return claims.get("role", String.class);
    }

    /**
     * 将令牌加入黑名单（登出）
     */
    public void blacklistToken(String token) {
        tokenBlacklist.addToBlacklist(token);
    }

    /**
     * 获取签名密钥
     * 将Base64编码的字符串转换为SecretKey对象
     */
    private SecretKey getSigningKey() {
        byte[] keyBytes = Decoders.BASE64.decode(secret);
        return Keys.hmacShaKeyFor(keyBytes);
    }
}
```

### 2.3 JWT认证过滤器

```java
@Component
public class JwtAuthenticationFilter extends OncePerRequestFilter {

    @Autowired
    private JwtUtil jwtUtil;

    @Autowired
    private RbacService rbacService;

    @Override
    protected void doFilterInternal(
            HttpServletRequest request,
            HttpServletResponse response,
            FilterChain filterChain
    ) throws ServletException, IOException {

        // ========== 步骤1: 从请求头提取Token ==========
        String token = getTokenFromRequest(request);
        // Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

        // ========== 步骤2: 验证Token ==========
        if (token != null && jwtUtil.validateToken(token)) {
            try {
                // ========== 步骤3: 解析JWT载荷 ==========
                Claims claims = Jwts.parserBuilder()
                        .setSigningKey(getSigningKey())
                        .build()
                        .parseClaimsJws(token)
                        .getBody();

                String username = claims.getSubject();      // "admin"
                String role = claims.get("role", String.class);  // "ADMIN"

                // ========== 步骤4: 获取用户权限 ==========
                List<SimpleGrantedAuthority> authorities = getUserAuthorities(username, role);
                // ["ROLE_ADMIN", "device:read", "device:write", ...]

                // ========== 步骤5: 创建认证对象 ==========
                UsernamePasswordAuthenticationToken authentication =
                    new UsernamePasswordAuthenticationToken(username, null, authorities);

                // 设置请求详情（IP、SessionID等）
                authentication.setDetails(
                    new WebAuthenticationDetailsSource().buildDetails(request)
                );

                // ========== 步骤6: 设置到SecurityContext ==========
                // SecurityContext是线程局部变量，在整个请求期间有效
                SecurityContextHolder.getContext().setAuthentication(authentication);

            } catch (Exception e) {
                // Token无效，继续过滤链（未认证状态）
                logger.error("JWT认证失败: {}", e.getMessage());
            }
        }

        // ========== 步骤7: 继续执行过滤器链 ==========
        filterChain.doFilter(request, response);
    }

    /**
     * 从请求头提取Bearer Token
     */
    private String getTokenFromRequest(HttpServletRequest request) {
        String bearerToken = request.getHeader("Authorization");
        if (bearerToken != null && bearerToken.startsWith("Bearer ")) {
            return bearerToken.substring(7);  // 去掉"Bearer "前缀
        }
        return null;
    }

    /**
     * 从RBAC系统获取用户权限
     */
    private List<SimpleGrantedAuthority> getUserAuthorities(String username, String defaultRole) {
        List<SimpleGrantedAuthority> authorities = new ArrayList<>();

        try {
            // 从RBAC系统查询用户角色
            List<Role> roles = rbacService.getUserRoles(username);

            if (roles.isEmpty() && defaultRole != null) {
                // 无分配角色，使用默认角色
                authorities.add(new SimpleGrantedAuthority("ROLE_" + defaultRole.toUpperCase()));
            } else {
                // 添加角色和权限
                for (Role role : roles) {
                    if (role.isEnabled()) {
                        // 角色权限（如ROLE_ADMIN）
                        authorities.add(new SimpleGrantedAuthority("ROLE_" + role.getCode().toUpperCase()));

                        // 具体权限（如device:read）
                        for (Permission permission : role.getPermissions()) {
                            if (permission.isEnabled()) {
                                authorities.add(new SimpleGrantedAuthority(permission.getCode()));
                            }
                        }
                    }
                }
            }
        } catch (Exception e) {
            // 获取权限失败，降级为默认角色
            logger.error("获取用户权限失败: {}", e.getMessage());
            if (defaultRole != null) {
                authorities.add(new SimpleGrantedAuthority("ROLE_" + defaultRole.toUpperCase()));
            }
        }

        return authorities;
    }
}
```

### 2.4 令牌黑名单实现

```java
@Component
public class TokenBlacklist {

    // 线程安全的HashMap，存储令牌及其加入黑名单的时间戳
    private final ConcurrentHashMap<String, Long> blacklist = new ConcurrentHashMap<>();

    // 令牌有效期（与JWT过期时间一致，2小时）
    private static final long EXPIRATION_MS = 2 * 60 * 60 * 1000;

    /**
     * 将令牌加入黑名单
     * 用于用户登出时使令牌立即失效
     */
    public void addToBlacklist(String token) {
        // 记录当前时间戳
        blacklist.put(token, System.currentTimeMillis());

        // 异步清理过期的黑名单条目，避免阻塞
        CompletableFuture.runAsync(this::cleanupExpiredTokens);
    }

    /**
     * 检查令牌是否在黑名单中
     */
    public boolean isBlacklisted(String token) {
        Long timestamp = blacklist.get(token);
        if (timestamp == null) {
            return false;  // 未在黑名单中
        }

        // 检查是否已过期（黑名单有效期2小时）
        return System.currentTimeMillis() - timestamp < EXPIRATION_MS;
    }

    /**
     * 清理过期的黑名单条目
     * 定期清理，避免内存泄漏
     */
    private void cleanupExpiredTokens() {
        long now = System.currentTimeMillis();

        // 移除所有过期的条目
        blacklist.entrySet().removeIf(entry ->
            now - entry.getValue() > EXPIRATION_MS
        );

        logger.debug("黑名单清理完成，当前大小: {}", blacklist.size());
    }

    /**
     * 获取黑名单大小（用于监控）
     */
    public int getSize() {
        return blacklist.size();
    }
}
```

**黑名单机制的优势**：

- 实现令牌即时失效（登出功能）
- 无需维护服务端Session
- 分布式环境下每个实例独立维护
- 自动清理过期条目，内存占用可控

---

## 3. AOP切面编程

### 3.1 AOP核心概念

AOP（Aspect-Oriented Programming，面向切面编程）是一种编程范式，用于将横切关注点与业务逻辑分离。

| 概念                    | 说明                   | 代码示例                             |
| ----------------------- | ---------------------- | ------------------------------------ |
| **切面 (Aspect)**       | 横切关注点的模块化     | `@Aspect @Component class ApiAspect` |
| **连接点 (Join Point)** | 程序执行的某个特定位置 | 方法调用、异常抛出                   |
| **通知 (Advice)**       | 在连接点执行的动作     | `@Around`, `@Before`, `@After`       |
| **切点 (Pointcut)**     | 匹配连接点的谓词       | `@annotation(RateLimit)`             |
| **目标对象 (Target)**   | 被通知的对象           | Controller、Service                  |

### 3.2 通知类型详解

```java
@Aspect
@Component
public class LoggingAspect {

    private static final Logger logger = LoggerFactory.getLogger(LoggingAspect.class);

    // ========== 1. 环绕通知 (@Around) ==========
    // 最强大的通知类型，可以控制方法执行
    @Around("execution(* com.dormpower.service.*.*(..))")
    public Object logAround(ProceedingJoinPoint joinPoint) throws Throwable {
        long start = System.currentTimeMillis();

        // 前置逻辑
        logger.info("方法开始: {}", joinPoint.getSignature());

        try {
            // 执行目标方法
            Object result = joinPoint.proceed();

            // 后置逻辑
            long duration = System.currentTimeMillis() - start;
            logger.info("方法完成: {} - {}ms", joinPoint.getSignature(), duration);

            return result;

        } catch (Throwable e) {
            // 异常逻辑
            logger.error("方法异常: {} - {}", joinPoint.getSignature(), e.getMessage());
            throw e;
        }
    }

    // ========== 2. 前置通知 (@Before) ==========
    // 方法执行前执行，不能阻止方法执行
    @Before("execution(* com.dormpower.controller.*.*(..))")
    public void logBefore(JoinPoint joinPoint) {
        logger.info("Controller方法调用: {}", joinPoint.getSignature());
    }

    // ========== 3. 后置通知 (@After) ==========
    // 方法执行后执行，无论是否异常
    @After("execution(* com.dormpower.repository.*.*(..))")
    public void logAfter(JoinPoint joinPoint) {
        logger.debug("Repository方法结束: {}", joinPoint.getSignature());
    }

    // ========== 4. 返回通知 (@AfterReturning) ==========
    // 方法正常返回后执行，可以获取返回值
    @AfterReturning(
        pointcut = "execution(* com.dormpower.service.*.*(..))",
        returning = "result"
    )
    public void logAfterReturning(JoinPoint joinPoint, Object result) {
        logger.info("方法返回: {} - 结果: {}", joinPoint.getSignature(), result);
    }

    // ========== 5. 异常通知 (@AfterThrowing) ==========
    // 方法抛出异常后执行，可以获取异常信息
    @AfterThrowing(
        pointcut = "execution(* com.dormpower.service.*.*(..))",
        throwing = "ex"
    )
    public void logAfterThrowing(JoinPoint joinPoint, Exception ex) {
        logger.error("方法异常: {} - {}", joinPoint.getSignature(), ex.getMessage());
    }
}
```

### 3.3 切点表达式语法

```java
@Aspect
@Component
public class PointcutExamples {

    // ========== 1. execution - 匹配方法执行 ==========
    // 格式: execution(修饰符? 返回类型 包名.类名.方法名(参数) 异常?)

    // 匹配service包下所有类的所有方法
    @Pointcut("execution(* com.dormpower.service.*.*(..))")
    public void serviceMethods() {}

    // 匹配特定类的特定方法
    @Pointcut("execution(public * com.dormpower.service.AuthService.login(..))")
    public void loginMethod() {}

    // 匹配所有public方法
    @Pointcut("execution(public * *(..))")
    public void publicMethods() {}

    // ========== 2. @annotation - 匹配带注解的方法 ==========
    @Pointcut("@annotation(com.dormpower.annotation.RateLimit)")
    public void rateLimitedMethods() {}

    @Pointcut("@annotation(com.dormpower.annotation.AuditLog)")
    public void auditedMethods() {}

    // ========== 3. @within - 匹配带注解的类 ==========
    // 匹配所有@RestController注解的类中的方法
    @Pointcut("@within(org.springframework.web.bind.annotation.RestController)")
    public void controllerMethods() {}

    // ========== 4. within - 匹配指定包或类 ==========
    // 匹配controller包及其子包下的所有类
    @Pointcut("within(com.dormpower.controller..*)")
    public void controllerPackage() {}

    // ========== 5. args - 匹配参数类型 ==========
    // 匹配第一个参数为String类型的方法
    @Pointcut("execution(* *(String,..))")
    public void firstParamString() {}

    // 匹配参数为(String, int)的方法
    @Pointcut("args(String, int)")
    public void stringIntArgs() {}

    // ========== 6. 组合使用 ==========
    // 带@RateLimit注解的方法 OR @RestController注解的类中的方法
    @Pointcut("@annotation(RateLimit) || @within(RestController)")
    public void rateLimitOrController() {}

    // 带@AuditLog注解的方法 AND 在controller包下
    @Pointcut("@annotation(AuditLog) && within(com.dormpower.controller..*)")
    public void auditedControllerMethods() {}
}
```

**切点表达式通配符**：

- `*`：匹配任意字符（不包括包分隔符）
- `..`：匹配任意数量的包或参数
- `+`：匹配指定类及其子类

### 3.4 实战：API切面实现

```java
@Aspect
@Component
public class ApiAspect {

    private static final Logger logger = LoggerFactory.getLogger(ApiAspect.class);
    private static final Logger auditLogger = LoggerFactory.getLogger("AUDIT");

    @Autowired
    private RateLimiter apiRateLimiter;

    @Autowired
    private RateLimiter loginRateLimiter;

    @Autowired
    private ConcurrentHashMap<String, RateLimiter> deviceRateLimiters;

    private final Map<String, RateLimiter> rateLimiters = new ConcurrentHashMap<>();

    @Around("@annotation(com.dormpower.annotation.RateLimit) || " +
            "@within(org.springframework.web.bind.annotation.RestController)")
    public Object handleApiRequest(ProceedingJoinPoint joinPoint) throws Throwable {
        long startTime = System.currentTimeMillis();
        HttpServletRequest request = getRequest();
        String method = request != null ? request.getMethod() : "UNKNOWN";
        String uri = request != null ? request.getRequestURI() : "UNKNOWN";
        String ip = request != null ? getClientIp(request) : "UNKNOWN";

        MethodSignature signature = (MethodSignature) joinPoint.getSignature();
        Method targetMethod = signature.getMethod();

        if (!isAdminUser()) {
            RateLimit rateLimit = targetMethod.getAnnotation(RateLimit.class);
            if (rateLimit != null) {
                RateLimiter limiter = getRateLimiter(rateLimit);
                if (!limiter.tryAcquire()) {
                    logger.warn("请求被限流: {} {} from {}", method, uri, ip);
                    throw new RuntimeException("请求过于频繁，请稍后再试");
                }
            } else {
                if (!apiRateLimiter.tryAcquire()) {
                    logger.warn("API请求被限流: {} {} from {}", method, uri, ip);
                    throw new RuntimeException("请求过于频繁，请稍后再试");
                }
            }
        }

        logger.debug("请求开始: {} {} from {}", method, uri, ip);

        try {
            Object result = joinPoint.proceed();
            long duration = System.currentTimeMillis() - startTime;
            logger.info("请求完成: {} {} - {}ms - {}", method, uri, duration, ip);
            return result;
        } catch (Throwable e) {
            long duration = System.currentTimeMillis() - startTime;
            logger.error("请求失败: {} {} - {}ms - {} - {}", method, uri, duration, ip, e.getMessage());
            throw e;
        }
    }

    @Around("@annotation(com.dormpower.annotation.AuditLog)")
    public Object handleAuditLog(ProceedingJoinPoint joinPoint) throws Throwable {
        MethodSignature signature = (MethodSignature) joinPoint.getSignature();
        Method method = signature.getMethod();
        AuditLog auditLog = method.getAnnotation(AuditLog.class);

        HttpServletRequest request = getRequest();
        String ip = request != null ? getClientIp(request) : "UNKNOWN";
        String user = request != null ? request.getHeader("X-User") : "anonymous";

        long startTime = System.currentTimeMillis();

        try {
            Object result = joinPoint.proceed();
            long duration = System.currentTimeMillis() - startTime;
            auditLogger.info("[{}] {} - {} - {}ms - SUCCESS",
                    auditLog.type(),
                    auditLog.value(),
                    user != null ? user : ip,
                    duration);
            return result;
        } catch (Throwable e) {
            long duration = System.currentTimeMillis() - startTime;
            auditLogger.error("[{}] {} - {} - {}ms - FAILED: {}",
                    auditLog.type(),
                    auditLog.value(),
                    user != null ? user : ip,
                    duration,
                    e.getMessage());
            throw e;
        }
    }

    private RateLimiter getRateLimiter(RateLimit rateLimit) {
        String key = rateLimit.type();
        return rateLimiters.computeIfAbsent(key, k -> RateLimiter.create(rateLimit.value()));
    }
}
```

教学时要提醒学生：`ApiAspect` 拦截的不只是“打了注解的方法”，还会覆盖 `@RestController` 中的方法，因此它承担的是系统级横切逻辑，而不是某一个业务模块的业务代码。

---

## 4. 限流机制实现

### 4.1 限流配置

```java
@Configuration
public class RateLimitConfig {

    @Bean
    public RateLimiter apiRateLimiter() {
        return RateLimiter.create(10.0);
    }

    @Bean
    public RateLimiter loginRateLimiter() {
        return RateLimiter.create(2.0);
    }

    @Bean
    public ConcurrentHashMap<String, RateLimiter> deviceRateLimiters() {
        return new ConcurrentHashMap<>();
    }
}
```

这里需要特别指出：项目里**没有**额外声明 `Map<String, RateLimiter>` 作为 Spring Bean，而是在 `ApiAspect` 内部维护了一个 `ConcurrentHashMap` 作为动态限流器容器。课堂上如果继续沿用通用模板，会把学生带偏。

### 4.2 自定义限流注解

```java
/**
 * 限流注解
 * 用于标记需要限流的方法
 */
@Target({ElementType.METHOD, ElementType.TYPE})
@Retention(RetentionPolicy.RUNTIME)
public @interface RateLimit {

    /**
     * 限流类型（用于区分不同的限流器）
     */
    String type() default "api";

    /**
     * 每秒允许的请求数
     */
    double value() default 10.0;

    /**
     * 限流提示信息
     */
    String message() default "请求过于频繁，请稍后再试";
}
```

### 4.3 令牌桶算法原理

```
┌─────────────────────────────────────┐
│         令牌桶 (Token Bucket)        │
├─────────────────────────────────────┤
│  容量: 10 (每秒生成10个令牌)         │
│  当前令牌数: ████████░░ (8/10)      │
│                                     │
│  生成速率: 每100ms生成1个令牌        │
│  ┌──────────────────────────────┐  │
│  │  时间轴:                      │  │
│  │  0ms    100ms   200ms   300ms │  │
│  │   ●      ●      ●      ●     │  │
│  │  (生成)  (生成) (生成) (生成)  │  │
│  └──────────────────────────────┘  │
└─────────────────────────────────────┘

请求到来:
1. 尝试获取令牌 (tryAcquire)
   ├─ 有令牌 → 放行，令牌数-1
   └─ 无令牌 → 拒绝

2. 令牌补充 (后台线程)
   每100ms补充1个令牌（平滑速率）
```

### 4.4 限流使用示例

```java
@RestController
@RequestMapping("/api/auth")
public class AuthController {

    @Autowired
    private AuthService authService;

    /**
     * 登录接口 - 每秒最多2个请求
     * 防止暴力破解攻击
     */
    @RateLimit(type = "login", value = 2.0)
    @AuditLog(value = "用户登录", type = "AUTH")
    @PostMapping("/login")
    public ResponseEntity<?> login(@Valid @RequestBody LoginRequest request) {
        Map<String, Object> result = authService.login(
            request.getAccount(),
            request.getPassword()
        );
        return ResponseEntity.ok(result);
    }

    /**
     * 设备控制接口 - 每秒最多5个请求
     */
    @RateLimit(type = "device-control", value = 5.0)
    @AuditLog(value = "设备控制", type = "DEVICE")
    @PostMapping("/devices/{deviceId}/control")
    public ResponseEntity<?> controlDevice(
            @PathVariable String deviceId,
            @RequestBody ControlRequest request) {
        deviceService.controlDevice(deviceId, request.getCommand());
        return ResponseEntity.ok(Map.of("message", "Command sent"));
    }
}
```

---

## 5. 缓存管理

### 5.1 Spring Cache注解

```java
@Service
public class DeviceService {

    @Autowired
    private DeviceRepository deviceRepository;

    /**
     * 查询设备列表（带缓存）
     */
    @Cacheable(value = "devices", unless = "#result == null || #result.isEmpty()")
    public List<Map<String, Object>> getDevices() {
        List<Device> devices = deviceRepository.findAll();
        return devices.stream()
            .map(this::toMap)
            .collect(Collectors.toList());
    }

    @Cacheable(value = "deviceStatus", key = "#deviceId")
    public Map<String, Object> getDeviceStatus(String deviceId) {
        Device device = deviceRepository.findById(deviceId).orElse(null);
        Map<String, Object> status = new HashMap<>();
        status.put("deviceId", deviceId);
        status.put("online", device != null && device.isOnline());
        return status;
    }

    @CacheEvict(value = {"devices", "deviceStatus"}, allEntries = true)
    public void updateDeviceStatus(String deviceId, boolean online) {
        // 更新数据库后统一清理相关缓存
    }

    @CacheEvict(value = "devices", allEntries = true)
    public Device addDevice(Device device) {
        return deviceRepository.save(device);
    }
}
```

项目里除了 `DeviceService`，`TelemetryService` 也使用了 `@Cacheable(value = "telemetry", key = "#deviceId + '_' + #range")`。这非常适合在课堂上讲“**查询类接口适合缓存，写操作要负责失效**”这条工程规律。

### 5.2 缓存配置

```java
@Configuration
@EnableCaching
public class CacheConfig {

    @Bean
    public CacheManager cacheManager() {
        return new ConcurrentMapCacheManager(
            "devices",
            "deviceStatus",
            "telemetry"
        );
    }
}
```

除了 Spring Cache，这个项目还有一个 `SimpleCacheService`，它通过 `ConcurrentHashMap + 定时清理线程` 提供轻量级内存缓存，用于低内存场景的补充方案。教学上建议把它讲成“同一系统里的两种缓存思路”：

- **声明式缓存**：适合常规查询接口，使用成本低。
- **手动缓存服务**：适合需要自定义过期、容量控制和显式清理的场景。

### 5.3 缓存策略选择

| 缓存方案                | 适用场景       | 优点             | 缺点           |
| ----------------------- | -------------- | ---------------- | -------------- |
| **本地缓存 (Caffeine)** | 单机、高性能   | 低延迟、高性能   | 不支持集群共享 |
| **分布式缓存 (Redis)**  | 集群、共享状态 | 数据共享、持久化 | 网络延迟       |
| **多级缓存**            | 高并发场景     | 兼顾性能和一致性 | 实现复杂       |

### 5.4 缓存常见问题

**缓存穿透**：查询不存在的数据，缓存无法命中

```java
// 解决方案：缓存空值
@Cacheable(value = "users", key = "#username", unless = "#result == null")
public User getUser(String username) {
    return userRepository.findByUsername(username)
        .orElse(null);  // 缓存null值
}
```

**缓存击穿**：热点key过期，大量请求打到数据库

```java
// 解决方案：加互斥锁
public User getUserWithLock(String username) {
    String key = "user:" + username;
    User user = cache.get(key);
    if (user == null) {
        synchronized (this) {
            user = cache.get(key);  // 双重检查
            if (user == null) {
                user = userRepository.findByUsername(username);
                cache.put(key, user, 30, TimeUnit.MINUTES);
            }
        }
    }
    return user;
}
```

**缓存雪崩**：大量key同时过期

```java
// 解决方案：随机过期时间
int randomExpire = 30 + new Random().nextInt(10);  // 30-40分钟
cache.put(key, value, randomExpire, TimeUnit.MINUTES);
```

---

## 本章小结

### 关键知识点

1. **Spring Security**：路径级授权和 JWT 过滤器共同构成第一道安全边界。
2. **JWT令牌**：当前项目通过 `JwtUtil` 生成与校验 Token，并借助黑名单支持登出失效。
3. **AOP切面**：`ApiAspect` 统一承担日志、限流和审计这类横切逻辑。
4. **限流机制**：项目使用 Guava `RateLimiter`，包含全局限流、登录限流和动态类型限流。
5. **缓存管理**：项目同时使用 Spring Cache 与 `SimpleCacheService` 两种本地缓存思路。

### 实践建议

1. 让学生按 `SecurityConfig` → `JwtAuthenticationFilter` → `JwtUtil` 的顺序走读一次登录后的请求链路。
2. 用 `AuthController` 的 `@RateLimit` 和 `@AuditLog` 作为切入点讲 AOP 的工程价值。
3. 对当前实现要敢于指出改进项，例如显式声明无状态 Session、收紧 WebSocket 跨域、让 Token 黑名单持久化。
4. 讲缓存时不要只讲命中率，要同步讲清楚“谁负责失效、何时清理、错了会有什么后果”。

### 下一章预告

第三章将深入讲解**业务逻辑与数据层设计**，包括：

- Controller/Service/Repository/Entity四层架构
- JPA实体映射与关系
- 自定义查询与JPQL
- 事务管理最佳实践
