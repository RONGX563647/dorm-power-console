# 系统管理模块 - 功能设计与实现细节文档

## 1. 模块概述

### 1.1 模块定位

系统管理模块是宿舍电源管理系统的基础设施模块，负责提供用户认证、权限管理、日志审计等系统管理功能。该模块采用RBAC权限模型，为系统提供安全可靠的用户管理和权限控制能力。

### 1.2 模块职责

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          系统管理模块职责边界                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                        核心职责                                   │   │
│  │  - 用户认证与授权                                                  │   │
│  │  - 用户管理（CRUD操作）                                            │   │
│  │  - 角色权限管理（RBAC）                                            │   │
│  │  - 操作日志审计                                                    │   │
│  │  - 系统配置管理                                                    │   │
│  │  - 数据字典管理                                                    │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                        边界职责                                   │   │
│  │  - 与所有模块协作：提供认证和权限校验                               │   │
│  │  - 与审计模块协作：记录操作日志                                    │   │
│  │  - 与前端协作：提供用户信息和权限数据                               │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                        非职责范围                                  │   │
│  │  - 设备管理（由设备模块负责）                                      │   │
│  │  - 遥测数据处理（由遥测模块负责）                                  │   │
│  │  - 告警规则配置（由告警模块负责）                                  │   │
│  │  - 实时通信（由通信模块负责）                                      │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 1.3 功能点总览

| 功能点 | 功能描述 | 优先级 |
|--------|----------|--------|
| 用户登录/登出 | JWT令牌认证 | P0 |
| 用户管理 | 用户CRUD操作 | P0 |
| 角色权限管理 | RBAC权限控制 | P0 |
| 操作日志审计 | 记录用户操作行为 | P1 |
| 系统配置管理 | 系统参数配置 | P1 |
| 数据字典管理 | 枚举值配置管理 | P2 |

---

## 2. RBAC权限模型

### 2.1 模型设计

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          RBAC权限模型                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────┐         ┌──────────────┐         ┌──────────────┐    │
│  │    User      │         │    Role      │         │ Permission   │    │
│  ├──────────────┤         ├──────────────┤         ├──────────────┤    │
│  │ username(PK) │         │ id (PK)      │         │ id (PK)      │    │
│  │ email        │         │ code         │         │ code         │    │
│  │ passwordHash │         │ name         │         │ name         │    │
│  │ role         │         │ description  │         │ action       │    │
│  │ enabled      │         │ enabled      │         │ enabled      │    │
│  │ createdAt    │         │ system       │         │ resource_id  │    │
│  └──────────────┘         └──────────────┘         └──────────────┘    │
│         │                        │                        │             │
│         │                        │                        │             │
│         │     ┌──────────────────┴──────────────────┐     │             │
│         │     │                                    │     │             │
│         │     ▼                                    ▼     │             │
│         │  ┌──────────────┐              ┌──────────────┐│             │
│         │  │  UserRole    │              │RolePermission││             │
│         │  ├──────────────┤              ├──────────────┤│             │
│         │  │ username(FK) │              │ role_id(FK)  ││             │
│         │  │ role_id(FK)  │              │ perm_id(FK)  ││             │
│         │  │ assignedBy   │              └──────────────┘│             │
│         │  │ assignedAt   │                      │       │             │
│         │  └──────────────┘                      │       │             │
│         │         │                              │       │             │
│         └─────────┴──────────────────────────────┴───────┘             │
│                                                                         │
│  关系说明：                                                              │
│  - User ──多对多──> Role（通过UserRole关联）                             │
│  - Role ──多对多──> Permission（通过RolePermission关联）                 │
│  - Permission ──多对一──> Resource                                      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.2 实体设计

#### 2.2.1 用户实体（UserAccount）

```java
@Entity
@Table(name = "user_account", indexes = {
    @Index(name = "idx_email", columnList = "email", unique = true)
})
public class UserAccount {
    @Id
    private String username;        // 用户名（主键）
    
    @NotNull
    private String email;           // 邮箱
    
    @NotNull
    private String passwordHash;    // 密码哈希
    
    @NotNull
    private String role;            // 默认角色
    
    @NotNull
    private boolean enabled;        // 是否启用
    
    private String resetCodeHash;   // 密码重置码哈希
    private long resetExpiresAt;    // 重置码过期时间
    
    @NotNull
    private long createdAt;         // 创建时间
    
    @NotNull
    private long updatedAt;         // 更新时间
}
```

#### 2.2.2 角色实体（Role）

```java
@Entity
@Table(name = "roles", indexes = {
    @Index(name = "idx_role_code", columnList = "code", unique = true)
})
public class Role {
    @Id
    private String id;              // 角色ID
    
    @NotBlank
    @Column(unique = true)
    private String code;            // 角色编码（如：admin、user）
    
    @NotBlank
    private String name;            // 角色名称
    
    private String description;     // 角色描述
    
    @NotNull
    private boolean enabled;        // 是否启用
    
    @NotNull
    private boolean system;         // 是否系统角色（不可删除）
    
    @ManyToMany(fetch = FetchType.LAZY)
    @JoinTable(
        name = "role_permissions",
        joinColumns = @JoinColumn(name = "role_id"),
        inverseJoinColumns = @JoinColumn(name = "permission_id")
    )
    private Set<Permission> permissions;
    
    @NotNull
    private long createdAt;
    
    @NotNull
    private long updatedAt;
}
```

#### 2.2.3 权限实体（Permission）

```java
@Entity
@Table(name = "permissions", indexes = {
    @Index(name = "idx_permission_code", columnList = "code", unique = true)
})
public class Permission {
    @Id
    private String id;              // 权限ID
    
    @NotBlank
    @Column(unique = true)
    private String code;            // 权限编码（如：api:devices:read）
    
    @NotBlank
    private String name;            // 权限名称
    
    private String description;     // 权限描述
    
    @NotNull
    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "resource_id")
    private Resource resource;      // 关联资源
    
    @NotNull
    private String action;          // 操作类型（create、read、update、delete）
    
    @NotNull
    private boolean enabled;        // 是否启用
    
    @NotNull
    private long createdAt;
    
    @NotNull
    private long updatedAt;
}
```

#### 2.2.4 资源实体（Resource）

```java
@Entity
@Table(name = "resources", indexes = {
    @Index(name = "idx_resource_code", columnList = "code", unique = true)
})
public class Resource {
    @Id
    private String id;              // 资源ID
    
    @NotBlank
    @Column(unique = true)
    private String code;            // 资源编码（如：api:devices）
    
    @NotBlank
    private String name;            // 资源名称
    
    private String description;     // 资源描述
    
    @NotNull
    private String type;            // 资源类型（API、MENU、BUTTON）
    
    private String url;             // 资源URL
    
    private String method;          // HTTP方法
    
    private Long parentId;          // 父资源ID
    
    private Integer sortOrder;      // 排序
    
    @NotNull
    private boolean enabled;        // 是否启用
    
    @NotNull
    private long createdAt;
    
    @NotNull
    private long updatedAt;
}
```

---

## 3. 功能点详细设计

### 3.1 用户登录/登出（P0）

#### 3.1.1 功能描述

使用JWT令牌实现用户认证，支持登录、登出、令牌刷新等功能。

#### 3.1.2 业务流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          用户登录流程                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐      │
│  │ 接收请求  │────>│ 限流检查  │────>│ 验证账号  │────>│ 生成令牌  │      │
│  │          │     │          │     │ 密码     │     │          │      │
│  └──────────┘     └──────────┘     └──────────┘     └──────────┘      │
│       │                │                │                │            │
│       │                │ 超限            │ 失败            │            │
│       │                ▼                ▼                ▼            │
│       │          ┌──────────┐     ┌──────────┐     ┌──────────┐       │
│       │          │ 返回429  │     │ 返回401  │     │ 返回令牌  │       │
│       │          │ 限流     │     │ 认证失败  │     │ 记录日志  │       │
│       │          └──────────┘     └──────────┘     └──────────┘       │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 3.1.3 业务规则

| 规则编号 | 规则描述 | 规则值 |
|----------|----------|--------|
| BR-AUTH-001 | 限流策略 | 2次/秒/IP |
| BR-AUTH-002 | 令牌有效期 | 24小时 |
| BR-AUTH-003 | 密码加密 | BCrypt哈希 |
| BR-AUTH-004 | 登出处理 | 令牌加入黑名单 |
| BR-AUTH-005 | 审计日志 | 记录登录/登出操作 |

#### 3.1.4 API接口设计

**接口：用户登录**

```
POST /api/auth/login
Content-Type: application/json

Request Body:
{
    "account": "admin",
    "password": "admin123"
}

Response 200:
{
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "expiresAt": 1678972800000,
    "user": {
        "username": "admin",
        "email": "admin@example.com",
        "role": "admin"
    }
}

Response 401:
{
    "message": "Invalid username or password"
}
```

**接口：用户登出**

```
POST /api/auth/logout
Authorization: Bearer {token}

Response 200:
{
    "message": "Logged out successfully",
    "timestamp": 1678886400000
}
```

**接口：刷新令牌**

```
POST /api/auth/refresh
Authorization: Bearer {token}

Response 200:
{
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "expiresAt": 1678972800000
}
```

**接口：获取当前用户**

```
GET /api/auth/me
Authorization: Bearer {token}

Response 200:
{
    "username": "admin",
    "email": "admin@example.com",
    "role": "admin",
    "permissions": ["api:devices:read", "api:devices:update", ...]
}
```

#### 3.1.5 实现代码

```java
@RestController
@RequestMapping("/api/auth")
@Tag(name = "认证管理", description = "提供用户认证和令牌管理的RESTful API接口")
public class AuthController {

    @Autowired
    private JwtUtil jwtUtil;

    @Autowired
    private AuthService authService;

    /**
     * 用户登录
     */
    @Operation(summary = "用户登录", description = "使用账号密码登录系统，返回JWT令牌")
    @RateLimit(value = 2.0, type = "login")
    @AuditLog(value = "用户登录", type = "AUTH")
    @PostMapping("/login")
    public ResponseEntity<?> login(@Valid @RequestBody LoginRequest request) {
        String account = request.account();
        String password = request.password();

        Map<String, Object> result = authService.login(account, password);
        
        return ResponseEntity.ok(result);
    }

    /**
     * 用户登出
     */
    @Operation(summary = "用户登出", description = "退出登录状态")
    @PostMapping("/logout")
    public ResponseEntity<?> logout(
            @RequestHeader(value = "Authorization", required = false) String token) {
        if (token != null && token.startsWith("Bearer ")) {
            String jwtToken = token.substring(7);
            jwtUtil.blacklistToken(jwtToken);
        }

        Map<String, Object> response = new HashMap<>();
        response.put("message", "Logged out successfully");
        response.put("timestamp", System.currentTimeMillis());
        return ResponseEntity.ok(response);
    }

    /**
     * 获取当前用户信息
     */
    @Operation(summary = "获取当前用户", description = "获取当前登录用户的信息")
    @GetMapping("/me")
    public ResponseEntity<?> getCurrentUser(
            @RequestHeader(value = "Authorization", required = false) String token) {
        if (token == null || !token.startsWith("Bearer ")) {
            throw new AuthenticationException("Missing or invalid authorization header");
        }

        String jwtToken = token.substring(7);
        
        if (!jwtUtil.validateToken(jwtToken)) {
            throw new AuthenticationException("Invalid or expired token");
        }
        
        String username = jwtUtil.getUsernameFromToken(jwtToken);
        Map<String, Object> userInfo = authService.getCurrentUser(username);
        return ResponseEntity.ok(userInfo);
    }

    /**
     * 刷新令牌
     */
    @Operation(summary = "刷新令牌", description = "刷新JWT令牌，延长有效期")
    @PostMapping("/refresh")
    public Map<String, Object> refreshToken(
            @RequestHeader(value = "Authorization", required = false) String token) {
        String username = "admin";
        if (token != null && token.startsWith("Bearer ")) {
            String jwtToken = token.substring(7);
            try {
                username = jwtUtil.getUsernameFromToken(jwtToken);
            } catch (Exception e) {
                // 使用默认用户名
            }
        }
        
        Map<String, Object> response = new HashMap<>();
        response.put("token", jwtUtil.generateToken(username));
        response.put("expiresAt", System.currentTimeMillis() + 86400000L);
        return response;
    }
}
```

#### 3.1.6 测试用例

| 用例编号 | 测试场景 | 输入 | 预期输出 | 优先级 |
|----------|----------|------|----------|--------|
| TC-AUTH-001 | 正常登录 | 正确账号密码 | 返回JWT令牌 | P0 |
| TC-AUTH-002 | 密码错误 | 错误密码 | 返回401 | P0 |
| TC-AUTH-003 | 用户不存在 | 不存在的账号 | 返回401 | P0 |
| TC-AUTH-004 | 限流测试 | 短时间多次请求 | 返回429 | P1 |
| TC-AUTH-005 | 正常登出 | 有效令牌 | 令牌加入黑名单 | P0 |
| TC-AUTH-006 | 刷新令牌 | 有效令牌 | 返回新令牌 | P1 |

---

### 3.2 用户管理（P0）

#### 3.2.1 功能描述

提供用户的增删改查操作，包括用户创建、更新、删除和查询等功能。

#### 3.2.2 业务流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          用户管理流程                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  创建用户：                                                              │
│  ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐      │
│  │ 接收请求  │────>│ 验证参数  │────>│ 检查重复  │────>│ 创建用户  │      │
│  │          │     │          │     │          │     │          │      │
│  └──────────┘     └──────────┘     └──────────┘     └──────────┘      │
│                                                                         │
│  更新用户：                                                              │
│  ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐      │
│  │ 接收请求  │────>│ 验证权限  │────>│ 检查用户  │────>│ 更新用户  │      │
│  │          │     │          │     │ 存在     │     │          │      │
│  └──────────┘     └──────────┘     └──────────┘     └──────────┘      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 3.2.3 业务规则

| 规则编号 | 规则描述 | 规则值 |
|----------|----------|--------|
| BR-USER-001 | 用户名唯一 | 用户名不能重复 |
| BR-USER-002 | 邮箱唯一 | 邮箱不能重复 |
| BR-USER-003 | 密码强度 | 最少6位字符 |
| BR-USER-004 | 默认角色 | 新用户默认为user角色 |
| BR-USER-005 | 审计日志 | 记录用户操作 |

#### 3.2.4 API接口设计

**接口：获取用户列表**

```
GET /api/users
Authorization: Bearer {token}

Response 200:
[
    {
        "username": "admin",
        "email": "admin@example.com",
        "role": "admin",
        "enabled": true,
        "createdAt": 1678886400
    }
]
```

**接口：创建用户**

```
POST /api/users
Authorization: Bearer {token}
Content-Type: application/json

Request Body:
{
    "username": "user001",
    "email": "user001@example.com",
    "password": "password123",
    "role": "user"
}

Response 200:
{
    "username": "user001",
    "email": "user001@example.com",
    "role": "user",
    "enabled": true,
    "createdAt": 1678886400
}
```

**接口：更新用户**

```
PUT /api/users/{username}
Authorization: Bearer {token}
Content-Type: application/json

Request Body:
{
    "email": "newemail@example.com"
}

Response 200:
{
    "username": "user001",
    "email": "newemail@example.com",
    "updatedAt": 1678886500
}
```

**接口：删除用户**

```
DELETE /api/users/{username}
Authorization: Bearer {token}

Response 200:
{
    "message": "User deleted successfully"
}
```

#### 3.2.5 实现代码

```java
@Service
public class UserService {

    @Autowired
    private UserAccountRepository userAccountRepository;

    /**
     * 获取所有用户
     */
    public List<UserAccount> getAllUsers() {
        return userAccountRepository.findAll();
    }

    /**
     * 根据用户名获取用户
     */
    public UserAccount getUserByUsername(String username) {
        return userAccountRepository.findById(username)
            .orElseThrow(() -> new RuntimeException("User not found"));
    }

    /**
     * 更新用户
     */
    public UserAccount updateUser(String username, String email, String password) {
        UserAccount user = userAccountRepository.findById(username)
            .orElseThrow(() -> new RuntimeException("User not found"));

        if (email != null && !email.isEmpty()) {
            user.setEmail(email);
        }

        if (password != null && !password.isEmpty()) {
            user.setPasswordHash(EncryptionUtil.hashPassword(password));
        }

        user.setUpdatedAt(System.currentTimeMillis());
        return userAccountRepository.save(user);
    }

    /**
     * 删除用户
     */
    public void deleteUser(String username) {
        UserAccount user = userAccountRepository.findById(username)
            .orElseThrow(() -> new RuntimeException("User not found"));
        userAccountRepository.delete(user);
    }

    /**
     * 修改密码
     */
    public void changePassword(String username, String oldPassword, String newPassword) {
        UserAccount user = userAccountRepository.findById(username)
            .orElseThrow(() -> new RuntimeException("User not found"));
        
        if (!EncryptionUtil.verifyPassword(oldPassword, user.getPasswordHash())) {
            throw new RuntimeException("Old password is incorrect");
        }
        
        user.setPasswordHash(EncryptionUtil.hashPassword(newPassword));
        user.setUpdatedAt(System.currentTimeMillis());
        userAccountRepository.save(user);
    }
}
```

#### 3.2.6 测试用例

| 用例编号 | 测试场景 | 输入 | 预期输出 | 优先级 |
|----------|----------|------|----------|--------|
| TC-USER-001 | 创建用户 | 有效用户数据 | 用户创建成功 | P0 |
| TC-USER-002 | 用户名重复 | 已存在的用户名 | 返回400错误 | P0 |
| TC-USER-003 | 更新用户 | 有效更新数据 | 用户更新成功 | P0 |
| TC-USER-004 | 删除用户 | 有效用户名 | 用户删除成功 | P0 |
| TC-USER-005 | 修改密码 | 正确旧密码 | 密码修改成功 | P1 |

---

### 3.3 角色权限管理（P0）

#### 3.3.1 功能描述

基于RBAC模型实现角色和权限的管理，包括角色CRUD、权限分配、资源管理等功能。

#### 3.3.2 业务流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          角色权限管理流程                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  角色管理：                                                              │
│  ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐      │
│  │ 创建角色  │────>│ 分配权限  │────>│ 分配用户  │────>│ 启用角色  │      │
│  │          │     │          │     │          │     │          │      │
│  └──────────┘     └──────────┘     └──────────┘     └──────────┘      │
│                                                                         │
│  权限检查：                                                              │
│  ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐      │
│  │ 用户请求  │────>│ 获取角色  │────>│ 获取权限  │────>│ 权限判断  │      │
│  │          │     │          │     │          │     │          │      │
│  └──────────┘     └──────────┘     └──────────┘     └──────────┘      │
│                                                         │              │
│                                          ┌──────────────┼──────────┐   │
│                                          │              │          │   │
│                                          ▼              ▼          │   │
│                                    ┌──────────┐  ┌──────────┐      │   │
│                                    │ 允许访问  │  │ 拒绝访问  │      │   │
│                                    └──────────┘  └──────────┘      │   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 3.3.3 业务规则

| 规则编号 | 规则描述 | 规则值 |
|----------|----------|--------|
| BR-RBAC-001 | 系统角色保护 | 系统角色不可删除 |
| BR-RBAC-002 | 角色编码唯一 | 角色编码不能重复 |
| BR-RBAC-003 | 权限编码唯一 | 权限编码不能重复 |
| BR-RBAC-004 | 级联删除 | 删除角色时清除用户关联 |
| BR-RBAC-005 | 缓存策略 | 权限数据缓存，变更时清除 |

#### 3.3.4 API接口设计

**接口：获取角色列表**

```
GET /api/rbac/roles
Authorization: Bearer {token}

Response 200:
[
    {
        "id": "role_admin",
        "code": "admin",
        "name": "管理员",
        "description": "系统管理员，拥有所有权限",
        "enabled": true,
        "system": true
    }
]
```

**接口：创建角色**

```
POST /api/rbac/roles
Authorization: Bearer {token}
Content-Type: application/json

Request Body:
{
    "code": "dorm_manager",
    "name": "宿管",
    "description": "宿舍管理员"
}

Response 200:
{
    "id": "role_abc123",
    "code": "dorm_manager",
    "name": "宿管",
    "description": "宿舍管理员",
    "enabled": true
}
```

**接口：为角色分配权限**

```
POST /api/rbac/roles/{roleId}/permissions
Authorization: Bearer {token}
Content-Type: application/json

Request Body:
{
    "permissionIds": ["perm_001", "perm_002", "perm_003"]
}

Response 200:
{
    "id": "role_abc123",
    "permissions": [...]
}
```

**接口：为用户分配角色**

```
POST /api/rbac/users/{username}/roles
Authorization: Bearer {token}
Content-Type: application/json

Request Body:
{
    "roleIds": ["role_admin", "role_dorm_manager"]
}

Response 200:
{
    "message": "Roles assigned successfully"
}
```

**接口：检查用户权限**

```
GET /api/rbac/check?username={username}&permission={permissionCode}
Authorization: Bearer {token}

Response 200:
{
    "hasPermission": true
}
```

#### 3.3.5 实现代码

```java
@Service
public class RbacService {
    
    @Autowired
    private RoleRepository roleRepository;
    
    @Autowired
    private PermissionRepository permissionRepository;
    
    @Autowired
    private UserRoleRepository userRoleRepository;

    // ==================== 角色管理 ====================
    
    /**
     * 创建角色
     */
    public Role createRole(Role role) {
        if (roleRepository.existsByCode(role.getCode())) {
            throw new RuntimeException("Role code already exists: " + role.getCode());
        }
        role.setId("role_" + UUID.randomUUID().toString().substring(0, 8));
        role.setCreatedAt(System.currentTimeMillis() / 1000);
        role.setUpdatedAt(System.currentTimeMillis() / 1000);
        return roleRepository.save(role);
    }
    
    /**
     * 删除角色
     */
    @Transactional
    public void deleteRole(String roleId) {
        Role role = roleRepository.findById(roleId)
            .orElseThrow(() -> new RuntimeException("Role not found: " + roleId));
        
        if (role.isSystem()) {
            throw new RuntimeException("Cannot delete system role: " + role.getCode());
        }
        
        userRoleRepository.deleteByRoleId(roleId);
        roleRepository.delete(role);
    }
    
    /**
     * 为角色分配权限
     */
    @Transactional
    public Role assignPermissions(String roleId, List<String> permissionIds) {
        Role role = roleRepository.findById(roleId)
            .orElseThrow(() -> new RuntimeException("Role not found: " + roleId));
        
        Set<Permission> permissions = new HashSet<>(
            permissionRepository.findAllById(permissionIds));
        role.setPermissions(permissions);
        role.setUpdatedAt(System.currentTimeMillis() / 1000);
        
        return roleRepository.save(role);
    }
    
    // ==================== 用户角色管理 ====================
    
    /**
     * 为用户分配角色
     */
    @Transactional
    public void assignRolesToUser(String username, List<String> roleIds, String assignedBy) {
        for (String roleId : roleIds) {
            if (!userRoleRepository.existsByUsernameAndRoleId(username, roleId)) {
                UserRole userRole = new UserRole(username, roleId);
                userRole.setAssignedBy(assignedBy);
                userRoleRepository.save(userRole);
            }
        }
    }
    
    /**
     * 获取用户的角色
     */
    public List<Role> getUserRoles(String username) {
        List<String> roleIds = userRoleRepository.findRoleIdsByUsername(username);
        if (roleIds.isEmpty()) {
            return new ArrayList<>();
        }
        return roleRepository.findAllById(roleIds);
    }
    
    /**
     * 获取用户的所有权限
     */
    public Set<Permission> getUserPermissions(String username) {
        List<Role> roles = getUserRoles(username);
        Set<Permission> permissions = new HashSet<>();
        
        for (Role role : roles) {
            if (role.isEnabled() && role.getPermissions() != null) {
                permissions.addAll(role.getPermissions());
            }
        }
        
        return permissions;
    }
    
    /**
     * 检查用户是否有指定权限
     */
    public boolean hasPermission(String username, String permissionCode) {
        Set<Permission> permissions = getUserPermissions(username);
        return permissions.stream()
            .anyMatch(p -> p.getCode().equals(permissionCode) && p.isEnabled());
    }
    
    /**
     * 检查用户是否有指定角色
     */
    public boolean hasRole(String username, String roleCode) {
        List<Role> roles = getUserRoles(username);
        return roles.stream()
            .anyMatch(r -> r.getCode().equals(roleCode) && r.isEnabled());
    }
}
```

#### 3.3.6 测试用例

| 用例编号 | 测试场景 | 输入 | 预期输出 | 优先级 |
|----------|----------|------|----------|--------|
| TC-RBAC-001 | 创建角色 | 有效角色数据 | 角色创建成功 | P0 |
| TC-RBAC-002 | 删除系统角色 | 系统角色ID | 返回400错误 | P0 |
| TC-RBAC-003 | 分配权限 | 有效权限ID | 权限分配成功 | P0 |
| TC-RBAC-004 | 用户角色分配 | 有效用户和角色 | 分配成功 | P0 |
| TC-RBAC-005 | 权限检查 | 有权限的用户 | 返回true | P0 |

---

### 3.4 操作日志审计（P1）

#### 3.4.1 功能描述

记录用户操作行为，支持日志查询、统计和清理功能。

#### 3.4.2 业务流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          操作日志审计流程                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  日志记录：                                                              │
│  ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐      │
│  │ 用户操作  │────>│ AOP拦截  │────>│ 构建日志  │────>│ 存储日志  │      │
│  │          │     │          │     │ 对象     │     │          │      │
│  └──────────┘     └──────────┘     └──────────┘     └──────────┘      │
│                                                                         │
│  日志查询：                                                              │
│  ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐      │
│  │ 接收请求  │────>│ 权限验证  │────>│ 条件查询  │────>│ 返回结果  │      │
│  │          │     │          │     │          │     │          │      │
│  └──────────┘     └──────────┘     └──────────┘     └──────────┘      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 3.4.3 业务规则

| 规则编号 | 规则描述 | 规则值 |
|----------|----------|--------|
| BR-LOG-001 | 日志级别 | INFO、WARN、ERROR |
| BR-LOG-002 | 日志类型 | AUTH、DEVICE、CONTROL、SYSTEM等 |
| BR-LOG-003 | 保留期限 | 默认保留90天 |
| BR-LOG-004 | 分页查询 | 默认每页20条 |

#### 3.4.4 数据模型

```java
@Entity
@Table(name = "system_log", indexes = {
    @Index(name = "idx_log_level", columnList = "level"),
    @Index(name = "idx_log_type", columnList = "type"),
    @Index(name = "idx_log_username", columnList = "username"),
    @Index(name = "idx_log_created", columnList = "createdAt")
})
public class SystemLog {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    
    @NotNull
    private String level;           // 日志级别
    
    @NotNull
    private String type;            // 日志类型
    
    @NotNull
    private String message;         // 日志消息
    
    private String source;          // 日志来源
    
    private String username;        // 操作用户
    
    private String ipAddress;       // IP地址
    
    private String userAgent;       // 用户代理
    
    private String details;         // 详细信息
    
    @NotNull
    private LocalDateTime createdAt; // 创建时间
}
```

#### 3.4.5 实现代码

```java
@Service
public class SystemLogService {

    @Autowired
    private SystemLogRepository systemLogRepository;

    /**
     * 记录信息日志
     */
    public SystemLog info(String type, String message, String source) {
        SystemLog log = new SystemLog();
        log.setLevel("INFO");
        log.setType(type);
        log.setMessage(message);
        log.setSource(source);
        return systemLogRepository.save(log);
    }

    /**
     * 记录警告日志
     */
    public SystemLog warn(String type, String message, String source) {
        SystemLog log = new SystemLog();
        log.setLevel("WARN");
        log.setType(type);
        log.setMessage(message);
        log.setSource(source);
        return systemLogRepository.save(log);
    }

    /**
     * 记录错误日志
     */
    public SystemLog error(String type, String message, String source, String details) {
        SystemLog log = new SystemLog();
        log.setLevel("ERROR");
        log.setType(type);
        log.setMessage(message);
        log.setSource(source);
        log.setDetails(details);
        return systemLogRepository.save(log);
    }

    /**
     * 记录操作日志
     */
    public SystemLog audit(String type, String message, String username, 
            String ipAddress, String userAgent) {
        SystemLog log = new SystemLog();
        log.setLevel("INFO");
        log.setType(type);
        log.setMessage(message);
        log.setSource("AUDIT");
        log.setUsername(username);
        log.setIpAddress(ipAddress);
        log.setUserAgent(userAgent);
        return systemLogRepository.save(log);
    }

    /**
     * 获取所有日志（分页）
     */
    public Page<SystemLog> getAllLogs(int page, int size) {
        Pageable pageable = PageRequest.of(page, size, Sort.by("createdAt").descending());
        return systemLogRepository.findAll(pageable);
    }

    /**
     * 根据级别获取日志
     */
    public Page<SystemLog> getLogsByLevel(String level, int page, int size) {
        Pageable pageable = PageRequest.of(page, size, Sort.by("createdAt").descending());
        return systemLogRepository.findByLevel(level, pageable);
    }

    /**
     * 根据用户名获取日志
     */
    public Page<SystemLog> getLogsByUsername(String username, int page, int size) {
        Pageable pageable = PageRequest.of(page, size, Sort.by("createdAt").descending());
        return systemLogRepository.findByUsername(username, pageable);
    }

    /**
     * 清理过期日志
     */
    public void cleanupOldLogs(int retentionDays) {
        LocalDateTime cutoffDate = LocalDateTime.now().minusDays(retentionDays);
        systemLogRepository.deleteByCreatedAtBefore(cutoffDate);
    }

    /**
     * 获取日志统计
     */
    public Map<String, Object> getLogStatistics(int days) {
        Map<String, Object> stats = new HashMap<>();
        LocalDateTime start = LocalDateTime.now().minusDays(days);

        // 按类型统计
        List<Object[]> typeCounts = systemLogRepository.countByTypeSince(start);
        Map<String, Long> typeStats = new HashMap<>();
        for (Object[] row : typeCounts) {
            typeStats.put((String) row[0], (Long) row[1]);
        }
        stats.put("byType", typeStats);

        // 按级别统计
        List<Object[]> levelCounts = systemLogRepository.countByLevelSince(start);
        Map<String, Long> levelStats = new HashMap<>();
        for (Object[] row : levelCounts) {
            levelStats.put((String) row[0], (Long) row[1]);
        }
        stats.put("byLevel", levelStats);

        return stats;
    }
}
```

#### 3.4.6 测试用例

| 用例编号 | 测试场景 | 输入 | 预期输出 | 优先级 |
|----------|----------|------|----------|--------|
| TC-LOG-001 | 记录信息日志 | 有效日志数据 | 日志记录成功 | P1 |
| TC-LOG-002 | 查询日志 | 分页参数 | 返回日志列表 | P1 |
| TC-LOG-003 | 按级别查询 | level=ERROR | 返回错误日志 | P1 |
| TC-LOG-004 | 清理过期日志 | 保留天数 | 过期日志删除 | P2 |

---

### 3.5 系统配置管理（P1）

#### 3.5.1 功能描述

管理系统参数配置，支持配置的增删改查和热更新。

#### 3.5.2 业务规则

| 规则编号 | 规则描述 | 规则值 |
|----------|----------|--------|
| BR-CONFIG-001 | 配置键唯一 | 配置键不能重复 |
| BR-CONFIG-002 | 配置类型 | string、number、boolean、json |
| BR-CONFIG-003 | 系统配置保护 | 系统配置不可删除 |
| BR-CONFIG-004 | 缓存策略 | 配置变更时清除缓存 |

#### 3.5.3 数据模型

```java
@Entity
@Table(name = "system_config", indexes = {
    @Index(name = "idx_config_key", columnList = "configKey", unique = true)
})
public class SystemConfig {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    
    @NotBlank
    @Column(unique = true)
    private String configKey;       // 配置键
    
    @NotBlank
    private String configValue;     // 配置值
    
    private String configType;      // 配置类型
    
    private String description;     // 配置描述
    
    @NotNull
    private boolean system;         // 是否系统配置
    
    @NotNull
    private long createdAt;
    
    @NotNull
    private long updatedAt;
}
```

---

### 3.6 数据字典管理（P2）

#### 3.6.1 功能描述

管理枚举值配置，支持字典类型和字典项的管理。

#### 3.6.2 业务规则

| 规则编号 | 规则描述 | 规则值 |
|----------|----------|--------|
| BR-DICT-001 | 字典类型唯一 | 字典类型编码不能重复 |
| BR-DICT-002 | 字典项值唯一 | 同类型下字典项值不能重复 |
| BR-DICT-003 | 排序支持 | 支持字典项排序 |

#### 3.6.3 数据模型

```java
@Entity
@Table(name = "dict_type")
public class DictType {
    @Id
    private String id;
    
    @NotBlank
    @Column(unique = true)
    private String code;            // 字典类型编码
    
    @NotBlank
    private String name;            // 字典类型名称
    
    private String description;
    
    @NotNull
    private boolean enabled;
    
    @NotNull
    private long createdAt;
}

@Entity
@Table(name = "dict_item")
public class DictItem {
    @Id
    private String id;
    
    @NotBlank
    private String dictTypeCode;    // 字典类型编码
    
    @NotBlank
    private String itemValue;       // 字典项值
    
    @NotBlank
    private String itemLabel;       // 字典项标签
    
    private Integer sortOrder;      // 排序
    
    @NotNull
    private boolean enabled;
}
```

---

## 4. 数据库设计

### 4.1 数据表汇总

| 表名 | 描述 | 主要字段 |
|------|------|----------|
| user_account | 用户账户表 | username, email, passwordHash, role |
| roles | 角色表 | id, code, name, enabled, system |
| permissions | 权限表 | id, code, name, action, resource_id |
| resources | 资源表 | id, code, name, type, url |
| user_roles | 用户角色关联表 | username, role_id |
| role_permissions | 角色权限关联表 | role_id, permission_id |
| system_log | 系统日志表 | level, type, message, username |
| system_config | 系统配置表 | configKey, configValue, configType |
| dict_type | 字典类型表 | code, name, enabled |
| dict_item | 字典项表 | dictTypeCode, itemValue, itemLabel |

---

## 5. 安全设计

### 5.1 密码安全

```java
public class EncryptionUtil {
    
    private static final int BCRYPT_STRENGTH = 12;
    
    /**
     * 使用BCrypt加密密码
     */
    public static String hashPassword(String password) {
        BCryptPasswordEncoder encoder = new BCryptPasswordEncoder(BCRYPT_STRENGTH);
        return encoder.encode(password);
    }
    
    /**
     * 验证密码
     */
    public static boolean verifyPassword(String rawPassword, String encodedPassword) {
        BCryptPasswordEncoder encoder = new BCryptPasswordEncoder();
        return encoder.matches(rawPassword, encodedPassword);
    }
}
```

### 5.2 JWT安全

```java
@Component
public class JwtUtil {
    
    @Value("${jwt.secret}")
    private String secret;
    
    @Value("${jwt.expiration}")
    private long expiration;
    
    private final Set<String> tokenBlacklist = ConcurrentHashMap.newKeySet();
    
    /**
     * 生成JWT令牌
     */
    public String generateToken(String username) {
        return Jwts.builder()
            .setSubject(username)
            .setIssuedAt(new Date())
            .setExpiration(new Date(System.currentTimeMillis() + expiration))
            .signWith(SignatureAlgorithm.HS512, secret)
            .compact();
    }
    
    /**
     * 验证令牌
     */
    public boolean validateToken(String token) {
        if (tokenBlacklist.contains(token)) {
            return false;
        }
        try {
            Jwts.parser().setSigningKey(secret).parseClaimsJws(token);
            return true;
        } catch (Exception e) {
            return false;
        }
    }
    
    /**
     * 将令牌加入黑名单
     */
    public void blacklistToken(String token) {
        tokenBlacklist.add(token);
    }
}
```

### 5.3 限流保护

```java
@Target({ElementType.METHOD, ElementType.TYPE})
@Retention(RetentionPolicy.RUNTIME)
public @interface RateLimit {
    double value();              // 每秒允许的请求数
    String type() default "";    // 限流类型标识
}

@Aspect
@Component
public class RateLimitAspect {
    
    private final Map<String, RateLimiter> limiters = new ConcurrentHashMap<>();
    
    @Around("@annotation(rateLimit)")
    public Object around(ProceedingJoinPoint point, RateLimit rateLimit) throws Throwable {
        String key = rateLimit.type();
        RateLimiter limiter = limiters.computeIfAbsent(key, 
            k -> RateLimiter.create(rateLimit.value()));
        
        if (!limiter.tryAcquire()) {
            throw new RateLimitException("Too many requests");
        }
        
        return point.proceed();
    }
}
```

---

## 6. 测试方案

### 6.1 单元测试

```java
@SpringBootTest
class AuthServiceTest {

    @Autowired
    private AuthService authService;

    @MockBean
    private UserAccountRepository userAccountRepository;

    @Test
    void testLoginSuccess() {
        UserAccount user = new UserAccount();
        user.setUsername("admin");
        user.setPasswordHash(EncryptionUtil.hashPassword("admin123"));
        
        when(userAccountRepository.findById("admin")).thenReturn(Optional.of(user));
        
        Map<String, Object> result = authService.login("admin", "admin123");
        
        assertNotNull(result.get("token"));
        assertEquals("admin", ((Map)result.get("user")).get("username"));
    }

    @Test
    void testLoginFailed() {
        when(userAccountRepository.findById("admin")).thenReturn(Optional.empty());
        
        assertThrows(AuthenticationException.class, () -> {
            authService.login("admin", "wrongpassword");
        });
    }
}
```

### 6.2 API测试脚本

```python
import requests

BASE_URL = "http://localhost:8000/api"

def test_auth_apis():
    # 测试登录
    login_response = requests.post(f"{BASE_URL}/auth/login", json={
        "account": "admin",
        "password": "admin123"
    })
    assert login_response.status_code == 200
    token = login_response.json()["token"]
    print(f"✅ 登录成功，获取令牌")
    
    headers = {"Authorization": f"Bearer {token}"}
    
    # 测试获取当前用户
    me_response = requests.get(f"{BASE_URL}/auth/me", headers=headers)
    assert me_response.status_code == 200
    print(f"✅ 获取当前用户成功: {me_response.json()['username']}")
    
    # 测试获取用户列表
    users_response = requests.get(f"{BASE_URL}/users", headers=headers)
    assert users_response.status_code == 200
    print(f"✅ 获取用户列表成功，共{len(users_response.json())}个用户")
    
    # 测试获取角色列表
    roles_response = requests.get(f"{BASE_URL}/rbac/roles", headers=headers)
    assert roles_response.status_code == 200
    print(f"✅ 获取角色列表成功")

if __name__ == "__main__":
    test_auth_apis()
    print("\n所有API测试通过！")
```

---

## 7. 总结

本文档详细描述了系统管理模块的功能设计与实现细节，包括：

1. **RBAC权限模型**：完整的用户-角色-权限三层模型
2. **功能点设计**：6个核心功能点的详细设计
3. **数据模型设计**：10个数据表的完整设计
4. **安全设计**：密码安全、JWT安全、限流保护
5. **实现代码**：完整的实现代码示例
6. **测试方案**：单元测试和API测试脚本

该模块采用Spring Boot 3.2.3实现，使用JWT进行认证，使用BCrypt进行密码加密，使用RBAC模型进行权限控制，是一个完整的系统管理解决方案。

---

**文档版本**：v1.0  
**编写日期**：2026年3月14日  
**编写人**：系统架构组
