# 实时通信模块 - 功能设计与实现细节文档

## 1. 模块概述

### 1.1 模块定位

实时通信模块是宿舍电源管理系统的核心基础设施模块，负责实现设备与系统、系统与前端之间的实时数据通信。该模块通过MQTT协议实现设备通信，通过WebSocket协议实现前端推送，是系统实时性的关键保障。

### 1.2 模块职责

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          实时通信模块职责边界                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                        核心职责                                   │   │
│  │  - MQTT连接管理与消息处理                                          │   │
│  │  - WebSocket连接管理与消息推送                                     │   │
│  │  - 设备状态上报处理                                                │   │
│  │  - 设备遥测数据上报处理                                            │   │
│  │  - 命令下发与确认处理                                              │   │
│  │  - 前端实时推送                                                    │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                        边界职责                                   │   │
│  │  - 与设备模块协作：更新设备在线状态                                 │   │
│  │  - 与遥测模块协作：存储遥测数据                                    │   │
│  │  - 与控制模块协作：处理命令确认                                    │   │
│  │  - 与告警模块协作：触发告警检查                                    │   │
│  │  - 与Kafka协作：异步消息处理                                       │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                        非职责范围                                  │   │
│  │  - 设备注册与管理（由设备模块负责）                                │   │
│  │  - 遥测数据存储与查询（由遥测模块负责）                            │   │
│  │  - 告警规则配置（由告警模块负责）                                  │   │
│  │  - 用户认证与授权（由认证模块负责）                                │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 1.3 功能点总览

| 功能点 | 功能描述 | 通信协议 | 优先级 |
|--------|----------|----------|--------|
| 设备状态上报 | 设备上报实时状态 | MQTT | P0 |
| 设备遥测上报 | 设备上报用电数据 | MQTT | P0 |
| 命令下发 | 系统向设备发送控制命令 | MQTT | P0 |
| 前端实时推送 | 向前端推送设备状态变化 | WebSocket | P0 |
| 告警推送 | 向前端推送告警信息 | WebSocket | P1 |

---

## 2. MQTT主题设计

### 2.1 主题模式定义

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          MQTT主题设计                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  主题前缀：dorm                                                          │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  设备→服务器主题                                                  │   │
│  ├─────────────────────────────────────────────────────────────────┤   │
│  │  dorm/{room}/{device}/status     - 状态上报                      │   │
│  │  dorm/{room}/{device}/telemetry  - 遥测上报                      │   │
│  │  dorm/{room}/{device}/ack        - 命令确认                      │   │
│  │  dorm/{room}/{device}/event      - 事件上报                      │   │
│  │  dorm/{room}/{device}/lwt        - 离线通知（遗嘱消息）           │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  服务器→设备主题                                                  │   │
│  ├─────────────────────────────────────────────────────────────────┤   │
│  │  dorm/{room}/{device}/cmd        - 命令下发                      │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  参数说明：                                                              │
│  - {room}: 房间编号，如 A1-301                                          │
│  - {device}: 设备ID，如 device_001                                      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.2 主题详细说明

| 主题模式 | 方向 | 用途 | QoS | 消息格式 |
|----------|------|------|-----|----------|
| `dorm/{room}/{device}/status` | 设备→服务器 | 状态上报 | 1 | JSON |
| `dorm/{room}/{device}/telemetry` | 设备→服务器 | 遥测上报 | 0 | JSON |
| `dorm/{room}/{device}/ack` | 设备→服务器 | 命令确认 | 1 | JSON |
| `dorm/{room}/{device}/event` | 设备→服务器 | 事件上报 | 1 | JSON |
| `dorm/{room}/{device}/lwt` | 设备→服务器 | 离线通知 | 1 | JSON |
| `dorm/{room}/{device}/cmd` | 服务器→设备 | 命令下发 | 1 | JSON |

---

## 3. 功能点详细设计

### 3.1 设备状态上报（P0）

#### 3.1.1 功能描述

设备通过MQTT协议上报实时状态，包括在线状态、功率、电压、电流和插座状态。

#### 3.1.2 业务流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          设备状态上报流程                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐      │
│  │ 设备上报  │────>│ MQTT接收  │────>│ Kafka队列 │────>│ 状态存储  │      │
│  │ 状态     │     │ 消息     │     │ 异步处理  │     │ 数据库   │      │
│  └──────────┘     └──────────┘     └──────────┘     └──────────┘      │
│                                                          │             │
│                                       ┌──────────────────┤             │
│                                       │                  │             │
│                                       ▼                  ▼             │
│                                 ┌──────────┐      ┌──────────┐        │
│                                 │ WebSocket │      │ 告警检查  │        │
│                                 │ 推送     │      │          │        │
│                                 └──────────┘      └──────────┘        │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 3.1.3 业务规则

| 规则编号 | 规则描述 | 规则值 |
|----------|----------|--------|
| BR-STATUS-001 | QoS级别 | 1（至少一次送达） |
| BR-STATUS-002 | 消息格式 | JSON |
| BR-STATUS-003 | 必填字段 | online、total_power_w、voltage_v、current_a、sockets |
| BR-STATUS-004 | 默认值 | 电压默认220V |
| BR-STATUS-005 | 状态历史 | 记录到device_status_history表 |

#### 3.1.4 消息格式

**设备状态上报消息：**

```json
{
    "online": true,
    "total_power_w": 150.5,
    "voltage_v": 220.0,
    "current_a": 0.68,
    "sockets": [
        {"id": 1, "on": true, "power_w": 50.0},
        {"id": 2, "on": false, "power_w": 0.0},
        {"id": 3, "on": true, "power_w": 60.5},
        {"id": 4, "on": true, "power_w": 40.0}
    ]
}
```

#### 3.1.5 实现代码

```java
@Component
public class MqttBridge {

    @Autowired
    private MqttConfig mqttConfig;

    @Autowired
    private DeviceRepository deviceRepository;

    @Autowired
    private StripStatusRepository stripStatusRepository;

    @Autowired
    private WebSocketNotificationService webSocketNotificationService;

    @Autowired(required = false)
    private TelemetryProducer telemetryProducer;

    @Value("${kafka.enabled:true}")
    private boolean kafkaEnabled;

    private void handleStatusMessage(String deviceId, JsonNode payload) {
        long now = System.currentTimeMillis() / 1000;
        
        boolean online = payload.has("online") ? payload.get("online").asBoolean() : true;
        double totalPowerW = payload.has("total_power_w") ? payload.get("total_power_w").asDouble() : 0.0;
        double voltageV = payload.has("voltage_v") ? payload.get("voltage_v").asDouble() : 220.0;
        double currentA = payload.has("current_a") ? payload.get("current_a").asDouble() : 0.0;
        String socketsJson = payload.has("sockets") ? payload.get("sockets").toString() : "[]";

        // 优先使用Kafka发送（支持多实例广播）
        if (kafkaEnabled && telemetryProducer != null) {
            telemetryProducer.sendDeviceStatus(deviceId, online, totalPowerW, voltageV, currentA, socketsJson);
        } else {
            // 降级：直接处理
            processStatusDirectly(deviceId, online, totalPowerW, voltageV, currentA, socketsJson, now, payload);
        }
    }

    private void processStatusDirectly(String deviceId, boolean online, double totalPowerW,
            double voltageV, double currentA, String socketsJson, long now, JsonNode payload) {
        
        // 更新设备在线状态
        deviceRepository.findById(deviceId).ifPresent(device -> {
            device.setOnline(online);
            device.setLastSeenTs(now);
            deviceRepository.save(device);
        });

        // 更新插座状态
        StripStatus status = stripStatusRepository.findByDeviceId(deviceId);
        if (status == null) {
            status = new StripStatus();
            status.setDeviceId(deviceId);
        }

        status.setTs(now);
        status.setOnline(online);
        status.setTotalPowerW(totalPowerW);
        status.setVoltageV(voltageV);
        status.setCurrentA(currentA);
        status.setSocketsJson(socketsJson);
        stripStatusRepository.save(status);

        // WebSocket推送
        webSocketNotificationService.broadcastDeviceStatus(deviceId, payload);
    }
}
```

#### 3.1.6 测试用例

| 用例编号 | 测试场景 | 输入 | 预期输出 | 优先级 |
|----------|----------|------|----------|--------|
| TC-STATUS-001 | 正常状态上报 | 完整状态数据 | 状态存储成功，WebSocket推送 | P0 |
| TC-STATUS-002 | 部分字段缺失 | 缺少voltage_v | 使用默认值220V | P1 |
| TC-STATUS-003 | Kafka不可用 | Kafka连接断开 | 降级直接处理 | P1 |
| TC-STATUS-004 | 设备离线上报 | online=false | 更新设备离线状态 | P0 |

---

### 3.2 设备遥测上报（P0）

#### 3.2.1 功能描述

设备通过MQTT协议上报用电遥测数据，包括功率、电压、电流等参数。

#### 3.2.2 业务流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          设备遥测上报流程                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐      │
│  │ 设备上报  │────>│ MQTT接收  │────>│ Kafka队列 │────>│ 遥测存储  │      │
│  │ 遥测     │     │ QoS=0    │     │ 异步处理  │     │ 数据库   │      │
│  └──────────┘     └──────────┘     └──────────┘     └──────────┘      │
│                                                          │             │
│                                       ┌──────────────────┤             │
│                                       │                  │             │
│                                       ▼                  ▼             │
│                                 ┌──────────┐      ┌──────────┐        │
│                                 │ 告警检查  │      │ 设备心跳  │        │
│                                 │          │      │ 更新     │        │
│                                 └──────────┘      └──────────┘        │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 3.2.3 业务规则

| 规则编号 | 规则描述 | 规则值 |
|----------|----------|--------|
| BR-TELE-001 | QoS级别 | 0（最多一次送达，高吞吐） |
| BR-TELE-002 | 消息格式 | JSON |
| BR-TELE-003 | 必填字段 | power_w |
| BR-TELE-004 | 可选字段 | ts、voltage_v、current_a |
| BR-TELE-005 | 异步处理 | 通过Kafka异步处理，提高吞吐量 |

#### 3.2.4 消息格式

**遥测数据上报消息：**

```json
{
    "ts": 1678886400,
    "power_w": 150.5,
    "voltage_v": 220.0,
    "current_a": 0.68
}
```

#### 3.2.5 实现代码

```java
private void handleTelemetryMessage(String deviceId, JsonNode payload) {
    long now = System.currentTimeMillis() / 1000;

    double powerW = payload.has("power_w") ? payload.get("power_w").asDouble() : 0.0;
    double voltageV = payload.has("voltage_v") ? payload.get("voltage_v").asDouble() : 220.0;
    double currentA = payload.has("current_a") ? payload.get("current_a").asDouble() : 0.0;
    long ts = payload.has("ts") ? payload.get("ts").asLong() : now;

    // 优先使用Kafka发送（异步处理，高吞吐）
    if (kafkaEnabled && telemetryProducer != null) {
        telemetryProducer.sendTelemetry(deviceId, ts, powerW, voltageV, currentA);
    } else {
        // 降级：直接写入数据库
        processTelemetryDirectly(deviceId, ts, powerW, voltageV, currentA, now, payload);
    }
}

private void processTelemetryDirectly(String deviceId, long ts, double powerW,
        double voltageV, double currentA, long now, JsonNode payload) {
    
    // 存储遥测数据
    Telemetry telemetry = new Telemetry();
    telemetry.setDeviceId(deviceId);
    telemetry.setTs(ts);
    telemetry.setPowerW(powerW);
    telemetry.setVoltageV(voltageV);
    telemetry.setCurrentA(currentA);
    telemetryRepository.save(telemetry);

    // 检查告警
    alertService.checkAndGenerateAlert(deviceId, "power", powerW);
    alertService.checkAndGenerateAlert(deviceId, "voltage", voltageV);
    alertService.checkAndGenerateAlert(deviceId, "current", currentA);

    // 更新设备心跳
    deviceRepository.findById(deviceId).ifPresent(device -> {
        device.setLastSeenTs(now);
        deviceRepository.save(device);
    });

    // WebSocket推送
    webSocketNotificationService.broadcastTelemetry(deviceId, payload);
}
```

#### 3.2.6 测试用例

| 用例编号 | 测试场景 | 输入 | 预期输出 | 优先级 |
|----------|----------|------|----------|--------|
| TC-TELE-001 | 正常遥测上报 | 完整遥测数据 | 数据存储成功 | P0 |
| TC-TELE-002 | 高频上报 | 100条/秒 | 全部成功处理 | P1 |
| TC-TELE-003 | 告警触发 | power_w > 阈值 | 生成告警记录 | P1 |
| TC-TELE-004 | 时间戳缺失 | 无ts字段 | 使用当前时间戳 | P1 |

---

### 3.3 命令下发（P0）

#### 3.3.1 功能描述

系统通过MQTT协议向设备发送控制命令，包括开关操作、定时功能等。

#### 3.3.2 业务流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          命令下发流程                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐      │
│  │ 接收请求  │────>│ 创建命令  │────>│ MQTT发布  │────>│ 等待确认  │      │
│  │          │     │ 记录     │     │ QoS=1    │     │          │      │
│  └──────────┘     └──────────┘     └──────────┘     └──────────┘      │
│                                                          │             │
│                                       ┌──────────────────┤             │
│                                       │                  │             │
│                                       ▼                  ▼             │
│                                 ┌──────────┐      ┌──────────┐        │
│                                 │ 设备确认  │      │ 超时处理  │        │
│                                 │ 更新状态  │      │          │        │
│                                 └──────────┘      └──────────┘        │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 3.3.3 业务规则

| 规则编号 | 规则描述 | 规则值 |
|----------|----------|--------|
| BR-CMD-001 | QoS级别 | 1（至少一次送达） |
| BR-CMD-002 | 消息格式 | JSON |
| BR-CMD-003 | 必填字段 | action、cmdId |
| BR-CMD-004 | 超时时间 | 30秒 |
| BR-CMD-005 | 重试策略 | 不重试，依赖QoS |

#### 3.3.4 消息格式

**命令下发消息：**

```json
{
    "cmdId": "cmd-1678886400000",
    "action": "toggle",
    "socket": 1,
    "duration": 0
}
```

**命令确认消息（设备返回）：**

```json
{
    "cmdId": "cmd-1678886400000",
    "status": "success",
    "message": "",
    "socket": 1,
    "state": "on"
}
```

#### 3.3.5 实现代码

```java
@Component
public class MqttBridge {

    @Autowired
    private MqttConfig mqttConfig;

    @Autowired
    private ObjectMapper objectMapper;

    @Autowired
    private CommandService commandService;

    @Autowired
    private WebSocketNotificationService webSocketNotificationService;

    private MqttClient mqttClient;
    private boolean connected = false;

    /**
     * 发布命令到设备
     * 
     * @param deviceId 设备ID
     * @param payload 命令负载
     * @return 是否发布成功
     */
    public boolean publishCommand(String deviceId, Object payload) {
        if (!connected || mqttClient == null || !mqttClient.isConnected()) {
            logger.error("MQTT未连接，无法下发命令");
            return false;
        }

        try {
            String topic = mqttConfig.buildTopic(deviceId, "cmd");
            String jsonPayload = objectMapper.writeValueAsString(payload);
            
            MqttMessage message = new MqttMessage(jsonPayload.getBytes());
            message.setQos(1);
            mqttClient.publish(topic, message);
            
            logger.info("命令已发布: topic={}, payload={}", topic, jsonPayload);
            return true;
        } catch (Exception e) {
            logger.error("发布命令失败: {}", e.getMessage());
            return false;
        }
    }

    /**
     * 处理命令确认消息
     */
    private void handleAckMessage(String deviceId, JsonNode payload) {
        String cmdId = payload.has("cmdId") ? payload.get("cmdId").asText() : "";
        String status = payload.has("status") ? payload.get("status").asText() : "success";
        String message = payload.has("message") ? payload.get("message").asText() : "";
        
        // 更新命令状态
        commandService.updateCommandState(cmdId, status, message);

        // 更新插座状态
        if (payload.has("socket") && payload.has("state")) {
            updateSocketStatus(deviceId, payload);
        }

        // WebSocket推送命令结果
        webSocketNotificationService.broadcastCommandResult(cmdId, status, payload);
    }
}
```

#### 3.3.6 测试用例

| 用例编号 | 测试场景 | 输入 | 预期输出 | 优先级 |
|----------|----------|------|----------|--------|
| TC-CMD-001 | 正常命令下发 | 有效命令 | MQTT发布成功 | P0 |
| TC-CMD-002 | 设备确认成功 | status=success | 命令状态更新为success | P0 |
| TC-CMD-003 | 设备确认失败 | status=failed | 命令状态更新为failed | P0 |
| TC-CMD-004 | MQTT断开 | 连接断开 | 返回false | P1 |

---

### 3.4 前端实时推送（P0）

#### 3.4.1 功能描述

通过WebSocket协议向前端实时推送设备状态变化、遥测数据和命令结果。

#### 3.4.2 业务流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          前端实时推送流程                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐      │
│  │ 前端连接  │────>│ 订阅设备  │────>│ 接收推送  │────>│ 更新界面  │      │
│  │ WebSocket │     │          │     │ 消息     │     │          │      │
│  └──────────┘     └──────────┘     └──────────┘     └──────────┘      │
│       │                │                                             │
│       │                │ 订阅请求                                     │
│       │                ▼                                             │
│       │          ┌──────────┐                                        │
│       │          │ 添加订阅  │                                        │
│       │          │ 关系     │                                        │
│       │          └──────────┘                                        │
│                                                                         │
│  消息类型：                                                              │
│  - DEVICE_STATUS: 设备状态更新                                          │
│  - TELEMETRY: 遥测数据更新                                              │
│  - CMD_ACK: 命令执行结果                                                │
│  - ALERT: 告警通知                                                      │
│  - SYSTEM: 系统通知                                                     │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 3.4.3 业务规则

| 规则编号 | 规则描述 | 规则值 |
|----------|----------|--------|
| BR-WS-001 | 连接端点 | /ws |
| BR-WS-002 | 消息格式 | JSON |
| BR-WS-003 | 订阅机制 | 客户端订阅特定设备 |
| BR-WS-004 | 心跳机制 | 客户端发送ping，服务端返回pong |
| BR-WS-005 | 异步发送 | 使用线程池异步发送消息 |

#### 3.4.4 消息格式

**订阅请求：**

```json
{
    "type": "subscribe",
    "deviceId": "device_001"
}
```

**订阅确认：**

```json
{
    "type": "subscribe",
    "status": "success",
    "deviceId": "device_001"
}
```

**设备状态推送：**

```json
{
    "type": "DEVICE_STATUS",
    "deviceId": "device_001",
    "payload": {
        "online": true,
        "total_power_w": 150.5,
        "voltage_v": 220.0,
        "current_a": 0.68
    },
    "timestamp": 1678886400000
}
```

**遥测数据推送：**

```json
{
    "type": "TELEMETRY",
    "deviceId": "device_001",
    "payload": {
        "power_w": 150.5,
        "voltage_v": 220.0,
        "current_a": 0.68
    },
    "timestamp": 1678886400000
}
```

**命令结果推送：**

```json
{
    "type": "CMD_ACK",
    "cmdId": "cmd-1678886400000",
    "state": "success",
    "payload": {
        "socket": 1,
        "state": "on"
    },
    "timestamp": 1678886400000
}
```

#### 3.4.5 实现代码

**WebSocket配置：**

```java
@Configuration
@EnableWebSocket
public class WebSocketConfig implements WebSocketConfigurer {

    @Override
    public void registerWebSocketHandlers(WebSocketHandlerRegistry registry) {
        registry.addHandler(new WebSocketHandler(), "/ws")
                .setAllowedOrigins("*");
    }
}
```

**WebSocket处理器：**

```java
public class WebSocketHandler extends TextWebSocketHandler {

    private final WebSocketManager webSocketManager = WebSocketManager.getInstance();
    private final ObjectMapper objectMapper = new ObjectMapper();

    @Override
    public void afterConnectionEstablished(WebSocketSession session) throws Exception {
        webSocketManager.addSession(session);
        
        Map<String, Object> message = new HashMap<>();
        message.put("type", "connection");
        message.put("status", "connected");
        message.put("sessionId", session.getId());
        session.sendMessage(new TextMessage(objectMapper.writeValueAsString(message)));
    }

    @Override
    public void afterConnectionClosed(WebSocketSession session, CloseStatus status) throws Exception {
        webSocketManager.removeSession(session);
    }

    @Override
    protected void handleTextMessage(WebSocketSession session, TextMessage message) throws Exception {
        String payload = message.getPayload();
        
        try {
            Map<String, Object> request = objectMapper.readValue(payload, Map.class);
            String type = (String) request.get("type");
            
            switch (type) {
                case "subscribe" -> handleSubscribe(session, request);
                case "unsubscribe" -> handleUnsubscribe(session, request);
                case "ping" -> handlePing(session);
                default -> sendError(session, "Unknown message type: " + type);
            }
        } catch (Exception e) {
            sendError(session, "Failed to parse message: " + e.getMessage());
        }
    }

    private void handleSubscribe(WebSocketSession session, Map<String, Object> request) throws Exception {
        String deviceId = (String) request.get("deviceId");
        if (deviceId != null) {
            webSocketManager.subscribeDevice(session, deviceId);
            
            Map<String, Object> response = new HashMap<>();
            response.put("type", "subscribe");
            response.put("status", "success");
            response.put("deviceId", deviceId);
            session.sendMessage(new TextMessage(objectMapper.writeValueAsString(response)));
        }
    }

    private void handlePing(WebSocketSession session) throws Exception {
        Map<String, Object> response = new HashMap<>();
        response.put("type", "pong");
        response.put("timestamp", System.currentTimeMillis());
        session.sendMessage(new TextMessage(objectMapper.writeValueAsString(response)));
    }
}
```

**WebSocket管理器：**

```java
public class WebSocketManager {

    private static volatile WebSocketManager instance;

    private final CopyOnWriteArraySet<WebSocketSession> sessions = new CopyOnWriteArraySet<>();
    private final Map<WebSocketSession, Set<String>> sessionDeviceSubscriptions = new ConcurrentHashMap<>();
    private final Map<String, Set<WebSocketSession>> deviceSubscribers = new ConcurrentHashMap<>();

    private final ThreadPoolExecutor sendExecutor;

    private WebSocketManager() {
        int poolSize = Runtime.getRuntime().availableProcessors();
        this.sendExecutor = new ThreadPoolExecutor(
            poolSize, poolSize,
            60L, TimeUnit.SECONDS,
            new LinkedBlockingQueue<>(100),
            r -> {
                Thread t = new Thread(r, "ws-sender");
                t.setDaemon(true);
                return t;
            },
            new ThreadPoolExecutor.CallerRunsPolicy()
        );
    }

    public static WebSocketManager getInstance() {
        if (instance == null) {
            synchronized (WebSocketManager.class) {
                if (instance == null) {
                    instance = new WebSocketManager();
                }
            }
        }
        return instance;
    }

    public void subscribeDevice(WebSocketSession session, String deviceId) {
        sessionDeviceSubscriptions.computeIfAbsent(session, k -> ConcurrentHashMap.newKeySet()).add(deviceId);
        deviceSubscribers.computeIfAbsent(deviceId, k -> ConcurrentHashMap.newKeySet()).add(session);
    }

    public void sendToDeviceSubscribers(String deviceId, String message) {
        Set<WebSocketSession> subscribers = deviceSubscribers.get(deviceId);
        if (subscribers != null) {
            subscribers.forEach(session -> sendSync(session, message));
        }
    }

    public void broadcast(String message) {
        sessions.forEach(session -> sendSync(session, message));
    }

    private void sendSync(WebSocketSession session, String message) {
        try {
            if (session.isOpen()) {
                synchronized (session) {
                    session.sendMessage(new TextMessage(message));
                }
            }
        } catch (IOException e) {
            removeSession(session);
        }
    }
}
```

**通知服务：**

```java
@Service
public class WebSocketNotificationService {

    private final WebSocketManager webSocketManager = WebSocketManager.getInstance();
    private final ObjectMapper objectMapper = new ObjectMapper();

    public void broadcastDeviceStatus(String deviceId, JsonNode status) {
        try {
            Map<String, Object> message = new HashMap<>();
            message.put("type", "DEVICE_STATUS");
            message.put("deviceId", deviceId);
            message.put("payload", status);
            message.put("timestamp", System.currentTimeMillis());
            
            String jsonMessage = objectMapper.writeValueAsString(message);
            webSocketManager.sendToDeviceSubscribers(deviceId, jsonMessage);
        } catch (Exception e) {
            logger.error("推送设备状态失败: {}", e.getMessage());
        }
    }

    public void broadcastTelemetry(String deviceId, JsonNode telemetry) {
        try {
            Map<String, Object> message = new HashMap<>();
            message.put("type", "TELEMETRY");
            message.put("deviceId", deviceId);
            message.put("payload", telemetry);
            message.put("timestamp", System.currentTimeMillis());
            
            String jsonMessage = objectMapper.writeValueAsString(message);
            webSocketManager.sendToDeviceSubscribers(deviceId, jsonMessage);
        } catch (Exception e) {
            logger.error("推送遥测数据失败: {}", e.getMessage());
        }
    }

    public void broadcastCommandResult(String cmdId, String state, JsonNode result) {
        try {
            Map<String, Object> message = new HashMap<>();
            message.put("type", "CMD_ACK");
            message.put("cmdId", cmdId);
            message.put("state", state);
            message.put("payload", result);
            message.put("timestamp", System.currentTimeMillis());
            
            String jsonMessage = objectMapper.writeValueAsString(message);
            webSocketManager.broadcast(jsonMessage);
        } catch (Exception e) {
            logger.error("推送命令结果失败: {}", e.getMessage());
        }
    }
}
```

#### 3.4.6 测试用例

| 用例编号 | 测试场景 | 输入 | 预期输出 | 优先级 |
|----------|----------|------|----------|--------|
| TC-WS-001 | 正常连接 | WebSocket连接 | 返回连接成功消息 | P0 |
| TC-WS-002 | 订阅设备 | subscribe请求 | 返回订阅成功 | P0 |
| TC-WS-003 | 接收推送 | 设备状态变化 | 收到推送消息 | P0 |
| TC-WS-004 | 心跳检测 | ping消息 | 返回pong消息 | P1 |
| TC-WS-005 | 取消订阅 | unsubscribe请求 | 取消订阅成功 | P1 |

---

### 3.5 告警推送（P1）

#### 3.5.1 功能描述

通过WebSocket协议向前端推送告警信息，包括功率超限、电压异常等。

#### 3.5.2 业务流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          告警推送流程                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐      │
│  │ 告警检测  │────>│ 创建告警  │────>│ WebSocket │────>│ 前端展示  │      │
│  │          │     │ 记录     │     │ 推送     │     │          │      │
│  └──────────┘     └──────────┘     └──────────┘     └──────────┘      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 3.5.3 消息格式

**告警推送消息：**

```json
{
    "type": "ALERT",
    "deviceId": "device_001",
    "payload": {
        "alertType": "power",
        "alertValue": 250.5,
        "threshold": 200.0,
        "message": "功率超过阈值"
    },
    "timestamp": 1678886400000
}
```

#### 3.5.4 实现代码

```java
@Service
public class WebSocketNotificationService {

    public void broadcastAlert(String deviceId, JsonNode alert) {
        try {
            Map<String, Object> message = new HashMap<>();
            message.put("type", "ALERT");
            message.put("deviceId", deviceId);
            message.put("payload", alert);
            message.put("timestamp", System.currentTimeMillis());
            
            String jsonMessage = objectMapper.writeValueAsString(message);
            webSocketManager.sendToDeviceSubscribers(deviceId, jsonMessage);
        } catch (Exception e) {
            logger.error("推送告警失败: {}", e.getMessage());
        }
    }

    public void broadcastAlert(String deviceId, Map<String, Object> alert) {
        try {
            Map<String, Object> message = new HashMap<>();
            message.put("type", "ALERT");
            message.put("deviceId", deviceId);
            message.put("payload", alert);
            message.put("timestamp", System.currentTimeMillis());
            
            String jsonMessage = objectMapper.writeValueAsString(message);
            webSocketManager.sendToDeviceSubscribers(deviceId, jsonMessage);
        } catch (Exception e) {
            logger.error("推送告警失败: {}", e.getMessage());
        }
    }
}
```

---

## 4. 架构设计

### 4.1 整体架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          实时通信架构                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────┐                              ┌──────────────┐         │
│  │   IoT设备    │                              │   前端应用   │         │
│  └──────┬───────┘                              └──────┬───────┘         │
│         │                                             │                 │
│         │ MQTT                                        │ WebSocket       │
│         ▼                                             ▼                 │
│  ┌──────────────┐                              ┌──────────────┐         │
│  │ MQTT Broker  │                              │ WebSocket    │         │
│  │ (Mosquitto)  │                              │ Server       │         │
│  └──────┬───────┘                              └──────┬───────┘         │
│         │                                             │                 │
│         │                                             │                 │
│         ▼                                             ▼                 │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                        后端服务                                   │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │   │
│  │  │ MqttBridge   │  │ Kafka        │  │ WebSocket    │          │   │
│  │  │              │──>│ Producer     │──>│ Manager      │          │   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘          │   │
│  │          │                │                                      │   │
│  │          │                ▼                                      │   │
│  │          │         ┌──────────────┐                             │   │
│  │          │         │ Kafka        │                             │   │
│  │          │         │ Consumer     │                             │   │
│  │          │         └──────────────┘                             │   │
│  │          │                │                                      │   │
│  │          ▼                ▼                                      │   │
│  │  ┌─────────────────────────────────────────────────────────┐    │   │
│  │  │                    PostgreSQL                            │    │   │
│  │  └─────────────────────────────────────────────────────────┘    │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 4.2 消息流转

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          消息流转图                                       │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  设备消息：                                                              │
│  ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐      │
│  │ 设备     │────>│ MQTT     │────>│ MqttBridge│────>│ Kafka    │      │
│  │          │     │ Broker   │     │          │     │ Topic    │      │
│  └──────────┘     └──────────┘     └──────────┘     └──────────┘      │
│                                                          │             │
│                                                          ▼             │
│                                                    ┌──────────┐       │
│                                                    │ Kafka    │       │
│                                                    │ Consumer │       │
│                                                    └──────────┘       │
│                                                          │             │
│                                       ┌──────────────────┼──────────┐  │
│                                       │                  │          │  │
│                                       ▼                  ▼          ▼  │
│                                 ┌──────────┐      ┌──────────┐ ┌─────┐│
│                                 │ 数据库   │      │ WebSocket │ │告警 ││
│                                 │ 存储     │      │ 推送     │ │检查 ││
│                                 └──────────┘      └──────────┘ └─────┘│
│                                                                         │
│  命令消息：                                                              │
│  ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐      │
│  │ 前端请求 │────>│ Controller│────>│ MqttBridge│────>│ MQTT     │      │
│  │          │     │          │     │          │     │ Broker   │      │
│  └──────────┘     └──────────┘     └──────────┘     └──────────┘      │
│                                                          │             │
│                                                          ▼             │
│                                                    ┌──────────┐       │
│                                                    │ 设备     │       │
│                                                    │          │       │
│                                                    └──────────┘       │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 5. Kafka主题设计

### 5.1 主题定义

| 主题名称 | 用途 | 分区数 | 副本数 |
|----------|------|--------|--------|
| dorm.telemetry | 遥测数据 | 3 | 1 |
| dorm.device.status | 设备状态 | 3 | 1 |
| dorm.alert | 告警数据 | 3 | 1 |

### 5.2 生产者实现

```java
@Component
public class TelemetryProducer {

    private static final String TELEMETRY_TOPIC = "dorm.telemetry";
    private static final String STATUS_TOPIC = "dorm.device.status";
    private static final String ALERT_TOPIC = "dorm.alert";

    @Autowired
    private KafkaTemplate<String, String> kafkaTemplate;

    @Autowired
    private ObjectMapper objectMapper;

    public void sendTelemetry(String deviceId, long ts, double powerW, double voltageV, double currentA) {
        Map<String, Object> data = new HashMap<>();
        data.put("deviceId", deviceId);
        data.put("ts", ts);
        data.put("powerW", powerW);
        data.put("voltageV", voltageV);
        data.put("currentA", currentA);
        data.put("timestamp", System.currentTimeMillis());

        sendAsync(TELEMETRY_TOPIC, deviceId, data);
    }

    public void sendDeviceStatus(String deviceId, boolean online, double totalPowerW,
                                  double voltageV, double currentA, String socketsJson) {
        Map<String, Object> data = new HashMap<>();
        data.put("deviceId", deviceId);
        data.put("online", online);
        data.put("totalPowerW", totalPowerW);
        data.put("voltageV", voltageV);
        data.put("currentA", currentA);
        data.put("socketsJson", socketsJson);
        data.put("timestamp", System.currentTimeMillis());

        sendAsync(STATUS_TOPIC, deviceId, data);
    }

    private void sendAsync(String topic, String key, Map<String, Object> data) {
        try {
            String message = objectMapper.writeValueAsString(data);
            CompletableFuture<SendResult<String, String>> future = kafkaTemplate.send(topic, key, message);

            future.whenComplete((result, ex) -> {
                if (ex != null) {
                    logger.error("发送Kafka消息失败: topic={}, key={}", topic, key);
                }
            });
        } catch (JsonProcessingException e) {
            logger.error("序列化消息失败: {}", e.getMessage());
        }
    }
}
```

---

## 6. 配置说明

### 6.1 MQTT配置

```yaml
mqtt:
  enabled: true
  broker-url: tcp://localhost:1883
  client-id: dormpower-server
  username: admin
  password: public
  topic-prefix: dorm
```

### 6.2 Kafka配置

```yaml
spring:
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
    consumer:
      group-id: dormpower-group
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      auto-offset-reset: latest

kafka:
  enabled: true
```

---

## 7. 测试方案

### 7.1 单元测试

```java
@SpringBootTest
class MqttBridgeTest {

    @Autowired
    private MqttBridge mqttBridge;

    @MockBean
    private DeviceRepository deviceRepository;

    @MockBean
    private StripStatusRepository stripStatusRepository;

    @Test
    void testPublishCommand() {
        Map<String, Object> payload = new HashMap<>();
        payload.put("cmdId", "cmd-123");
        payload.put("action", "toggle");
        payload.put("socket", 1);

        boolean result = mqttBridge.publishCommand("device_001", payload);

        assertTrue(result || !mqttBridge.isConnected());
    }
}
```

### 7.2 WebSocket测试

```javascript
// WebSocket客户端测试
const ws = new WebSocket('ws://localhost:8000/ws');

ws.onopen = function() {
    console.log('WebSocket连接成功');
    
    // 订阅设备
    ws.send(JSON.stringify({
        type: 'subscribe',
        deviceId: 'device_001'
    }));
};

ws.onmessage = function(event) {
    const message = JSON.parse(event.data);
    console.log('收到消息:', message);
    
    switch (message.type) {
        case 'DEVICE_STATUS':
            console.log('设备状态更新:', message.payload);
            break;
        case 'TELEMETRY':
            console.log('遥测数据更新:', message.payload);
            break;
        case 'CMD_ACK':
            console.log('命令执行结果:', message.state);
            break;
        case 'ALERT':
            console.log('告警通知:', message.payload);
            break;
    }
};

// 发送心跳
setInterval(() => {
    ws.send(JSON.stringify({ type: 'ping' }));
}, 30000);
```

---

## 8. 性能优化

### 8.1 MQTT优化

1. **QoS选择**
   - 遥测数据使用QoS 0，提高吞吐量
   - 状态和命令使用QoS 1，保证可靠性

2. **连接池**
   - 使用单一MQTT连接，避免频繁创建销毁

### 8.2 WebSocket优化

1. **异步发送**
   - 使用线程池异步发送消息，不阻塞业务线程

2. **连接管理**
   - 自动清理失效连接
   - 支持大量并发连接

### 8.3 Kafka优化

1. **异步发送**
   - 使用异步发送，不阻塞MQTT消息处理

2. **批量处理**
   - 消费者批量处理消息，提高吞吐量

---

## 9. 安全考虑

### 9.1 MQTT安全

- 使用用户名密码认证
- 建议启用TLS加密

### 9.2 WebSocket安全

- 生产环境应限制允许的来源域名
- 可添加Token认证

---

## 10. 总结

本文档详细描述了实时通信模块的功能设计与实现细节，包括：

1. **MQTT主题设计**：6个主题模式，覆盖设备通信全场景
2. **功能点设计**：5个核心功能点的详细设计
3. **架构设计**：MQTT + Kafka + WebSocket三层架构
4. **消息格式**：标准化的JSON消息格式
5. **实现代码**：完整的实现代码示例
6. **测试方案**：单元测试和WebSocket客户端测试
7. **性能优化**：MQTT、WebSocket、Kafka优化建议
8. **安全考虑**：MQTT和WebSocket安全措施

该模块采用Spring Boot 3.2.3实现，通过MQTT协议与IoT设备通信，通过WebSocket协议与前端通信，使用Kafka进行异步消息处理，是一个完整的实时通信解决方案。

---

**文档版本**：v1.0  
**编写日期**：2026年3月14日  
**编写人**：系统架构组
