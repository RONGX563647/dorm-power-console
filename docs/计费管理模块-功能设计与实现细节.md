# 计费管理模块 - 功能设计与实现细节文档

## 1. 模块概述

### 1.1 模块定位

计费管理模块是宿舍电源管理系统的核心业务模块，负责实现宿舍用电计费和充值管理。该模块提供电费计算、余额管理、充值管理、账单生成和缴费记录等功能，确保用电费用的准确计算和便捷管理。

### 1.2 模块职责

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          计费管理模块职责边界                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                        核心职责                                   │   │
│  │  - 电费计算（阶梯电价、时段电价）                                   │   │
│  │  - 余额管理与预警                                                  │   │
│  │  - 充值记录管理                                                    │   │
│  │  - 账单生成与查询                                                  │   │
│  │  - 缴费记录管理                                                    │   │
│  │  - 电价规则配置                                                    │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                        边界职责                                   │   │
│  │  - 与遥测模块协作：获取用电量数据                                   │   │
│  │  - 与设备模块协作：关联房间设备                                     │   │
│  │  - 与控制模块协作：余额不足时断电                                   │   │
│  │  - 与告警模块协作：余额预警通知                                     │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                        非职责范围                                  │   │
│  │  - 设备管理（由设备模块负责）                                      │   │
│  │  - 遥测数据采集（由遥测模块负责）                                  │   │
│  │  - 用户认证与授权（由系统管理模块负责）                            │   │
│  │  - 告警触发（由告警模块负责）                                      │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 1.3 功能点总览

| 功能点 | 功能描述 | 优先级 |
|--------|----------|--------|
| 电费计算 | 根据用电量计算费用 | P1 |
| 余额管理 | 管理宿舍电费余额 | P1 |
| 充值管理 | 处理充值记录 | P1 |
| 账单生成 | 生成用电账单 | P1 |
| 缴费记录 | 记录缴费历史 | P1 |

---

## 2. 功能点详细设计

### 2.1 电费计算（P1）

#### 2.1.1 功能描述

根据用电量和电价规则计算电费，支持阶梯电价、时段电价和混合电价三种计价方式。

#### 2.1.2 电价类型设计

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          电价类型设计                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  阶梯电价（TIER）                                                 │   │
│  │  ┌──────────────────────────────────────────────────────────┐   │   │
│  │  │  第一阶梯：0-200度，0.5元/度                               │   │   │
│  │  │  第二阶梯：200-400度，0.6元/度                             │   │   │
│  │  │  第三阶梯：400度以上，0.8元/度                             │   │   │
│  │  └──────────────────────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  时段电价（TIME）                                                 │   │
│  │  ┌──────────────────────────────────────────────────────────┐   │   │
│  │  │  峰时：08:00-22:00，0.8元/度                              │   │   │
│  │  │  谷时：22:00-08:00，0.3元/度                              │   │   │
│  │  │  平时：其他时段，0.5元/度                                  │   │   │
│  │  └──────────────────────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  混合电价（MIXED）                                                │   │
│  │  ┌──────────────────────────────────────────────────────────┐   │   │
│  │  │  结合阶梯电价和时段电价                                    │   │   │
│  │  │  先按阶梯计算基础费用，再按时段调整                        │   │   │
│  │  └──────────────────────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 2.1.3 业务规则

| 规则编号 | 规则描述 | 规则值 |
|----------|----------|--------|
| BR-CALC-001 | 电价类型 | TIER（阶梯）、TIME（时段）、MIXED（混合） |
| BR-CALC-002 | 阶梯计算 | 按用电量分段计算，超出部分按下一阶梯计价 |
| BR-CALC-003 | 时段计算 | 按峰谷平时段分别计算 |
| BR-CALC-004 | 精度控制 | 金额保留2位小数 |
| BR-CALC-005 | 默认规则 | 无指定规则时使用第一个启用的规则 |

#### 2.1.4 数据模型

```java
@Entity
@Table(name = "electricity_price_rules")
public class ElectricityPriceRule {
    @Id
    private String id;              // 规则ID
    
    @NotNull
    private String name;            // 规则名称
    
    @NotNull
    private String type;            // 规则类型：TIER、TIME、MIXED
    
    private String description;     // 规则描述
    
    @NotNull
    private double basePrice;       // 基础电价（元/度）
    
    // 阶梯电价参数
    private double tier1Price;      // 第一阶梯电价
    private double tier1Limit;      // 第一阶梯上限（度）
    private double tier2Price;      // 第二阶梯电价
    private double tier2Limit;      // 第二阶梯上限（度）
    private double tier3Price;      // 第三阶梯电价
    
    // 时段电价参数
    private double peakPrice;       // 峰时电价
    private double valleyPrice;     // 谷时电价
    private double flatPrice;       // 平时电价
    private int peakStartHour;      // 峰时开始时间（0-23）
    private int peakEndHour;        // 峰时结束时间（0-23）
    private int valleyStartHour;    // 谷时开始时间（0-23）
    private int valleyEndHour;      // 谷时结束时间（0-23）
    
    @NotNull
    private boolean enabled;        // 是否启用
    
    @NotNull
    private long createdAt;
    
    private long updatedAt;
}
```

#### 2.1.5 实现代码

```java
@Service
public class BillingService {

    @Autowired
    private ElectricityPriceRuleRepository priceRuleRepository;

    /**
     * 计算电费
     */
    public double calculateElectricityCost(double consumption, ElectricityPriceRule rule) {
        if (consumption <= 0) {
            return 0;
        }

        double cost = 0;

        switch (rule.getType()) {
            case "TIER":
                cost = calculateTierPrice(consumption, rule);
                break;
            case "TIME":
                cost = consumption * rule.getBasePrice();
                break;
            case "MIXED":
                cost = calculateTierPrice(consumption, rule);
                break;
            default:
                cost = consumption * rule.getBasePrice();
        }

        return Math.round(cost * 100) / 100.0;
    }

    /**
     * 阶梯电价计算
     */
    private double calculateTierPrice(double consumption, ElectricityPriceRule rule) {
        double cost = 0;
        double remaining = consumption;

        // 第一阶梯
        if (rule.getTier1Limit() > 0 && remaining > 0) {
            double tier1Consumption = Math.min(remaining, rule.getTier1Limit());
            cost += tier1Consumption * rule.getTier1Price();
            remaining -= tier1Consumption;
        }

        // 第二阶梯
        if (rule.getTier2Limit() > 0 && remaining > 0) {
            double tier2Consumption = Math.min(remaining, 
                rule.getTier2Limit() - rule.getTier1Limit());
            cost += tier2Consumption * rule.getTier2Price();
            remaining -= tier2Consumption;
        }

        // 第三阶梯
        if (remaining > 0) {
            cost += remaining * rule.getTier3Price();
        }

        return cost;
    }
}
```

#### 2.1.6 测试用例

| 用例编号 | 测试场景 | 输入 | 预期输出 | 优先级 |
|----------|----------|------|----------|--------|
| TC-CALC-001 | 阶梯电价计算 | 300度，三阶梯规则 | 第一阶梯200度 + 第二阶梯100度 | P1 |
| TC-CALC-002 | 时段电价计算 | 峰时100度，谷时50度 | 分别计算后求和 | P1 |
| TC-CALC-003 | 零用电量 | 0度 | 返回0元 | P1 |
| TC-CALC-004 | 精度测试 | 123.456度 | 金额保留2位小数 | P2 |

---

### 2.2 余额管理（P1）

#### 2.2.1 功能描述

管理宿舍电费余额，支持余额查询、余额预警和自动断电功能。

#### 2.2.2 业务流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          余额管理流程                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  余额检查（定时任务）：                                                   │
│  ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐      │
│  │ 定时检查  │────>│ 查询余额  │────>│ 判断阈值  │────>│ 发送预警  │      │
│  │          │     │          │     │          │     │          │      │
│  └──────────┘     └──────────┘     └──────────┘     └──────────┘      │
│                                         │                │             │
│                                         │ 余额不足        │             │
│                                         ▼                ▼             │
│                                   ┌──────────┐    ┌──────────┐        │
│                                   │ 自动断电  │    │ 通知用户  │        │
│                                   │          │    │          │        │
│                                   └──────────┘    └──────────┘        │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 2.2.3 业务规则

| 规则编号 | 规则描述 | 规则值 |
|----------|----------|--------|
| BR-BAL-001 | 预警阈值 | 默认10元 |
| BR-BAL-002 | 预警通知 | 余额低于阈值时发送预警 |
| BR-BAL-003 | 自动断电 | 余额为0时自动断电（可配置） |
| BR-BAL-004 | 预警频率 | 每个房间每天最多发送1次预警 |

#### 2.2.4 数据模型

```java
@Entity
@Table(name = "room_balances")
public class RoomBalance {
    @Id
    private String id;              // 余额ID
    
    @NotNull
    private String roomId;          // 房间ID
    
    @NotNull
    private double balance;         // 当前余额
    
    @NotNull
    private double totalRecharged;  // 累计充值金额
    
    @NotNull
    private double totalConsumed;   // 累计消费金额
    
    private double warningThreshold; // 余额预警阈值
    
    private boolean warningSent;    // 是否已发送预警
    
    private boolean autoCutoff;     // 是否自动断电
    
    @NotNull
    private long lastRechargeAt;    // 最后充值时间
    
    @NotNull
    private long lastConsumptionAt; // 最后消费时间
    
    @NotNull
    private long createdAt;
    
    private long updatedAt;
}
```

#### 2.2.5 API接口设计

**接口：获取房间余额**

```
GET /api/billing/balance/{roomId}
Authorization: Bearer {token}

Path Parameters:
- roomId: 房间ID

Response 200:
{
    "id": "bal_abc123",
    "roomId": "A1-301",
    "balance": 50.5,
    "totalRecharged": 200.0,
    "totalConsumed": 149.5,
    "warningThreshold": 10.0,
    "warningSent": false,
    "autoCutoff": true,
    "lastRechargeAt": 1678886400,
    "lastConsumptionAt": 1678886500
}
```

**接口：获取余额不足房间**

```
GET /api/billing/low-balance?threshold={threshold}
Authorization: Bearer {token}

Query Parameters:
- threshold: 阈值（默认10.0）

Response 200:
[
    {
        "id": "bal_xyz789",
        "roomId": "A1-302",
        "balance": 5.0,
        "warningThreshold": 10.0
    }
]
```

#### 2.2.6 实现代码

```java
@Service
public class BillingService {

    @Autowired
    private RoomBalanceRepository roomBalanceRepository;

    /**
     * 获取房间余额
     */
    public RoomBalance getRoomBalance(String roomId) {
        return roomBalanceRepository.findByRoomId(roomId)
            .orElseGet(() -> createRoomBalance(roomId));
    }

    /**
     * 扣除电费
     */
    @Transactional
    public boolean deductElectricityFee(String roomId, double amount) {
        RoomBalance balance = roomBalanceRepository.findByRoomId(roomId)
            .orElseThrow(() -> new RuntimeException("Room balance not found"));

        if (balance.getBalance() < amount) {
            return false; // 余额不足
        }

        balance.setBalance(balance.getBalance() - amount);
        balance.setTotalConsumed(balance.getTotalConsumed() + amount);
        balance.setLastConsumptionAt(System.currentTimeMillis() / 1000);
        balance.setUpdatedAt(System.currentTimeMillis() / 1000);

        roomBalanceRepository.save(balance);
        return true;
    }

    /**
     * 获取余额不足的房间
     */
    public List<RoomBalance> getLowBalanceRooms(double threshold) {
        return roomBalanceRepository.findByBalanceLessThan(threshold);
    }

    private RoomBalance createRoomBalance(String roomId) {
        RoomBalance balance = new RoomBalance();
        balance.setId("bal_" + UUID.randomUUID().toString().substring(0, 8));
        balance.setRoomId(roomId);
        balance.setBalance(0);
        balance.setTotalRecharged(0);
        balance.setTotalConsumed(0);
        balance.setWarningThreshold(10);
        balance.setWarningSent(false);
        balance.setAutoCutoff(true);
        balance.setLastRechargeAt(System.currentTimeMillis() / 1000);
        balance.setLastConsumptionAt(System.currentTimeMillis() / 1000);
        balance.setCreatedAt(System.currentTimeMillis() / 1000);
        return roomBalanceRepository.save(balance);
    }
}
```

#### 2.2.7 测试用例

| 用例编号 | 测试场景 | 输入 | 预期输出 | 优先级 |
|----------|----------|------|----------|--------|
| TC-BAL-001 | 查询余额 | 有效房间ID | 返回余额信息 | P1 |
| TC-BAL-002 | 扣除电费 | 余额充足 | 扣费成功 | P1 |
| TC-BAL-003 | 余额不足 | 余额不足 | 扣费失败 | P1 |
| TC-BAL-004 | 新房间余额 | 无余额记录 | 自动创建余额记录 | P2 |

---

### 2.3 充值管理（P1）

#### 2.3.1 功能描述

处理房间电费充值，支持多种支付方式，记录充值历史。

#### 2.3.2 业务流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          充值流程                                         │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐      │
│  │ 接收充值  │────>│ 验证房间  │────>│ 更新余额  │────>│ 创建记录  │      │
│  │ 请求     │     │ 存在     │     │          │     │          │      │
│  └──────────┘     └──────────┘     └──────────┘     └──────────┘      │
│                                         │                │             │
│                                         │                ▼             │
│                                         │          ┌──────────┐       │
│                                         │          │ 清除预警  │       │
│                                         │          │ 标记     │       │
│                                         │          └──────────┘       │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 2.3.3 业务规则

| 规则编号 | 规则描述 | 规则值 |
|----------|----------|--------|
| BR-RECH-001 | 支付方式 | CASH（现金）、WECHAT（微信）、ALIPAY（支付宝） |
| BR-RECH-002 | 最小金额 | 1元 |
| BR-RECH-003 | 最大金额 | 10000元 |
| BR-RECH-004 | 交易流水 | 自动生成交易流水号 |
| BR-RECH-005 | 预警清除 | 充值后清除预警标记 |

#### 2.3.4 数据模型

```java
@Entity
@Table(name = "recharge_records")
public class RechargeRecord {
    @Id
    private String id;              // 记录ID
    
    @NotNull
    private String roomId;          // 房间ID
    
    @NotNull
    private double amount;          // 充值金额
    
    @NotNull
    private double balanceBefore;   // 充值前余额
    
    @NotNull
    private double balanceAfter;    // 充值后余额
    
    @NotNull
    private String paymentMethod;   // 支付方式：CASH、WECHAT、ALIPAY
    
    private String transactionId;   // 第三方交易号
    
    @NotNull
    private String status;          // 状态：SUCCESS、FAILED、PENDING
    
    private String operator;        // 操作员
    
    private String remark;          // 备注
    
    @NotNull
    private long createdAt;
}
```

#### 2.3.5 API接口设计

**接口：充值**

```
POST /api/billing/recharge
Authorization: Bearer {token}
Content-Type: application/json

Request Parameters:
- roomId: 房间ID（必填）
- amount: 充值金额（必填）
- paymentMethod: 支付方式（必填）
- operator: 操作员（可选）

Response 200:
{
    "id": "rec_abc123",
    "roomId": "A1-301",
    "amount": 100.0,
    "balanceBefore": 50.5,
    "balanceAfter": 150.5,
    "paymentMethod": "WECHAT",
    "status": "SUCCESS",
    "transactionId": "TXN1678886400000",
    "createdAt": 1678886400
}
```

**接口：获取充值记录**

```
GET /api/billing/recharge-records?roomId={roomId}
Authorization: Bearer {token}

Query Parameters:
- roomId: 房间ID（必填）
- page: 页码（默认0）
- size: 每页数量（默认10）

Response 200:
{
    "content": [
        {
            "id": "rec_abc123",
            "roomId": "A1-301",
            "amount": 100.0,
            "balanceBefore": 50.5,
            "balanceAfter": 150.5,
            "paymentMethod": "WECHAT",
            "status": "SUCCESS",
            "createdAt": 1678886400
        }
    ],
    "totalElements": 10,
    "totalPages": 1,
    "number": 0
}
```

#### 2.3.6 实现代码

```java
@RestController
@RequestMapping("/api/billing")
public class BillingController {

    @Autowired
    private BillingService billingService;

    /**
     * 充值
     */
    @Operation(summary = "余额充值", description = "为指定房间充值电费")
    @AuditLog(value = "电费充值", type = "BILLING")
    @PostMapping("/recharge")
    public ResponseEntity<?> recharge(
            @RequestParam String roomId,
            @RequestParam double amount,
            @RequestParam String paymentMethod,
            @RequestParam(required = false) String operator) {
        try {
            RechargeRecord record = billingService.recharge(roomId, amount, paymentMethod, operator);
            return ResponseEntity.ok(record);
        } catch (Exception e) {
            Map<String, Object> error = new HashMap<>();
            error.put("message", "Failed to recharge: " + e.getMessage());
            return ResponseEntity.status(HttpStatus.BAD_REQUEST).body(error);
        }
    }
}
```

```java
@Service
public class BillingService {

    /**
     * 充值
     */
    @Transactional
    public RechargeRecord recharge(String roomId, double amount, String paymentMethod, String operator) {
        DormRoom room = dormRoomRepository.findById(roomId)
            .orElseThrow(() -> new RuntimeException("Room not found"));

        // 获取或创建余额记录
        RoomBalance balance = roomBalanceRepository.findByRoomId(roomId)
            .orElseGet(() -> createRoomBalance(roomId));

        double balanceBefore = balance.getBalance();
        double balanceAfter = balanceBefore + amount;

        // 更新余额
        balance.setBalance(balanceAfter);
        balance.setTotalRecharged(balance.getTotalRecharged() + amount);
        balance.setLastRechargeAt(System.currentTimeMillis() / 1000);
        balance.setUpdatedAt(System.currentTimeMillis() / 1000);
        balance.setWarningSent(false); // 清除预警标记
        roomBalanceRepository.save(balance);

        // 创建充值记录
        RechargeRecord record = new RechargeRecord();
        record.setId("rec_" + UUID.randomUUID().toString().substring(0, 8));
        record.setRoomId(roomId);
        record.setAmount(amount);
        record.setBalanceBefore(balanceBefore);
        record.setBalanceAfter(balanceAfter);
        record.setPaymentMethod(paymentMethod);
        record.setStatus("SUCCESS");
        record.setOperator(operator);
        record.setTransactionId("TXN" + System.currentTimeMillis());
        record.setCreatedAt(System.currentTimeMillis() / 1000);

        return rechargeRecordRepository.save(record);
    }
}
```

#### 2.3.7 测试用例

| 用例编号 | 测试场景 | 输入 | 预期输出 | 优先级 |
|----------|----------|------|----------|--------|
| TC-RECH-001 | 正常充值 | 有效参数 | 充值成功 | P1 |
| TC-RECH-002 | 房间不存在 | 无效房间ID | 返回404错误 | P1 |
| TC-RECH-003 | 金额过小 | amount=0.5 | 返回400错误 | P1 |
| TC-RECH-004 | 金额过大 | amount=20000 | 返回400错误 | P2 |

---

### 2.4 账单生成（P1）

#### 2.4.1 功能描述

根据用电量生成月度账单，支持账单查询和状态管理。

#### 2.4.2 业务流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          账单生成流程                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐      │
│  │ 接收请求  │────>│ 查询用电  │────>│ 计算电费  │────>│ 创建账单  │      │
│  │          │     │ 量数据   │     │          │     │          │      │
│  └──────────┘     └──────────┘     └──────────┘     └──────────┘      │
│                                                                         │
│  账单状态：                                                              │
│  ┌──────────┐     ┌──────────┐     ┌──────────┐                        │
│  │ PENDING  │────>│   PAID   │────>│  完成    │                        │
│  │ (待缴费) │     │ (已缴费) │     │          │                        │
│  └──────────┘     └──────────┘     └──────────┘                        │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 2.4.3 业务规则

| 规则编号 | 规则描述 | 规则值 |
|----------|----------|--------|
| BR-BILL-001 | 账单周期 | 按月生成，格式YYYY-MM |
| BR-BILL-002 | 账单状态 | PENDING（待缴费）、PAID（已缴费）、OVERDUE（逾期） |
| BR-BILL-003 | 重复检查 | 同房间同周期只能生成一次账单 |
| BR-BILL-004 | 用电量计算 | 从遥测数据统计月度用电量 |

#### 2.4.4 数据模型

```java
@Entity
@Table(name = "electricity_bills")
public class ElectricityBill {
    @Id
    private String id;              // 账单ID
    
    @NotNull
    private String roomId;          // 房间ID
    
    @NotNull
    private String period;          // 账单周期：2024-01
    
    @NotNull
    private double totalConsumption; // 总用电量（度）
    
    @NotNull
    private double totalAmount;     // 总金额（元）
    
    private double peakConsumption; // 峰时用电量
    private double peakAmount;      // 峰时电费
    
    private double valleyConsumption; // 谷时用电量
    private double valleyAmount;    // 谷时电费
    
    private double flatConsumption; // 平时用电量
    private double flatAmount;      // 平时电费
    
    @NotNull
    private String status;          // 账单状态：PENDING、PAID、OVERDUE
    
    private long paidAt;            // 缴费时间
    
    private String paymentMethod;   // 缴费方式
    
    private String transactionId;   // 交易流水号
    
    @NotNull
    private long startDate;         // 账单开始日期
    
    @NotNull
    private long endDate;           // 账单结束日期
    
    @NotNull
    private long createdAt;
    
    private long updatedAt;
}
```

#### 2.4.5 API接口设计

**接口：生成账单**

```
POST /api/billing/bills/generate
Authorization: Bearer {token}

Query Parameters:
- roomId: 房间ID（必填）
- period: 账单周期（必填，格式：YYYY-MM）

Response 200:
{
    "id": "bill_abc123",
    "roomId": "A1-301",
    "period": "2024-01",
    "totalConsumption": 150.5,
    "totalAmount": 75.25,
    "peakConsumption": 100.0,
    "peakAmount": 50.0,
    "valleyConsumption": 50.5,
    "valleyAmount": 25.25,
    "status": "PENDING",
    "startDate": 1704067200,
    "endDate": 1706745600,
    "createdAt": 1678886400
}
```

**接口：获取房间账单**

```
GET /api/billing/bills?roomId={roomId}
Authorization: Bearer {token}

Query Parameters:
- roomId: 房间ID（必填）
- page: 页码（默认0）
- size: 每页数量（默认10）

Response 200:
{
    "content": [...],
    "totalElements": 12,
    "totalPages": 2
}
```

**接口：获取待缴费账单**

```
GET /api/billing/bills/pending
Authorization: Bearer {token}

Response 200:
[
    {
        "id": "bill_abc123",
        "roomId": "A1-301",
        "period": "2024-01",
        "totalAmount": 75.25,
        "status": "PENDING"
    }
]
```

#### 2.4.6 实现代码

```java
@Service
public class BillingService {

    /**
     * 生成月度账单
     */
    @Transactional
    public ElectricityBill generateMonthlyBill(String roomId, String period) {
        DormRoom room = dormRoomRepository.findById(roomId)
            .orElseThrow(() -> new RuntimeException("Room not found"));

        String deviceId = room.getDeviceId();
        if (deviceId == null) {
            throw new RuntimeException("Room has no associated device");
        }

        // 解析周期
        String[] parts = period.split("-");
        int year = Integer.parseInt(parts[0]);
        int month = Integer.parseInt(parts[1]);

        Calendar cal = Calendar.getInstance();
        cal.set(year, month - 1, 1, 0, 0, 0);
        long startTime = cal.getTimeInMillis() / 1000;
        cal.set(year, month, 1, 0, 0, 0);
        long endTime = cal.getTimeInMillis() / 1000;

        // 获取用电量
        List<Telemetry> telemetryList = telemetryRepository
            .findByDeviceIdAndTsBetween(deviceId, startTime, endTime);
        double totalConsumption = telemetryList.stream()
            .mapToDouble(Telemetry::getPowerW).sum() / 1000; // 转换为度

        // 获取电价规则
        ElectricityPriceRule priceRule = priceRuleRepository.findById(room.getPriceRuleId())
            .orElse(priceRuleRepository.findFirstByEnabledTrueOrderByCreatedAtAsc()
                .orElseThrow(() -> new RuntimeException("No price rule found")));

        // 计算电费
        double totalAmount = calculateElectricityCost(totalConsumption, priceRule);

        // 创建账单
        ElectricityBill bill = new ElectricityBill();
        bill.setId("bill_" + UUID.randomUUID().toString().substring(0, 8));
        bill.setRoomId(roomId);
        bill.setPeriod(period);
        bill.setTotalConsumption(Math.round(totalConsumption * 100) / 100.0);
        bill.setTotalAmount(totalAmount);
        bill.setStatus("PENDING");
        bill.setStartDate(startTime);
        bill.setEndDate(endTime);
        bill.setCreatedAt(System.currentTimeMillis() / 1000);

        return billRepository.save(bill);
    }
}
```

#### 2.4.7 测试用例

| 用例编号 | 测试场景 | 输入 | 预期输出 | 优先级 |
|----------|----------|------|----------|--------|
| TC-BILL-001 | 生成账单 | 有效房间和周期 | 账单生成成功 | P1 |
| TC-BILL-002 | 房间不存在 | 无效房间ID | 返回404错误 | P1 |
| TC-BILL-003 | 无设备房间 | 无关联设备 | 返回400错误 | P1 |
| TC-BILL-004 | 查询账单 | 有效房间ID | 返回账单列表 | P1 |

---

### 2.5 缴费记录（P1）

#### 2.5.1 功能描述

记录缴费历史，支持缴费操作和缴费记录查询。

#### 2.5.2 业务流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          缴费流程                                         │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐      │
│  │ 接收请求  │────>│ 验证账单  │────>│ 更新状态  │────>│ 记录日志  │      │
│  │          │     │ 状态     │     │          │     │          │      │
│  └──────────┘     └──────────┘     └──────────┘     └──────────┘      │
│       │                │                                             │
│       │                │ 已缴费                                       │
│       │                ▼                                             │
│       │          ┌──────────┐                                        │
│       │          │ 返回错误  │                                        │
│       │          │          │                                        │
│       │          └──────────┘                                        │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 2.5.3 业务规则

| 规则编号 | 规则描述 | 规则值 |
|----------|----------|--------|
| BR-PAY-001 | 支付方式 | CASH（现金）、WECHAT（微信）、ALIPAY（支付宝） |
| BR-PAY-002 | 状态检查 | 只有PENDING状态可以缴费 |
| BR-PAY-003 | 交易流水 | 自动生成交易流水号 |
| BR-PAY-004 | 审计日志 | 记录缴费操作 |

#### 2.5.4 API接口设计

**接口：缴纳电费**

```
POST /api/billing/bills/{billId}/pay
Authorization: Bearer {token}

Path Parameters:
- billId: 账单ID

Query Parameters:
- paymentMethod: 支付方式（必填）
- operator: 操作员（可选）

Response 200:
{
    "id": "bill_abc123",
    "roomId": "A1-301",
    "period": "2024-01",
    "totalAmount": 75.25,
    "status": "PAID",
    "paidAt": 1678886500,
    "paymentMethod": "WECHAT",
    "transactionId": "TXN1678886500000"
}
```

#### 2.5.5 实现代码

```java
@Service
public class BillingService {

    /**
     * 缴费
     */
    @Transactional
    public ElectricityBill payBill(String billId, String paymentMethod, String operator) {
        ElectricityBill bill = billRepository.findById(billId)
            .orElseThrow(() -> new RuntimeException("Bill not found"));

        if (!"PENDING".equals(bill.getStatus())) {
            throw new RuntimeException("Bill is not pending");
        }

        bill.setStatus("PAID");
        bill.setPaymentMethod(paymentMethod);
        bill.setPaidAt(System.currentTimeMillis() / 1000);
        bill.setTransactionId("TXN" + System.currentTimeMillis());
        bill.setUpdatedAt(System.currentTimeMillis() / 1000);

        return billRepository.save(bill);
    }
}
```

#### 2.5.6 测试用例

| 用例编号 | 测试场景 | 输入 | 预期输出 | 优先级 |
|----------|----------|------|----------|--------|
| TC-PAY-001 | 正常缴费 | 有效账单ID | 缴费成功 | P1 |
| TC-PAY-002 | 账单不存在 | 无效账单ID | 返回404错误 | P1 |
| TC-PAY-003 | 重复缴费 | 已缴费账单 | 返回400错误 | P1 |

---

## 3. 数据模型设计

### 3.1 实体关系图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          计费管理模块ER图                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────┐       ┌──────────────┐       ┌──────────────┐        │
│  │  DormRoom    │       │ RoomBalance  │       │RechargeRecord│        │
│  ├──────────────┤       ├──────────────┤       ├──────────────┤        │
│  │ id (PK)      │◄──────│ roomId(FK)   │       │ id (PK)      │        │
│  │ name         │  1:1  │ balance      │       │ roomId(FK)   │──────┐ │
│  │ deviceId     │       │ totalRecharged│      │ amount       │      │ │
│  │ priceRuleId  │       │ totalConsumed│       │ balanceBefore│      │ │
│  └──────────────┘       │ warningThresh│       │ balanceAfter │      │ │
│         │               └──────────────┘       │ paymentMethod│      │ │
│         │                                      │ status       │      │ │
│         │                                      └──────────────┘      │ │
│         │                                             │              │ │
│         │                                             │              │ │
│         ▼                                             ▼              │ │
│  ┌──────────────┐                              ┌──────────────┐      │ │
│  │ElectricityPri│                              │  DormRoom    │      │ │
│  │  ceRule      │                              └──────────────┘      │ │
│  ├──────────────┤                                     ▲              │ │
│  │ id (PK)      │                                     │              │ │
│  │ name         │                                     └──────────────┘ │
│  │ type         │                                                      │
│  │ basePrice    │                                                      │
│  └──────────────┘                                                      │
│         │                                                               │
│         │                                                               │
│         ▼                                                               │
│  ┌──────────────┐                                                      │
│  │ElectricityBill│                                                     │
│  ├──────────────┤                                                      │
│  │ id (PK)      │                                                      │
│  │ roomId(FK)   │                                                      │
│  │ period       │                                                      │
│  │ totalAmount  │                                                      │
│  │ status       │                                                      │
│  └──────────────┘                                                      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 3.2 数据表设计

#### 3.2.1 electricity_price_rules（电价规则表）

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
|--------|----------|------|--------|------|
| id | VARCHAR(64) | PRIMARY KEY | - | 规则ID |
| name | VARCHAR(128) | NOT NULL | - | 规则名称 |
| type | VARCHAR(16) | NOT NULL | - | 规则类型 |
| description | VARCHAR(500) | NULL | - | 规则描述 |
| base_price | DOUBLE | NOT NULL | - | 基础电价 |
| tier1_price | DOUBLE | NULL | - | 第一阶梯电价 |
| tier1_limit | DOUBLE | NULL | - | 第一阶梯上限 |
| tier2_price | DOUBLE | NULL | - | 第二阶梯电价 |
| tier2_limit | DOUBLE | NULL | - | 第二阶梯上限 |
| tier3_price | DOUBLE | NULL | - | 第三阶梯电价 |
| peak_price | DOUBLE | NULL | - | 峰时电价 |
| valley_price | DOUBLE | NULL | - | 谷时电价 |
| flat_price | DOUBLE | NULL | - | 平时电价 |
| enabled | BOOLEAN | NOT NULL | TRUE | 是否启用 |
| created_at | BIGINT | NOT NULL | - | 创建时间 |
| updated_at | BIGINT | NULL | - | 更新时间 |

#### 3.2.2 room_balances（房间余额表）

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
|--------|----------|------|--------|------|
| id | VARCHAR(64) | PRIMARY KEY | - | 余额ID |
| room_id | VARCHAR(64) | NOT NULL UNIQUE | - | 房间ID |
| balance | DOUBLE | NOT NULL | 0 | 当前余额 |
| total_recharged | DOUBLE | NOT NULL | 0 | 累计充值 |
| total_consumed | DOUBLE | NOT NULL | 0 | 累计消费 |
| warning_threshold | DOUBLE | NULL | 10 | 预警阈值 |
| warning_sent | BOOLEAN | NULL | FALSE | 是否已预警 |
| auto_cutoff | BOOLEAN | NULL | TRUE | 是否自动断电 |
| last_recharge_at | BIGINT | NOT NULL | - | 最后充值时间 |
| last_consumption_at | BIGINT | NOT NULL | - | 最后消费时间 |
| created_at | BIGINT | NOT NULL | - | 创建时间 |
| updated_at | BIGINT | NULL | - | 更新时间 |

#### 3.2.3 recharge_records（充值记录表）

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
|--------|----------|------|--------|------|
| id | VARCHAR(64) | PRIMARY KEY | - | 记录ID |
| room_id | VARCHAR(64) | NOT NULL | - | 房间ID |
| amount | DOUBLE | NOT NULL | - | 充值金额 |
| balance_before | DOUBLE | NOT NULL | - | 充值前余额 |
| balance_after | DOUBLE | NOT NULL | - | 充值后余额 |
| payment_method | VARCHAR(16) | NOT NULL | - | 支付方式 |
| transaction_id | VARCHAR(64) | NULL | - | 交易流水号 |
| status | VARCHAR(16) | NOT NULL | - | 状态 |
| operator | VARCHAR(64) | NULL | - | 操作员 |
| remark | VARCHAR(500) | NULL | - | 备注 |
| created_at | BIGINT | NOT NULL | - | 创建时间 |

#### 3.2.4 electricity_bills（电费账单表）

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
|--------|----------|------|--------|------|
| id | VARCHAR(64) | PRIMARY KEY | - | 账单ID |
| room_id | VARCHAR(64) | NOT NULL | - | 房间ID |
| period | VARCHAR(16) | NOT NULL | - | 账单周期 |
| total_consumption | DOUBLE | NOT NULL | - | 总用电量 |
| total_amount | DOUBLE | NOT NULL | - | 总金额 |
| peak_consumption | DOUBLE | NULL | - | 峰时用电量 |
| peak_amount | DOUBLE | NULL | - | 峰时电费 |
| valley_consumption | DOUBLE | NULL | - | 谷时用电量 |
| valley_amount | DOUBLE | NULL | - | 谷时电费 |
| flat_consumption | DOUBLE | NULL | - | 平时用电量 |
| flat_amount | DOUBLE | NULL | - | 平时电费 |
| status | VARCHAR(16) | NOT NULL | - | 账单状态 |
| paid_at | BIGINT | NULL | - | 缴费时间 |
| payment_method | VARCHAR(16) | NULL | - | 缴费方式 |
| transaction_id | VARCHAR(64) | NULL | - | 交易流水号 |
| start_date | BIGINT | NOT NULL | - | 开始日期 |
| end_date | BIGINT | NOT NULL | - | 结束日期 |
| created_at | BIGINT | NOT NULL | - | 创建时间 |
| updated_at | BIGINT | NULL | - | 更新时间 |

---

## 4. API接口汇总

### 4.1 电价规则接口

| 接口 | 方法 | 路径 | 描述 | 优先级 |
|------|------|------|------|--------|
| 获取电价规则列表 | GET | /api/billing/price-rules | 获取所有电价规则 | P1 |
| 创建电价规则 | POST | /api/billing/price-rules | 创建新的电价规则 | P1 |
| 更新电价规则 | PUT | /api/billing/price-rules/{id} | 更新电价规则 | P1 |
| 删除电价规则 | DELETE | /api/billing/price-rules/{id} | 删除电价规则 | P1 |

### 4.2 余额管理接口

| 接口 | 方法 | 路径 | 描述 | 优先级 |
|------|------|------|------|--------|
| 获取房间余额 | GET | /api/billing/balance/{roomId} | 获取房间余额信息 | P1 |
| 获取余额不足房间 | GET | /api/billing/low-balance | 获取余额不足房间列表 | P1 |

### 4.3 充值管理接口

| 接口 | 方法 | 路径 | 描述 | 优先级 |
|------|------|------|------|--------|
| 充值 | POST | /api/billing/recharge | 为房间充值 | P1 |
| 获取充值记录 | GET | /api/billing/recharge-records | 获取充值记录列表 | P1 |

### 4.4 账单管理接口

| 接口 | 方法 | 路径 | 描述 | 优先级 |
|------|------|------|------|--------|
| 生成账单 | POST | /api/billing/bills/generate | 生成月度账单 | P1 |
| 获取房间账单 | GET | /api/billing/bills | 获取房间账单列表 | P1 |
| 获取待缴费账单 | GET | /api/billing/bills/pending | 获取待缴费账单列表 | P1 |
| 缴纳电费 | POST | /api/billing/bills/{billId}/pay | 缴纳电费 | P1 |

---

## 5. 测试方案

### 5.1 单元测试

```java
@SpringBootTest
class BillingServiceTest {

    @Autowired
    private BillingService billingService;

    @MockBean
    private RoomBalanceRepository roomBalanceRepository;

    @MockBean
    private ElectricityPriceRuleRepository priceRuleRepository;

    @Test
    void testCalculateTierPrice() {
        ElectricityPriceRule rule = new ElectricityPriceRule();
        rule.setType("TIER");
        rule.setTier1Price(0.5);
        rule.setTier1Limit(200);
        rule.setTier2Price(0.6);
        rule.setTier2Limit(400);
        rule.setTier3Price(0.8);

        double cost = billingService.calculateElectricityCost(300, rule);

        // 第一阶梯200度 * 0.5 + 第二阶梯100度 * 0.6 = 100 + 60 = 160
        assertEquals(160.0, cost);
    }

    @Test
    void testRecharge() {
        RoomBalance balance = new RoomBalance();
        balance.setRoomId("A1-301");
        balance.setBalance(50.0);
        balance.setTotalRecharged(200.0);

        when(roomBalanceRepository.findByRoomId("A1-301"))
            .thenReturn(Optional.of(balance));

        RechargeRecord record = billingService.recharge("A1-301", 100.0, "WECHAT", "admin");

        assertEquals(50.0, record.getBalanceBefore());
        assertEquals(150.0, record.getBalanceAfter());
        assertEquals("SUCCESS", record.getStatus());
    }
}
```

### 5.2 API测试脚本

```python
import requests

BASE_URL = "http://localhost:8000/api"

def test_billing_apis():
    # 登录获取token
    login_response = requests.post(f"{BASE_URL}/auth/login", json={
        "account": "admin",
        "password": "admin123"
    })
    token = login_response.json()["token"]
    headers = {"Authorization": f"Bearer {token}"}

    # 测试获取电价规则
    rules_response = requests.get(
        f"{BASE_URL}/billing/price-rules",
        headers=headers
    )
    assert rules_response.status_code == 200
    print(f"✅ 获取电价规则成功")

    # 测试获取房间余额
    balance_response = requests.get(
        f"{BASE_URL}/billing/balance/A1-301",
        headers=headers
    )
    assert balance_response.status_code == 200
    print(f"✅ 获取房间余额成功")

    # 测试充值
    recharge_response = requests.post(
        f"{BASE_URL}/billing/recharge",
        headers=headers,
        params={
            "roomId": "A1-301",
            "amount": 100.0,
            "paymentMethod": "WECHAT"
        }
    )
    assert recharge_response.status_code == 200
    print(f"✅ 充值成功")

    # 测试获取充值记录
    records_response = requests.get(
        f"{BASE_URL}/billing/recharge-records",
        headers=headers,
        params={"roomId": "A1-301"}
    )
    assert records_response.status_code == 200
    print(f"✅ 获取充值记录成功")

if __name__ == "__main__":
    test_billing_apis()
    print("\n所有API测试通过！")
```

---

## 6. 总结

本文档详细描述了计费管理模块的功能设计与实现细节，包括：

1. **功能点设计**：5个核心功能点的详细设计
2. **电价类型**：阶梯电价、时段电价、混合电价三种计价方式
3. **数据模型设计**：4个数据表的完整设计
4. **API接口汇总**：12个API接口的完整定义
5. **实现代码**：完整的实现代码示例
6. **测试方案**：单元测试和API测试脚本

该模块采用Spring Boot 3.2.3实现，支持多种电价计算方式，提供完整的充值和账单管理功能，是一个完整的计费管理解决方案。

---

**文档版本**：v1.0  
**编写日期**：2026年3月14日  
**编写人**：系统架构组
