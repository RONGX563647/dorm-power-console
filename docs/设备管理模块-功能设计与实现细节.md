# 设备管理模块 - 功能设计与实现细节文档

## 1. 模块概述

### 1.1 模块定位

设备管理模块是宿舍电源管理系统的核心模块之一，负责实现对宿舍智能插座设备的全生命周期管理。该模块提供设备的注册、查询、状态监控、分组管理和固件升级等功能，是系统与IoT设备交互的基础。

### 1.2 模块职责

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          设备管理模块职责边界                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                        核心职责                                   │   │
│  │  - 设备注册与生命周期管理                                          │   │
│  │  - 设备状态查询与监控                                              │   │
│  │  - 设备在线状态检测                                                │   │
│  │  - 设备分组管理                                                    │   │
│  │  - 设备固件版本管理                                                │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                        边界职责                                   │   │
│  │  - 与MQTT模块协作：接收设备心跳和状态上报                           │   │
│  │  - 与控制模块协作：提供设备信息支持命令下发                          │   │
│  │  - 与告警模块协作：提供设备状态触发告警                              │   │
│  │  - 与WebSocket协作：推送设备状态变更                                │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                        非职责范围                                  │   │
│  │  - 设备控制命令下发（由控制模块负责）                                │   │
│  │  - 遥测数据存储与查询（由遥测模块负责）                              │   │
│  │  - 告警规则配置与触发（由告警模块负责）                              │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 1.3 功能点总览

| 功能点 | 功能描述 | 输入 | 输出 | 优先级 |
|--------|----------|------|------|--------|
| 设备自动注册 | 设备首次连接时自动在系统中注册 | 设备ID、房间信息 | 注册成功确认 | P0 |
| 设备列表查询 | 查询所有设备的列表信息 | 查询条件、分页参数 | 设备列表 | P0 |
| 设备状态查询 | 查询单个设备的详细状态 | 设备ID | 设备状态详情 | P0 |
| 设备在线检测 | 自动检测设备在线/离线状态 | 设备心跳 | 状态更新 | P0 |
| 设备分组管理 | 对设备进行分组管理 | 分组信息 | 分组结果 | P1 |
| 设备固件管理 | 管理设备固件版本 | 固件文件 | 版本信息 | P2 |

---

## 2. 功能点详细设计

### 2.1 设备自动注册（P0）

#### 2.1.1 功能描述

设备首次连接到系统时，自动在数据库中注册设备信息，无需人工干预。

#### 2.1.2 业务流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          设备自动注册流程                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐      │
│  │ 设备连接  │────>│ MQTT认证 │────>│ 检查注册 │────>│ 自动注册 │      │
│  │ MQTT     │     │  成功    │     │ 状态     │     │ 设备     │      │
│  └──────────┘     └──────────┘     └──────────┘     └──────────┘      │
│       │                │                │                │            │
│       │                │                │                │            │
│       │                │                ▼                ▼            │
│       │                │          ┌──────────┐     ┌──────────┐      │
│       │                │          │ 已注册？  │────>│ 更新状态  │      │
│       │                │          │          │     │ lastSeen │      │
│       │                │          └──────────┘     └──────────┘      │
│       │                │                │                             │
│       │                │                │ 是                          │
│       │                │                ▼                             │
│       │                │          ┌──────────┐                        │
│       │                │          │ 返回成功  │                        │
│       │                │          └──────────┘                        │
│       │                │                                             │
│       ▼                ▼                                             │
│  ┌──────────┐     ┌──────────┐                                      │
│  │ 认证失败  │────>│ 拒绝连接  │                                      │
│  └──────────┘     └──────────┘                                      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 2.1.3 业务规则

| 规则编号 | 规则描述 | 规则值 |
|----------|----------|--------|
| BR-REG-001 | 设备ID格式校验 | 支持字母、数字、下划线，长度不超过64字符 |
| BR-REG-002 | 房间编号格式 | 楼栋号-楼层-房间号（如：A1-301） |
| BR-REG-003 | 设备初始状态 | 新注册设备默认为离线状态 |
| BR-REG-004 | 设备唯一性 | 同一设备ID只能注册一次 |
| BR-REG-005 | 注册时间记录 | 记录设备创建时间（createdAt） |

#### 2.1.4 数据模型

```java
@Entity
@Table(name = "devices")
public class Device {
    @Id
    private String id;              // 设备唯一标识符
    
    @NotNull
    private String name;            // 设备名称
    
    @NotNull
    private String room;            // 所在房间号
    
    @NotNull
    private boolean online;         // 在线状态
    
    @NotNull
    private long lastSeenTs;        // 最后心跳时间戳（秒）
    
    @NotNull
    private long createdAt;         // 创建时间戳（秒）
}
```

#### 2.1.5 API接口设计

**接口：创建设备**

```
POST /api/devices
Content-Type: application/json
Authorization: Bearer {token}

Request Body:
{
    "id": "device_001",
    "name": "A1-301智能插座",
    "room": "A1-301"
}

Response 200:
{
    "id": "device_001",
    "name": "A1-301智能插座",
    "room": "A1-301",
    "online": false,
    "lastSeenTs": 1678886400,
    "createdAt": 1678886400
}

Response 400:
{
    "message": "Failed to create device: Device ID already exists"
}
```

#### 2.1.6 实现代码

**Controller层：**

```java
@RestController
@RequestMapping("/api")
@Tag(name = "设备管理", description = "设备查询和状态管理接口")
public class DeviceController {

    @Autowired
    private DeviceRepository deviceRepository;

    @Operation(summary = "创建设备", description = "创建新的设备记录")
    @RateLimit(value = 2.0, type = "create-device")
    @AuditLog(value = "创建设备", type = "DEVICE")
    @PostMapping("/devices")
    public ResponseEntity<?> createDevice(@Valid @RequestBody Device device) {
        try {
            device.setCreatedAt(System.currentTimeMillis() / 1000);
            device.setLastSeenTs(System.currentTimeMillis() / 1000);
            device.setOnline(false);
            
            Device savedDevice = deviceRepository.save(device);
            return ResponseEntity.ok(savedDevice);
        } catch (Exception e) {
            Map<String, Object> error = new HashMap<>();
            error.put("message", "Failed to create device: " + e.getMessage());
            return ResponseEntity.status(HttpStatus.BAD_REQUEST).body(error);
        }
    }
}
```

**Service层：**

```java
@Service
public class DeviceService {

    @Autowired
    private DeviceRepository deviceRepository;

    @CacheEvict(value = "devices", allEntries = true)
    public Device addDevice(Device device) {
        logger.debug("添加设备: {}", device.getId());
        device.setCreatedAt(System.currentTimeMillis());
        device.setLastSeenTs(System.currentTimeMillis());
        Device savedDevice = deviceRepository.save(device);
        logger.info("设备添加成功: {}", savedDevice.getId());
        return savedDevice;
    }
}
```

#### 2.1.7 测试用例

| 用例编号 | 测试场景 | 输入 | 预期输出 | 优先级 |
|----------|----------|------|----------|--------|
| TC-REG-001 | 正常注册新设备 | 有效设备ID和房间信息 | 返回200，设备创建成功 | P0 |
| TC-REG-002 | 注册已存在设备 | 已存在的设备ID | 返回400，提示设备已存在 | P0 |
| TC-REG-003 | 设备ID格式错误 | 超过64字符的设备ID | 返回400，参数校验失败 | P1 |
| TC-REG-004 | 缺少必填字段 | 缺少name字段 | 返回400，参数校验失败 | P1 |
| TC-REG-005 | 房间格式错误 | 无效的房间编号格式 | 返回400，参数校验失败 | P2 |

---

### 2.2 设备列表查询（P0）

#### 2.2.1 功能描述

查询系统中所有注册设备的列表信息，支持按条件筛选和分页查询。

#### 2.2.2 业务流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          设备列表查询流程                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐      │
│  │ 接收请求  │────>│ 权限验证  │────>│ 查询设备  │────>│ 计算在线  │      │
│  │          │     │          │     │ 列表     │     │ 状态     │      │
│  └──────────┘     └──────────┘     └──────────┘     └──────────┘      │
│                                                          │             │
│                                                          ▼             │
│                                                    ┌──────────┐       │
│                                                    │ 格式化响应 │       │
│                                                    │ 返回列表  │       │
│                                                    └──────────┘       │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 2.2.3 业务规则

| 规则编号 | 规则描述 | 规则值 |
|----------|----------|--------|
| BR-LIST-001 | 在线状态判定 | 最后心跳时间在60秒内判定为在线 |
| BR-LIST-002 | 时间格式转换 | lastSeenTs转换为ISO 8601格式返回 |
| BR-LIST-003 | 缓存策略 | 设备列表缓存5分钟 |
| BR-LIST-004 | 排序规则 | 按创建时间倒序排列 |

#### 2.2.4 API接口设计

**接口：获取设备列表**

```
GET /api/devices
Authorization: Bearer {token}

Response 200:
[
    {
        "id": "device_001",
        "name": "A1-301智能插座",
        "room": "A1-301",
        "online": true,
        "lastSeen": "2024-03-15T10:30:00Z"
    },
    {
        "id": "device_002",
        "name": "A1-302智能插座",
        "room": "A1-302",
        "online": false,
        "lastSeen": "2024-03-15T09:00:00Z"
    }
]
```

**接口：获取房间设备列表**

```
GET /api/devices/room/{room}
Authorization: Bearer {token}

Path Parameters:
- room: 房间号（如：A1-301）

Response 200:
[
    {
        "id": "device_001",
        "name": "A1-301智能插座",
        "room": "A1-301",
        "online": true,
        "lastSeen": "2024-03-15T10:30:00Z"
    }
]
```

#### 2.2.5 实现代码

```java
@GetMapping("/devices")
public List<Map<String, Object>> getDevices() {
    List<Device> devices = deviceRepository.findAll();
    List<Map<String, Object>> result = new ArrayList<>();
    
    long now = System.currentTimeMillis() / 1000;
    
    for (Device d : devices) {
        boolean isOnline = d.isOnline() && (now - d.getLastSeenTs()) < ONLINE_TIMEOUT_SECONDS;
        
        Map<String, Object> deviceMap = new HashMap<>();
        deviceMap.put("id", d.getId());
        deviceMap.put("name", d.getName());
        deviceMap.put("room", d.getRoom());
        deviceMap.put("online", isOnline);
        deviceMap.put("lastSeen", Instant.ofEpochSecond(d.getLastSeenTs()).toString());
        result.add(deviceMap);
    }
    
    return result;
}

@GetMapping("/devices/room/{room}")
public ResponseEntity<?> getDevicesByRoom(@PathVariable String room) {
    List<Device> devices = deviceRepository.findByRoom(room);
    List<Map<String, Object>> result = new ArrayList<>();
    
    long now = System.currentTimeMillis() / 1000;
    
    for (Device d : devices) {
        boolean isOnline = d.isOnline() && (now - d.getLastSeenTs()) < ONLINE_TIMEOUT_SECONDS;
        
        Map<String, Object> deviceMap = new HashMap<>();
        deviceMap.put("id", d.getId());
        deviceMap.put("name", d.getName());
        deviceMap.put("room", d.getRoom());
        deviceMap.put("online", isOnline);
        deviceMap.put("lastSeen", Instant.ofEpochSecond(d.getLastSeenTs()).toString());
        result.add(deviceMap);
    }
    
    return ResponseEntity.ok(result);
}
```

#### 2.2.6 测试用例

| 用例编号 | 测试场景 | 输入 | 预期输出 | 优先级 |
|----------|----------|------|----------|--------|
| TC-LIST-001 | 查询所有设备 | 无参数 | 返回所有设备列表 | P0 |
| TC-LIST-002 | 查询指定房间设备 | room=A1-301 | 返回该房间设备列表 | P0 |
| TC-LIST-003 | 查询空房间设备 | room=Z9-999 | 返回空列表 | P1 |
| TC-LIST-004 | 未授权访问 | 无token | 返回401未授权 | P0 |

---

### 2.3 设备状态查询（P0）

#### 2.3.1 功能描述

查询单个设备的详细状态信息，包括总功率、电压、电流和各插座状态。

#### 2.3.2 业务流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          设备状态查询流程                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐      │
│  │ 接收请求  │────>│ 查询设备  │────>│ 查询状态  │────>│ 判断在线  │      │
│  │          │     │ 基本信息  │     │ 信息     │     │ 状态     │      │
│  └──────────┘     └──────────┘     └──────────┘     └──────────┘      │
│       │                │                │                │            │
│       │                │ 不存在          │                │            │
│       │                ▼                │                ▼            │
│       │          ┌──────────┐          │          ┌──────────┐       │
│       │          │ 返回404  │          │          │ 解析插座  │       │
│       │          │ 设备不存在│          │          │ 状态JSON │       │
│       │          └──────────┘          │          └──────────┘       │
│       │                                │                │            │
│       │                                │                ▼            │
│       │                                │          ┌──────────┐       │
│       │                                │          │ 构建响应  │       │
│       │                                │          │ 返回状态  │       │
│       │                                │          └──────────┘       │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 2.3.3 业务规则

| 规则编号 | 规则描述 | 规则值 |
|----------|----------|--------|
| BR-STATUS-001 | 在线状态判定 | 需同时满足：Device表在线 + StripStatus表在线 + 心跳在超时范围内 |
| BR-STATUS-002 | 超时阈值 | 60秒无心跳判定为离线 |
| BR-STATUS-003 | 插座状态解析 | 解析socketsJson字段，提取id、on、power_w |
| BR-STATUS-004 | 缓存策略 | 设备状态缓存1分钟 |

#### 2.3.4 数据模型

```java
@Entity
@Table(name = "strip_status")
public class StripStatus {
    @Id
    private String deviceId;        // 设备ID
    
    @NotNull
    private long ts;                // 状态更新时间戳（秒）
    
    @NotNull
    private boolean online;         // 设备在线状态
    
    @NotNull
    private double totalPowerW;     // 总功率（瓦）
    
    @NotNull
    private double voltageV;        // 电压（伏）
    
    @NotNull
    private double currentA;        // 电流（安）
    
    @NotNull
    private String socketsJson;     // 各插孔状态的JSON字符串
}
```

#### 2.3.5 API接口设计

**接口：获取设备状态**

```
GET /api/devices/{deviceId}/status
Authorization: Bearer {token}

Path Parameters:
- deviceId: 设备ID

Response 200:
{
    "ts": 1678886400,
    "online": true,
    "total_power_w": 150.5,
    "voltage_v": 220.0,
    "current_a": 0.68,
    "sockets": [
        {
            "id": 1,
            "on": true,
            "power_w": 50.0
        },
        {
            "id": 2,
            "on": false,
            "power_w": 0.0
        }
    ]
}

Response 404:
{
    "message": "device not found"
}
```

#### 2.3.6 实现代码

```java
@GetMapping("/devices/{deviceId}/status")
public ResponseEntity<?> getDeviceStatus(@PathVariable String deviceId) {
    Device device = deviceRepository.findById(deviceId).orElse(null);
    StripStatus status = stripStatusRepository.findByDeviceId(deviceId);
    
    if (device == null || status == null) {
        throw new ResourceNotFoundException("device not found");
    }

    long now = System.currentTimeMillis() / 1000;
    boolean isOnline = device.isOnline() && status.isOnline() 
        && (now - device.getLastSeenTs()) < ONLINE_TIMEOUT_SECONDS;

    List<Map<String, Object>> sockets = new ArrayList<>();
    try {
        List<?> socketList = objectMapper.readValue(status.getSocketsJson(), List.class);
        for (Object s : socketList) {
            if (s instanceof Map<?, ?> sMap) {
                Map<String, Object> socket = new HashMap<>();
                socket.put("id", sMap.get("id"));
                socket.put("on", sMap.get("on"));
                socket.put("power_w", sMap.get("power_w"));
                sockets.add(socket);
            }
        }
    } catch (Exception e) {
        // JSON解析失败时忽略错误
    }

    Map<String, Object> response = new HashMap<>();
    response.put("ts", status.getTs());
    response.put("online", isOnline);
    response.put("total_power_w", status.getTotalPowerW());
    response.put("voltage_v", status.getVoltageV());
    response.put("current_a", status.getCurrentA());
    response.put("sockets", sockets);
    
    return ResponseEntity.ok(response);
}
```

#### 2.3.7 测试用例

| 用例编号 | 测试场景 | 输入 | 预期输出 | 优先级 |
|----------|----------|------|----------|--------|
| TC-STATUS-001 | 查询在线设备状态 | 有效设备ID | 返回完整状态信息，online=true | P0 |
| TC-STATUS-002 | 查询离线设备状态 | 离线设备ID | 返回状态信息，online=false | P0 |
| TC-STATUS-003 | 查询不存在设备 | 无效设备ID | 返回404 | P0 |
| TC-STATUS-004 | 插座状态解析异常 | socketsJson格式错误 | 返回空插座列表 | P1 |

---

### 2.4 设备在线检测（P0）

#### 2.4.1 功能描述

通过设备心跳自动检测设备的在线/离线状态，并实时更新数据库。

#### 2.4.2 业务流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          设备在线检测流程                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                        心跳检测机制                                │  │
│  │                                                                   │  │
│  │  设备 ──(心跳)──> MQTT Broker ──(消息)──> 后端服务                │  │
│  │                                                   │               │  │
│  │                                                   ▼               │  │
│  │                                          ┌──────────────┐        │  │
│  │                                          │ 更新lastSeenTs│        │  │
│  │                                          │ 设置online=true│       │  │
│  │                                          └──────────────┘        │  │
│  │                                                                   │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                        超时检测机制                                │  │
│  │                                                                   │  │
│  │  定时任务(每10秒) ──> 检查所有设备 ──> lastSeenTs超时?             │  │
│  │                                           │                       │  │
│  │                                           ▼                       │  │
│  │                                   ┌──────────────┐                │  │
│  │                                   │ 设置online=false│              │  │
│  │                                   │ 推送离线通知    │              │  │
│  │                                   └──────────────┘                │  │
│  │                                                                   │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 2.4.3 业务规则

| 规则编号 | 规则描述 | 规则值 |
|----------|----------|--------|
| BR-ONLINE-001 | 心跳超时阈值 | 60秒无心跳判定为离线 |
| BR-ONLINE-002 | 心跳主题 | `dorm/{room}/{device}/status` |
| BR-ONLINE-003 | 离线检测频率 | 每10秒检测一次 |
| BR-ONLINE-004 | 状态变更通知 | 状态变更时通过WebSocket推送 |
| BR-ONLINE-005 | 遗嘱消息 | 设备断开时发送LWT消息 |

#### 2.4.4 MQTT主题设计

| 主题模式 | 方向 | 用途 | QoS |
|----------|------|------|-----|
| `dorm/{room}/{device}/status` | 设备→服务器 | 状态上报（心跳） | 1 |
| `dorm/{room}/{device}/lwt` | 设备→服务器 | 离线通知（遗嘱） | 1 |

#### 2.4.5 实现代码

**MQTT消息处理器：**

```java
@Component
public class MqttBridge implements MqttCallback {

    @Autowired
    private DeviceService deviceService;
    
    @Autowired
    private WebSocketNotificationService webSocketService;

    private static final String STATUS_TOPIC_PATTERN = "dorm/+/+/status";
    private static final String LWT_TOPIC_PATTERN = "dorm/+/+/lwt";

    @Override
    public void messageArrived(String topic, MqttMessage message) {
        try {
            String payload = new String(message.getPayload());
            
            if (topic.matches(STATUS_TOPIC_PATTERN.replace("+", "[^/]+"))) {
                handleStatusMessage(topic, payload);
            } else if (topic.matches(LWT_TOPIC_PATTERN.replace("+", "[^/]+"))) {
                handleLwtMessage(topic, payload);
            }
        } catch (Exception e) {
            logger.error("处理MQTT消息失败: {}", e.getMessage());
        }
    }

    private void handleStatusMessage(String topic, String payload) {
        // 解析主题获取设备ID
        String[] parts = topic.split("/");
        String deviceId = parts[2];
        
        // 更新设备状态
        deviceService.updateDeviceStatus(deviceId, true);
        
        // 推送WebSocket通知
        webSocketService.broadcastDeviceStatus(deviceId, true);
    }

    private void handleLwtMessage(String topic, String payload) {
        String[] parts = topic.split("/");
        String deviceId = parts[2];
        
        // 更新设备为离线
        deviceService.updateDeviceStatus(deviceId, false);
        
        // 推送离线通知
        webSocketService.broadcastDeviceStatus(deviceId, false);
    }
}
```

**定时离线检测：**

```java
@Component
public class DeviceStatusMonitorScheduler {

    @Autowired
    private DeviceRepository deviceRepository;
    
    @Autowired
    private WebSocketNotificationService webSocketService;

    private static final long ONLINE_TIMEOUT_SECONDS = 60;

    @Scheduled(fixedRate = 10000) // 每10秒执行一次
    public void checkDeviceOnlineStatus() {
        long now = System.currentTimeMillis() / 1000;
        List<Device> devices = deviceRepository.findAll();
        
        for (Device device : devices) {
            if (device.isOnline()) {
                long offlineThreshold = now - ONLINE_TIMEOUT_SECONDS;
                if (device.getLastSeenTs() < offlineThreshold) {
                    // 设备超时，标记为离线
                    device.setOnline(false);
                    deviceRepository.save(device);
                    
                    // 推送离线通知
                    webSocketService.broadcastDeviceStatus(device.getId(), false);
                    
                    logger.info("设备离线: {}", device.getId());
                }
            }
        }
    }
}
```

#### 2.4.6 测试用例

| 用例编号 | 测试场景 | 输入 | 预期输出 | 优先级 |
|----------|----------|------|----------|--------|
| TC-ONLINE-001 | 设备心跳正常 | 心跳消息 | 设备保持在线状态 | P0 |
| TC-ONLINE-002 | 设备心跳超时 | 60秒无心跳 | 设备标记为离线 | P0 |
| TC-ONLINE-003 | 设备断开连接 | LWT消息 | 设备立即标记为离线 | P0 |
| TC-ONLINE-004 | 设备重新上线 | 心跳恢复 | 设备标记为在线 | P1 |

---

### 2.5 设备分组管理（P1）

#### 2.5.1 功能描述

对设备进行分组管理，支持按楼栋、楼层、房间等维度进行分组，便于批量操作和管理。

#### 2.5.2 业务流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          设备分组管理流程                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                        分组层级结构                                │  │
│  │                                                                   │  │
│  │                      ┌───────────┐                               │  │
│  │                      │  楼栋分组  │                               │  │
│  │                      │  (building)│                               │  │
│  │                      └─────┬─────┘                               │  │
│  │                            │                                      │  │
│  │              ┌─────────────┼─────────────┐                       │  │
│  │              │             │             │                       │  │
│  │              ▼             ▼             ▼                       │  │
│  │        ┌───────────┐ ┌───────────┐ ┌───────────┐                │  │
│  │        │ 楼层分组   │ │ 楼层分组   │ │ 楼层分组   │                │  │
│  │        │  (floor)  │ │  (floor)  │ │  (floor)  │                │  │
│  │        └─────┬─────┘ └─────┬─────┘ └─────┬─────┘                │  │
│  │              │             │             │                       │  │
│  │              ▼             ▼             ▼                       │  │
│  │        ┌───────────┐ ┌───────────┐ ┌───────────┐                │  │
│  │        │ 房间分组   │ │ 房间分组   │ │ 房间分组   │                │  │
│  │        │  (room)   │ │  (room)   │ │  (room)   │                │  │
│  │        └─────┬─────┘ └─────┬─────┘ └─────┬─────┘                │  │
│  │              │             │             │                       │  │
│  │              ▼             ▼             ▼                       │  │
│  │        ┌───────────┐ ┌───────────┐ ┌───────────┐                │  │
│  │        │   设备    │ │   设备    │ │   设备    │                │  │
│  │        └───────────┘ └───────────┘ └───────────┘                │  │
│  │                                                                   │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 2.5.3 业务规则

| 规则编号 | 规则描述 | 规则值 |
|----------|----------|--------|
| BR-GROUP-001 | 分组类型 | building（楼栋）、floor（楼层）、room（房间） |
| BR-GROUP-002 | 层级关系 | 通过parentId建立父子关系 |
| BR-GROUP-003 | 设备多分组 | 一个设备可以属于多个分组 |
| BR-GROUP-004 | 分组删除 | 删除分组时同时删除设备关联关系 |

#### 2.5.4 数据模型

```java
@Entity
@Table(name = "device_groups")
public class DeviceGroup {
    @Id
    private String id;              // 分组ID
    
    @NotNull
    private String name;            // 分组名称
    
    @NotNull
    private String type;            // 分组类型：room, floor, building
    
    @NotNull
    private String parentId;        // 父分组ID
    
    @NotNull
    private long createdAt;         // 创建时间戳
}

@Entity
@Table(name = "device_group_mappings")
public class DeviceGroupMapping {
    @Id
    private String id;              // 映射ID
    
    @NotNull
    private String deviceId;        // 设备ID
    
    @NotNull
    private String groupId;         // 分组ID
    
    @NotNull
    private long createdAt;         // 创建时间戳
}
```

#### 2.5.5 API接口设计

**接口：获取分组列表**

```
GET /api/groups?type={type}
Authorization: Bearer {token}

Query Parameters:
- type: 分组类型（可选）

Response 200:
[
    {
        "id": "group_001",
        "name": "A1楼栋",
        "type": "building",
        "parentId": "",
        "createdAt": 1678886400
    }
]
```

**接口：创建分组**

```
POST /api/groups
Authorization: Bearer {token}

Request Body:
{
    "name": "A1-3楼",
    "type": "floor",
    "parentId": "group_001"
}

Response 200:
{
    "id": "group_002",
    "name": "A1-3楼",
    "type": "floor",
    "parentId": "group_001",
    "createdAt": 1678886400
}
```

**接口：向分组添加设备**

```
POST /api/groups/{groupId}/devices
Authorization: Bearer {token}

Request Body:
["device_001", "device_002"]

Response 200:
{
    "message": "Devices added to group successfully",
    "count": 2
}
```

**接口：获取分组下的设备列表**

```
GET /api/groups/{groupId}/devices
Authorization: Bearer {token}

Response 200:
{
    "deviceIds": ["device_001", "device_002"],
    "count": 2
}
```

#### 2.5.6 实现代码

```java
@RestController
@RequestMapping("/api/groups")
@Tag(name = "设备分组管理", description = "设备分组的CRUD操作和关联管理")
public class DeviceGroupController {

    @Autowired
    private DeviceGroupRepository deviceGroupRepository;

    @Autowired
    private DeviceGroupMappingRepository deviceGroupMappingRepository;

    @GetMapping
    public ResponseEntity<?> getGroups(@RequestParam(required = false) String type) {
        List<DeviceGroup> groups;
        if (type != null) {
            groups = deviceGroupRepository.findByType(type);
        } else {
            groups = deviceGroupRepository.findAll();
        }
        return ResponseEntity.ok(groups);
    }

    @PostMapping
    @RateLimit(value = 2.0, type = "create-group")
    @AuditLog(value = "创建设备分组", type = "DEVICE")
    public ResponseEntity<?> createGroup(@Valid @RequestBody DeviceGroup group) {
        if (group.getId() == null) {
            group.setId("group_" + UUID.randomUUID().toString().substring(0, 8));
        }
        group.setCreatedAt(System.currentTimeMillis() / 1000);
        DeviceGroup savedGroup = deviceGroupRepository.save(group);
        return ResponseEntity.ok(savedGroup);
    }

    @PostMapping("/{groupId}/devices")
    @AuditLog(value = "向分组添加设备", type = "DEVICE")
    public ResponseEntity<?> addDeviceToGroup(
            @PathVariable String groupId,
            @RequestBody List<String> deviceIds) {
        List<DeviceGroupMapping> mappings = new ArrayList<>();
        long now = System.currentTimeMillis() / 1000;
        
        for (String deviceId : deviceIds) {
            List<DeviceGroupMapping> existing = deviceGroupMappingRepository.findByDeviceId(deviceId);
            boolean alreadyExists = existing.stream()
                .anyMatch(m -> m.getGroupId().equals(groupId));
            if (!alreadyExists) {
                DeviceGroupMapping mapping = new DeviceGroupMapping();
                mapping.setId("mapping_" + UUID.randomUUID().toString().substring(0, 8));
                mapping.setDeviceId(deviceId);
                mapping.setGroupId(groupId);
                mapping.setCreatedAt(now);
                mappings.add(mapping);
            }
        }
        
        if (!mappings.isEmpty()) {
            deviceGroupMappingRepository.saveAll(mappings);
        }
        
        Map<String, Object> response = new HashMap<>();
        response.put("message", "Devices added to group successfully");
        response.put("count", mappings.size());
        return ResponseEntity.ok(response);
    }

    @DeleteMapping("/{groupId}")
    @AuditLog(value = "删除设备分组", type = "DEVICE")
    public ResponseEntity<?> deleteGroup(@PathVariable String groupId) {
        DeviceGroup group = deviceGroupRepository.findById(groupId).orElse(null);
        if (group == null) {
            throw new ResourceNotFoundException("group not found");
        }
        
        deviceGroupMappingRepository.deleteByGroupId(groupId);
        deviceGroupRepository.delete(group);
        
        Map<String, Object> response = new HashMap<>();
        response.put("message", "Group deleted successfully");
        return ResponseEntity.ok(response);
    }
}
```

#### 2.5.7 测试用例

| 用例编号 | 测试场景 | 输入 | 预期输出 | 优先级 |
|----------|----------|------|----------|--------|
| TC-GROUP-001 | 创建分组 | 有效分组信息 | 返回200，分组创建成功 | P0 |
| TC-GROUP-002 | 添加设备到分组 | 有效设备ID列表 | 返回200，设备添加成功 | P0 |
| TC-GROUP-003 | 重复添加设备 | 已存在的设备ID | 忽略重复，返回成功 | P1 |
| TC-GROUP-004 | 删除分组 | 有效分组ID | 分组和关联关系删除成功 | P0 |
| TC-GROUP-005 | 查询分组设备 | 有效分组ID | 返回设备ID列表 | P0 |

---

### 2.6 设备固件管理（P2）

#### 2.6.1 功能描述

管理设备固件版本，支持OTA（Over-The-Air）升级任务的创建、执行和监控。

#### 2.6.2 业务流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          固件升级流程                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐      │
│  │ 发起升级  │────>│ 创建记录  │────>│ 发送命令  │────>│ 设备下载  │      │
│  │ 请求     │     │ PENDING  │     │ IN_PROGRESS│    │ 固件     │      │
│  └──────────┘     └──────────┘     └──────────┘     └──────────┘      │
│                                                          │             │
│                                       ┌──────────────────┤             │
│                                       │                  │             │
│                                       ▼                  ▼             │
│                                 ┌──────────┐      ┌──────────┐        │
│                                 │ 升级成功  │      │ 升级失败  │        │
│                                 │ COMPLETED│      │ FAILED   │        │
│                                 └──────────┘      └──────────┘        │
│                                                                         │
│  状态流转：PENDING → IN_PROGRESS → COMPLETED/FAILED/CANCELLED          │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 2.6.3 业务规则

| 规则编号 | 规则描述 | 规则值 |
|----------|----------|--------|
| BR-FW-001 | 升级状态 | PENDING、IN_PROGRESS、COMPLETED、FAILED、CANCELLED |
| BR-FW-002 | 进度范围 | 0-100百分比 |
| BR-FW-003 | 唯一性检查 | 同一设备同时只能有一个待处理升级 |
| BR-FW-004 | 版本记录 | 记录升级前后的版本号 |

#### 2.6.4 数据模型

```java
@Entity
@Table(name = "device_firmware", indexes = {
    @Index(name = "idx_firmware_device", columnList = "deviceId"),
    @Index(name = "idx_firmware_version", columnList = "version")
})
public class DeviceFirmware {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @NotBlank
    private String deviceId;        // 设备ID

    @NotBlank
    private String version;         // 固件版本

    private String previousVersion; // 之前版本

    private String filePath;        // 固件文件路径

    private String description;     // 描述

    private String checksum;        // 校验和

    private long fileSize;          // 文件大小

    @NotBlank
    private String status;          // 状态

    private int progress = 0;       // 进度百分比

    private String errorMessage;    // 错误消息

    private String initiatedBy;     // 发起人

    private long startedAt;         // 开始时间

    private long completedAt;       // 完成时间

    private long createdAt;         // 创建时间
}
```

#### 2.6.5 API接口设计

**接口：发起固件升级**

```
POST /api/firmware/upgrade
Authorization: Bearer {token}

Query Parameters:
- deviceId: 设备ID
- version: 目标版本
- filePath: 固件文件路径
- checksum: 文件校验和（可选）
- fileSize: 文件大小（可选）
- initiatedBy: 发起人（可选）

Response 200:
{
    "id": 1,
    "deviceId": "device_001",
    "version": "1.2.0",
    "status": "PENDING",
    "progress": 0,
    "createdAt": 1678886400
}
```

**接口：更新升级进度**

```
PUT /api/firmware/{firmwareId}/progress?progress={progress}
Authorization: Bearer {token}

Response 200:
{
    "id": 1,
    "progress": 50,
    "status": "IN_PROGRESS"
}
```

**接口：完成升级**

```
POST /api/firmware/{firmwareId}/complete?success={success}&errorMessage={errorMessage}
Authorization: Bearer {token}

Response 200:
{
    "id": 1,
    "status": "COMPLETED",
    "completedAt": 1678886500
}
```

**接口：获取设备固件历史**

```
GET /api/firmware/device/{deviceId}
Authorization: Bearer {token}

Response 200:
[
    {
        "id": 1,
        "deviceId": "device_001",
        "version": "1.2.0",
        "previousVersion": "1.1.0",
        "status": "COMPLETED",
        "createdAt": 1678886400
    }
]
```

#### 2.6.6 实现代码

```java
@Service
public class DeviceFirmwareService {

    @Autowired
    private DeviceFirmwareRepository firmwareRepository;

    @Autowired
    private MqttService mqttService;

    public DeviceFirmware initiateUpgrade(String deviceId, String version, 
            String filePath, String checksum, long fileSize, String initiatedBy) {
        
        // 检查是否有待处理的升级
        List<DeviceFirmware> pending = firmwareRepository
            .findByDeviceIdAndStatusIn(deviceId, 
                Arrays.asList("PENDING", "IN_PROGRESS"));
        if (!pending.isEmpty()) {
            throw new RuntimeException("Device already has a pending upgrade");
        }

        // 获取当前版本
        Optional<DeviceFirmware> current = firmwareRepository
            .findFirstByDeviceIdAndStatusOrderByCreatedAtDesc(deviceId, "COMPLETED");
        String previousVersion = current.map(DeviceFirmware::getVersion).orElse("unknown");

        // 创建升级记录
        DeviceFirmware firmware = new DeviceFirmware();
        firmware.setDeviceId(deviceId);
        firmware.setVersion(version);
        firmware.setPreviousVersion(previousVersion);
        firmware.setFilePath(filePath);
        firmware.setChecksum(checksum);
        firmware.setFileSize(fileSize);
        firmware.setInitiatedBy(initiatedBy);
        firmware.setStatus("PENDING");

        return firmwareRepository.save(firmware);
    }

    public boolean sendUpgradeCommand(Long firmwareId) {
        DeviceFirmware firmware = firmwareRepository.findById(firmwareId)
            .orElseThrow(() -> new RuntimeException("Firmware record not found"));

        if (!"PENDING".equals(firmware.getStatus())) {
            return false;
        }

        // 发送MQTT升级命令
        String topic = String.format("dorm/%s/%s/cmd", 
            getRoomByDeviceId(firmware.getDeviceId()), firmware.getDeviceId());
        Map<String, Object> payload = new HashMap<>();
        payload.put("action", "upgrade");
        payload.put("version", firmware.getVersion());
        payload.put("url", firmware.getFilePath());
        payload.put("checksum", firmware.getChecksum());

        mqttService.publish(topic, payload);

        // 更新状态
        firmware.setStatus("IN_PROGRESS");
        firmware.setStartedAt(System.currentTimeMillis() / 1000);
        firmwareRepository.save(firmware);

        return true;
    }

    public DeviceFirmware updateProgress(Long firmwareId, int progress) {
        DeviceFirmware firmware = firmwareRepository.findById(firmwareId)
            .orElseThrow(() -> new RuntimeException("Firmware record not found"));

        firmware.setProgress(Math.min(100, Math.max(0, progress)));
        return firmwareRepository.save(firmware);
    }

    public DeviceFirmware completeUpgrade(Long firmwareId, boolean success, String errorMessage) {
        DeviceFirmware firmware = firmwareRepository.findById(firmwareId)
            .orElseThrow(() -> new RuntimeException("Firmware record not found"));

        firmware.setStatus(success ? "COMPLETED" : "FAILED");
        firmware.setCompletedAt(System.currentTimeMillis() / 1000);
        if (!success && errorMessage != null) {
            firmware.setErrorMessage(errorMessage);
        }

        return firmwareRepository.save(firmware);
    }

    public List<DeviceFirmware> getDeviceFirmwareHistory(String deviceId) {
        return firmwareRepository.findByDeviceIdOrderByCreatedAtDesc(deviceId);
    }

    public Optional<DeviceFirmware> getCurrentFirmware(String deviceId) {
        return firmwareRepository.findFirstByDeviceIdAndStatusOrderByCreatedAtDesc(
            deviceId, "COMPLETED");
    }
}
```

#### 2.6.7 测试用例

| 用例编号 | 测试场景 | 输入 | 预期输出 | 优先级 |
|----------|----------|------|----------|--------|
| TC-FW-001 | 发起固件升级 | 有效参数 | 返回200，状态为PENDING | P0 |
| TC-FW-002 | 重复发起升级 | 已有待处理升级 | 返回400，提示已有待处理升级 | P0 |
| TC-FW-003 | 更新升级进度 | progress=50 | 返回200，进度更新成功 | P1 |
| TC-FW-004 | 完成升级成功 | success=true | 状态更新为COMPLETED | P0 |
| TC-FW-005 | 完成升级失败 | success=false | 状态更新为FAILED，记录错误信息 | P0 |
| TC-FW-006 | 查询固件历史 | 有效设备ID | 返回升级历史列表 | P1 |

---

## 3. 数据模型设计

### 3.1 实体关系图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          设备管理模块ER图                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────┐       ┌──────────────┐       ┌──────────────┐        │
│  │   Device     │       │ StripStatus  │       │   Telemetry  │        │
│  ├──────────────┤       ├──────────────┤       ├──────────────┤        │
│  │ id (PK)      │◄──────│ deviceId(PK) │       │ id (PK)      │        │
│  │ name         │  1:1  │ ts           │       │ deviceId(FK) │──────┐ │
│  │ room         │       │ online       │       │ ts           │      │ │
│  │ online       │       │ totalPowerW  │       │ powerW       │      │ │
│  │ lastSeenTs   │       │ voltageV     │       │ voltageV     │      │ │
│  │ createdAt    │       │ currentA     │       │ currentA     │      │ │
│  └──────────────┘       │ socketsJson  │       └──────────────┘      │ │
│         │               └──────────────┘              ▲              │ │
│         │                                             │              │ │
│         │ 1:N                                         │ N:1          │ │
│         ▼                                             │              │ │
│  ┌──────────────┐                                     │              │ │
│  │DeviceFirmware│                                     │              │ │
│  ├──────────────┤                                     │              │ │
│  │ id (PK)      │                                     │              │ │
│  │ deviceId(FK) │─────────────────────────────────────┘              │ │
│  │ version      │                                                    │ │
│  │ status       │                                                    │ │
│  │ progress     │                                                    │ │
│  └──────────────┘                                                    │ │
│                                                                      │ │
│  ┌──────────────┐       ┌──────────────┐       ┌──────────────┐      │ │
│  │ DeviceGroup  │       │GroupMapping  │       │   Device     │      │ │
│  ├──────────────┤       ├──────────────┤       └──────────────┘      │ │
│  │ id (PK)      │◄──────│ groupId(FK)  │──────────────────────────────┘ │
│  │ name         │  N:N  │ deviceId(FK) │                                 │
│  │ type         │       │ createdAt    │                                 │
│  │ parentId     │       └──────────────┘                                 │
│  └──────────────┘                                                       │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 3.2 数据表设计

#### 3.2.1 devices（设备表）

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
|--------|----------|------|--------|------|
| id | VARCHAR(64) | PRIMARY KEY | - | 设备唯一标识符 |
| name | VARCHAR(128) | NOT NULL | - | 设备名称 |
| room | VARCHAR(64) | NOT NULL | - | 设备所在房间 |
| online | BOOLEAN | NOT NULL | FALSE | 设备在线状态 |
| last_seen_ts | BIGINT | NOT NULL | 0 | 最后心跳时间戳（秒） |
| created_at | BIGINT | NOT NULL | - | 创建时间戳（秒） |

**索引设计：**
| 索引名 | 字段 | 类型 | 描述 |
|--------|------|------|------|
| idx_devices_room | room | 普通索引 | 加速按房间查询设备 |
| idx_devices_online | online | 普通索引 | 加速查询在线/离线设备 |

#### 3.2.2 strip_status（插座状态表）

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
|--------|----------|------|--------|------|
| device_id | VARCHAR(64) | PRIMARY KEY | - | 设备ID |
| ts | BIGINT | NOT NULL | - | 状态更新时间戳（秒） |
| online | BOOLEAN | NOT NULL | FALSE | 设备在线状态 |
| total_power_w | DOUBLE | NOT NULL | 0.0 | 总功率（瓦） |
| voltage_v | DOUBLE | NOT NULL | 220.0 | 电压（伏） |
| current_a | DOUBLE | NOT NULL | 0.0 | 电流（安） |
| sockets_json | TEXT | NOT NULL | '[]' | 各插孔状态的JSON字符串 |

#### 3.2.3 device_groups（设备分组表）

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
|--------|----------|------|--------|------|
| id | VARCHAR(64) | PRIMARY KEY | - | 分组ID |
| name | VARCHAR(128) | NOT NULL | - | 分组名称 |
| type | VARCHAR(32) | NOT NULL | - | 分组类型 |
| parent_id | VARCHAR(64) | NOT NULL | '' | 父分组ID |
| created_at | BIGINT | NOT NULL | - | 创建时间戳 |

#### 3.2.4 device_group_mappings（设备分组关联表）

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
|--------|----------|------|--------|------|
| id | VARCHAR(64) | PRIMARY KEY | - | 映射ID |
| device_id | VARCHAR(64) | NOT NULL | - | 设备ID |
| group_id | VARCHAR(64) | NOT NULL | - | 分组ID |
| created_at | BIGINT | NOT NULL | - | 创建时间戳 |

#### 3.2.5 device_firmware（设备固件表）

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
|--------|----------|------|--------|------|
| id | BIGINT | PRIMARY KEY AUTO_INCREMENT | - | 固件ID |
| device_id | VARCHAR(50) | NOT NULL | - | 设备ID |
| version | VARCHAR(50) | NOT NULL | - | 固件版本 |
| previous_version | VARCHAR(50) | - | - | 之前版本 |
| file_path | VARCHAR(200) | - | - | 固件文件路径 |
| description | VARCHAR(500) | - | - | 描述 |
| checksum | VARCHAR(64) | - | - | 校验和 |
| file_size | BIGINT | - | - | 文件大小 |
| status | VARCHAR(20) | NOT NULL | 'PENDING' | 状态 |
| progress | INTEGER | - | 0 | 进度百分比 |
| error_message | VARCHAR(500) | - | - | 错误消息 |
| initiated_by | VARCHAR(64) | - | - | 发起人 |
| started_at | BIGINT | - | - | 开始时间 |
| completed_at | BIGINT | - | - | 完成时间 |
| created_at | BIGINT | NOT NULL | - | 创建时间戳 |

**索引设计：**
| 索引名 | 字段 | 类型 | 描述 |
|--------|------|------|------|
| idx_firmware_device | device_id | 普通索引 | 按设备查询 |
| idx_firmware_version | version | 普通索引 | 按版本查询 |

---

## 4. API接口汇总

### 4.1 设备管理接口

| 接口 | 方法 | 路径 | 描述 | 优先级 |
|------|------|------|------|--------|
| 获取设备列表 | GET | /api/devices | 查询所有设备列表 | P0 |
| 创建设备 | POST | /api/devices | 创建新设备 | P0 |
| 更新设备 | PUT | /api/devices/{deviceId} | 更新设备信息 | P0 |
| 删除设备 | DELETE | /api/devices/{deviceId} | 删除设备 | P0 |
| 批量删除设备 | DELETE | /api/devices/batch | 批量删除设备 | P1 |
| 获取房间设备 | GET | /api/devices/room/{room} | 获取指定房间设备 | P0 |
| 获取设备状态 | GET | /api/devices/{deviceId}/status | 获取设备详细状态 | P0 |

### 4.2 设备分组接口

| 接口 | 方法 | 路径 | 描述 | 优先级 |
|------|------|------|------|--------|
| 获取分组列表 | GET | /api/groups | 查询所有分组 | P1 |
| 创建分组 | POST | /api/groups | 创建新分组 | P1 |
| 更新分组 | PUT | /api/groups/{groupId} | 更新分组信息 | P1 |
| 删除分组 | DELETE | /api/groups/{groupId} | 删除分组 | P1 |
| 获取分组设备 | GET | /api/groups/{groupId}/devices | 获取分组下设备 | P1 |
| 添加设备到分组 | POST | /api/groups/{groupId}/devices | 添加设备到分组 | P1 |
| 从分组移除设备 | DELETE | /api/groups/{groupId}/devices/{deviceId} | 移除设备 | P1 |
| 获取设备分组 | GET | /api/groups/device/{deviceId} | 获取设备所属分组 | P1 |

### 4.3 设备固件接口

| 接口 | 方法 | 路径 | 描述 | 优先级 |
|------|------|------|------|--------|
| 发起升级 | POST | /api/firmware/upgrade | 发起固件升级 | P2 |
| 发送升级命令 | POST | /api/firmware/{firmwareId}/send | 发送升级命令 | P2 |
| 更新进度 | PUT | /api/firmware/{firmwareId}/progress | 更新升级进度 | P2 |
| 完成升级 | POST | /api/firmware/{firmwareId}/complete | 标记升级完成 | P2 |
| 取消升级 | POST | /api/firmware/{firmwareId}/cancel | 取消升级 | P2 |
| 获取固件历史 | GET | /api/firmware/device/{deviceId} | 获取设备固件历史 | P2 |
| 获取当前版本 | GET | /api/firmware/device/{deviceId}/current | 获取当前固件版本 | P2 |
| 获取待处理升级 | GET | /api/firmware/pending | 获取待处理升级列表 | P2 |
| 获取进行中升级 | GET | /api/firmware/active | 获取进行中升级列表 | P2 |

---

## 5. 技术实现细节

### 5.1 缓存策略

```java
@Configuration
@EnableCaching
public class CacheConfig {

    @Bean
    public CacheManager cacheManager(RedisConnectionFactory factory) {
        RedisCacheConfiguration config = RedisCacheConfiguration.defaultCacheConfig()
            .entryTtl(Duration.ofMinutes(5))
            .serializeValuesWith(RedisSerializationContext.SerializationPair
                .fromSerializer(new GenericJackson2JsonRedisSerializer()));

        return RedisCacheManager.builder(factory)
            .cacheDefaults(config)
            .withCacheConfiguration("devices", 
                config.entryTtl(Duration.ofMinutes(5)))
            .withCacheConfiguration("deviceStatus", 
                config.entryTtl(Duration.ofMinutes(1)))
            .build();
    }
}
```

**缓存策略说明：**

| 缓存名称 | TTL | 说明 |
|----------|-----|------|
| devices | 5分钟 | 设备列表缓存，变化频率低 |
| deviceStatus | 1分钟 | 设备状态缓存，实时性要求高 |

### 5.2 限流策略

```java
@Target({ElementType.METHOD, ElementType.TYPE})
@Retention(RetentionPolicy.RUNTIME)
@Documented
public @interface RateLimit {
    double value();              // 每秒允许的请求数
    String type() default "";    // 限流类型标识
}
```

**限流配置：**

| 接口类型 | 限流策略 | 说明 |
|----------|----------|------|
| 创建设备 | 2次/秒/IP | 防止恶意创建 |
| 创建分组 | 2次/秒/IP | 防止恶意创建 |

### 5.3 审计日志

```java
@Target({ElementType.METHOD, ElementType.TYPE})
@Retention(RetentionPolicy.RUNTIME)
@Documented
public @interface AuditLog {
    String value();              // 操作描述
    String type() default "";    // 操作类型
}
```

**审计日志记录：**

| 操作类型 | 操作描述 | 记录内容 |
|----------|----------|----------|
| DEVICE | 创建设备 | 设备ID、名称、房间 |
| DEVICE | 更新设备 | 设备ID、更新字段 |
| DEVICE | 删除设备 | 设备ID |
| DEVICE | 创建分组 | 分组ID、名称、类型 |
| DEVICE | 添加设备到分组 | 分组ID、设备ID列表 |

### 5.4 异常处理

```java
@RestControllerAdvice
public class GlobalExceptionHandler {

    @ExceptionHandler(ResourceNotFoundException.class)
    public ResponseEntity<?> handleResourceNotFound(ResourceNotFoundException e) {
        Map<String, Object> error = new HashMap<>();
        error.put("message", e.getMessage());
        return ResponseEntity.status(HttpStatus.NOT_FOUND).body(error);
    }

    @ExceptionHandler(MethodArgumentNotValidException.class)
    public ResponseEntity<?> handleValidationException(MethodArgumentNotValidException e) {
        Map<String, Object> error = new HashMap<>();
        error.put("message", "Validation failed");
        error.put("errors", e.getBindingResult().getAllErrors());
        return ResponseEntity.status(HttpStatus.BAD_REQUEST).body(error);
    }

    @ExceptionHandler(Exception.class)
    public ResponseEntity<?> handleGenericException(Exception e) {
        Map<String, Object> error = new HashMap<>();
        error.put("message", "Internal server error");
        return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR).body(error);
    }
}
```

---

## 6. 测试方案

### 6.1 单元测试

```java
@SpringBootTest
class DeviceServiceTest {

    @Autowired
    private DeviceService deviceService;

    @MockBean
    private DeviceRepository deviceRepository;

    @Test
    void testAddDevice() {
        Device device = new Device();
        device.setId("test_device");
        device.setName("测试设备");
        device.setRoom("A1-301");

        when(deviceRepository.save(any(Device.class))).thenReturn(device);

        Device saved = deviceService.addDevice(device);

        assertNotNull(saved);
        assertEquals("test_device", saved.getId());
        assertFalse(saved.isOnline());
    }

    @Test
    void testGetDevices() {
        List<Device> devices = new ArrayList<>();
        Device device = new Device();
        device.setId("test_device");
        device.setName("测试设备");
        device.setRoom("A1-301");
        device.setOnline(true);
        device.setLastSeenTs(System.currentTimeMillis() / 1000);
        devices.add(device);

        when(deviceRepository.findAll()).thenReturn(devices);

        List<Map<String, Object>> result = deviceService.getDevices();

        assertNotNull(result);
        assertEquals(1, result.size());
        assertTrue((Boolean) result.get(0).get("online"));
    }
}
```

### 6.2 集成测试

```java
@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
class DeviceControllerIntegrationTest {

    @Autowired
    private TestRestTemplate restTemplate;

    @Test
    void testCreateAndGetDevice() {
        // 创建设备
        Device device = new Device();
        device.setId("integration_test_device");
        device.setName("集成测试设备");
        device.setRoom("A1-301");

        ResponseEntity<Device> createResponse = restTemplate.postForEntity(
            "/api/devices", device, Device.class);

        assertEquals(HttpStatus.OK, createResponse.getStatusCode());
        assertNotNull(createResponse.getBody());

        // 获取设备列表
        ResponseEntity<Device[]> listResponse = restTemplate.getForEntity(
            "/api/devices", Device[].class);

        assertEquals(HttpStatus.OK, listResponse.getStatusCode());
        assertTrue(listResponse.getBody().length > 0);
    }
}
```

### 6.3 API测试脚本

```python
import requests
import json

BASE_URL = "http://localhost:8000/api"

def test_device_apis():
    # 登录获取token
    login_response = requests.post(f"{BASE_URL}/auth/login", json={
        "account": "admin",
        "password": "admin123"
    })
    assert login_response.status_code == 200
    token = login_response.json()["token"]
    headers = {"Authorization": f"Bearer {token}"}

    # 测试创建设备
    create_response = requests.post(f"{BASE_URL}/devices", 
        headers=headers,
        json={
            "id": "test_device_001",
            "name": "测试设备001",
            "room": "A1-301"
        }
    )
    assert create_response.status_code == 200
    print("✅ 创建设备成功")

    # 测试获取设备列表
    list_response = requests.get(f"{BASE_URL}/devices", headers=headers)
    assert list_response.status_code == 200
    devices = list_response.json()
    assert len(devices) > 0
    print(f"✅ 获取设备列表成功，共{len(devices)}个设备")

    # 测试获取设备状态
    status_response = requests.get(
        f"{BASE_URL}/devices/test_device_001/status", 
        headers=headers
    )
    assert status_response.status_code in [200, 404]
    print("✅ 获取设备状态成功")

    # 测试删除设备
    delete_response = requests.delete(
        f"{BASE_URL}/devices/test_device_001", 
        headers=headers
    )
    assert delete_response.status_code == 200
    print("✅ 删除设备成功")

if __name__ == "__main__":
    test_device_apis()
    print("\n所有API测试通过！")
```

---

## 7. 性能优化建议

### 7.1 数据库优化

1. **索引优化**
   - 为常用查询字段添加索引
   - 使用复合索引优化多条件查询

2. **查询优化**
   - 使用分页查询减少数据传输
   - 避免N+1查询问题

3. **连接池配置**
   ```yaml
   spring:
     datasource:
       hikari:
         maximum-pool-size: 20
         minimum-idle: 5
         connection-timeout: 30000
   ```

### 7.2 缓存优化

1. **多级缓存**
   - 本地缓存（Caffeine）+ 分布式缓存（Redis）
   - 热点数据使用本地缓存，减少网络开销

2. **缓存预热**
   ```java
   @Component
   public class CacheWarmupRunner implements CommandLineRunner {
       @Autowired
       private DeviceService deviceService;

       @Override
       public void run(String... args) {
           deviceService.getDevices();
       }
   }
   ```

### 7.3 并发优化

1. **异步处理**
   ```java
   @Async
   public CompletableFuture<Void> updateDeviceStatusAsync(String deviceId, boolean online) {
       updateDeviceStatus(deviceId, online);
       return CompletableFuture.completedFuture(null);
   }
   ```

2. **批量操作**
   ```java
   @Transactional
   public void batchUpdateDevices(List<Device> devices) {
       deviceRepository.saveAll(devices);
   }
   ```

---

## 8. 安全考虑

### 8.1 认证与授权

- 所有API接口需要JWT认证
- 使用RBAC进行权限控制
- 敏感操作记录审计日志

### 8.2 输入验证

```java
@Entity
public class Device {
    @Id
    @Pattern(regexp = "^[a-zA-Z0-9_]{1,64}$", message = "设备ID格式错误")
    private String id;

    @NotBlank(message = "设备名称不能为空")
    @Size(max = 128, message = "设备名称过长")
    private String name;

    @NotBlank(message = "房间号不能为空")
    @Pattern(regexp = "^[A-Z0-9]-[0-9]{1,3}$", message = "房间号格式错误")
    private String room;
}
```

### 8.3 数据脱敏

- 日志中不记录敏感信息
- API响应中对敏感字段进行脱敏处理

---

## 9. 总结

本文档详细描述了设备管理模块的功能设计与实现细节，包括：

1. **功能点设计**：6个核心功能点的详细设计，包括业务流程、业务规则、API接口和实现代码
2. **数据模型设计**：5个核心数据表的设计，包括字段定义和索引设计
3. **API接口汇总**：24个API接口的完整定义
4. **技术实现细节**：缓存策略、限流策略、审计日志、异常处理等技术实现
5. **测试方案**：单元测试、集成测试和API测试脚本
6. **性能优化建议**：数据库优化、缓存优化、并发优化等建议
7. **安全考虑**：认证授权、输入验证、数据脱敏等安全措施

该模块采用Spring Boot 3.2.3 + Spring Data JPA实现，支持PostgreSQL数据库，使用Redis进行缓存，通过MQTT协议与IoT设备通信，是一个完整的设备管理解决方案。

---

**文档版本**：v1.0  
**编写日期**：2026年3月14日  
**编写人**：系统架构组
