# 模块 00：基础依赖与环境配置

> **学习时长**: 2-3 天  
> **难度**: ⭐⭐☆☆☆  
> **前置知识**: Java 基础、Maven 基础、Node.js 基础

---

## 一、项目概述

### 1.1 技术栈总览

```yaml
后端技术栈:
  核心框架:
    - Java 21 (LTS)
    - Spring Boot 3.2.3
    - Spring Security (认证授权)
    - Spring Data JPA (数据持久化)
  
  通信协议:
    - MQTT (IoT设备通信)
    - WebSocket (实时推送)
    - REST API (前后端交互)
  
  数据存储:
    - PostgreSQL (主数据库)
    - H2 (开发环境内存数据库)
    - Redis (分布式缓存)
    - Caffeine (本地缓存)
  
  消息中间件:
    - RabbitMQ (消息队列)
    - Kafka (高吞吐消息处理)
  
  监控与可观测性:
    - Spring Boot Actuator
    - Prometheus + Grafana
    - Micrometer
  
  其他工具:
    - Lombok (代码简化)
    - JWT (Token认证)
    - Springdoc OpenAPI (API文档)
    - Guava (工具库)
    - Resilience4j (熔断降级)

前端技术栈:
  核心框架:
    - Vue 3.5 (Composition API)
    - TypeScript 5.7
    - Vite 6.2 (构建工具)
  
  UI组件库:
    - Ant Design Vue 4.2
  
  状态管理:
    - Pinia 3.0
  
  路由管理:
    - Vue Router 4.5
  
  数据可视化:
    - ECharts 5.6
    - Vue-ECharts 7.0
  
  HTTP客户端:
    - Axios 1.7
  
  工具库:
    - Day.js (时间处理)
  
  代码质量:
    - ESLint 9.2
    - Husky (Git钩子)
    - Commitlint (提交规范)
```

### 1.2 项目结构

```
dorm-power-console/
├── backend/                           # 后端项目
│   ├── src/main/java/com/dormpower/
│   │   ├── config/                   # 配置类（22个配置类）
│   │   ├── controller/               # REST控制器
│   │   ├── service/                  # 业务逻辑层
│   │   ├── repository/               # 数据访问层
│   │   ├── model/                    # JPA实体类
│   │   ├── dto/                      # 数据传输对象
│   │   ├── mqtt/                     # MQTT消息处理
│   │   ├── websocket/                # WebSocket处理
│   │   ├── scheduler/                # 定时任务
│   │   ├── annotation/               # 自定义注解
│   │   ├── exception/                # 异常处理
│   │   └── utils/                    # 工具类
│   ├── src/main/resources/
│   │   ├── application.yml           # 主配置文件
│   │   ├── application-dev.yml       # 开发环境配置
│   │   ├── application-local.yml     # 本地环境配置
│   │   ├── application-prod.yml      # 生产环境配置
│   │   └── application-low-memory.yml # 低内存配置
│   └── pom.xml                       # Maven依赖配置
│
├── frontend/                          # 前端项目
│   ├── src/
│   │   ├── api/                      # API接口封装
│   │   ├── components/               # 公共组件
│   │   ├── views/                    # 页面组件
│   │   ├── router/                   # 路由配置
│   │   ├── stores/                   # Pinia状态管理
│   │   ├── types/                    # TypeScript类型定义
│   │   └── utils/                    # 工具函数
│   ├── package.json                  # 前端依赖配置
│   ├── vite.config.ts                # Vite构建配置
│   └── tsconfig.json                 # TypeScript配置
│
└── docs/                              # 项目文档
    ├── learn/                         # 学习文档
    └── html_docs/                     # HTML文档
```

---

## 二、后端依赖配置

### 2.1 Maven 核心依赖

**文件位置**: `backend/pom.xml`

#### 2.1.1 Spring Boot 基础依赖

```xml
<!-- 父POM：统一管理Spring Boot版本 -->
<parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.2.3</version>
    <relativePath/>
</parent>

<!-- 核心依赖说明 -->
<properties>
    <java.version>21</java.version>              <!-- 使用Java 21 LTS -->
    <maven.compiler.source>${java.version}</maven.compiler.source>
    <maven.compiler.target>${java.version}</maven.compiler.target>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <lombok.version>1.18.34</lombok.version>
</properties>
```

```xml
<!-- Web服务依赖 -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-web</artifactId>
    <exclusions>
        <!-- 排除默认Logback，使用Log4j2 -->
        <exclusion>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-logging</artifactId>
        </exclusion>
    </exclusions>
</dependency>

<!-- JPA数据持久化 -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-data-jpa</artifactId>
    <exclusions>
        <exclusion>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-logging</artifactId>
        </exclusion>
    </exclusions>
</dependency>

<!-- WebSocket实时通信 -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-websocket</artifactId>
    <exclusions>
        <exclusion>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-logging</artifactId>
        </exclusion>
    </exclusions>
</dependency>

<!-- 安全框架 -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-security</artifactId>
    <exclusions>
        <exclusion>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-logging</artifactId>
        </exclusion>
    </exclusions>
</dependency>

<!-- 监控端点 -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-actuator</artifactId>
    <exclusions>
        <exclusion>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-logging</artifactId>
        </exclusion>
    </exclusions>
</dependency>
```

#### 2.1.2 数据库依赖

```xml
<!-- PostgreSQL生产数据库 -->
<dependency>
    <groupId>org.postgresql</groupId>
    <artifactId>postgresql</artifactId>
    <version>42.7.3</version>
</dependency>

<!-- H2开发环境内存数据库 -->
<dependency>
    <groupId>com.h2database</groupId>
    <artifactId>h2</artifactId>
    <version>2.2.224</version>
    <scope>runtime</scope>  <!-- 仅运行时使用 -->
</dependency>
```

#### 2.1.3 缓存依赖

```xml
<!-- Redis分布式缓存 -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-data-redis</artifactId>
    <exclusions>
        <exclusion>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-logging</artifactId>
        </exclusion>
    </exclusions>
</dependency>

<!-- Redis连接池 -->
<dependency>
    <groupId>org.apache.commons</groupId>
    <artifactId>commons-pool2</artifactId>
</dependency>

<!-- Caffeine本地缓存（多级缓存） -->
<dependency>
    <groupId>com.github.ben-manes.caffeine</groupId>
    <artifactId>caffeine</artifactId>
    <version>3.1.8</version>
</dependency>
```

#### 2.1.4 消息中间件依赖

```xml
<!-- RabbitMQ消息队列 -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-amqp</artifactId>
</dependency>

<!-- Kafka高吞吐消息处理 -->
<dependency>
    <groupId>org.springframework.kafka</groupId>
    <artifactId>spring-kafka</artifactId>
    <exclusions>
        <exclusion>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-logging</artifactId>
        </exclusion>
    </exclusions>
</dependency>
```

#### 2.1.5 IoT通信依赖

```xml
<!-- MQTT客户端（Eclipse Paho） -->
<dependency>
    <groupId>org.eclipse.paho</groupId>
    <artifactId>org.eclipse.paho.client.mqttv3</artifactId>
    <version>1.2.5</version>
</dependency>
```

#### 2.1.6 安全与认证依赖

```xml
<!-- JWT认证 -->
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-api</artifactId>
    <version>0.11.5</version>
</dependency>
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-impl</artifactId>
    <version>0.11.5</version>
    <scope>runtime</scope>
</dependency>
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-jackson</artifactId>
    <version>0.11.5</version>
    <scope>runtime</scope>
</dependency>
```

#### 2.1.7 工具类依赖

```xml
<!-- Lombok代码简化 -->
<dependency>
    <groupId>org.projectlombok</groupId>
    <artifactId>lombok</artifactId>
    <version>1.18.34</version>
    <optional>true</optional>  <!-- 编译时依赖，不打包到jar -->
</dependency>

<!-- Guava工具库 -->
<dependency>
    <groupId>com.google.guava</groupId>
    <artifactId>guava</artifactId>
    <version>33.0.0-jre</version>
</dependency>

<!-- 参数校验 -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-validation</artifactId>
    <exclusions>
        <exclusion>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-logging</artifactId>
        </exclusion>
    </exclusions>
</dependency>
```

#### 2.1.8 监控与可观测性依赖

```xml
<!-- Prometheus监控指标 -->
<dependency>
    <groupId>io.micrometer</groupId>
    <artifactId>micrometer-registry-prometheus</artifactId>
</dependency>

<!-- Resilience4j熔断降级 -->
<dependency>
    <groupId>io.github.resilience4j</groupId>
    <artifactId>resilience4j-circuitbreaker</artifactId>
    <version>2.2.0</version>
</dependency>
<dependency>
    <groupId>io.github.resilience4j</groupId>
    <artifactId>resilience4j-micrometer</artifactId>
    <version>2.2.0</version>
</dependency>
```

#### 2.1.9 API文档依赖

```xml
<!-- Springdoc OpenAPI（替代Swagger） -->
<dependency>
    <groupId>org.springdoc</groupId>
    <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
    <version>2.3.0</version>
</dependency>
```

#### 2.1.10 日志依赖

```xml
<!-- Log4j2日志框架（替代Logback） -->
<dependency>
    <groupId>org.apache.logging.log4j</groupId>
    <artifactId>log4j-core</artifactId>
    <version>2.20.0</version>
</dependency>
<dependency>
    <groupId>org.apache.logging.log4j</groupId>
    <artifactId>log4j-slf4j2-impl</artifactId>
    <version>2.20.0</version>
</dependency>
```

#### 2.1.11 测试依赖

```xml
<!-- Spring Boot测试 -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-test</artifactId>
    <scope>test</scope>
    <exclusions>
        <exclusion>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-logging</artifactId>
        </exclusion>
    </exclusions>
</dependency>

<!-- Spring Security测试 -->
<dependency>
    <groupId>org.springframework.security</groupId>
    <artifactId>spring-security-test</artifactId>
    <scope>test</scope>
</dependency>

<!-- JUnit 5 -->
<dependency>
    <groupId>org.junit.jupiter</groupId>
    <artifactId>junit-jupiter</artifactId>
    <version>5.9.3</version>
    <scope>test</scope>
</dependency>

<!-- Mockito（Java 25兼容） -->
<dependency>
    <groupId>org.mockito</groupId>
    <artifactId>mockito-inline</artifactId>
    <version>5.3.1</version>
    <scope>test</scope>
</dependency>
<dependency>
    <groupId>net.bytebuddy</groupId>
    <artifactId>byte-buddy-agent</artifactId>
    <version>1.14.18</version>
    <scope>test</scope>
</dependency>
```

### 2.2 Maven 构建配置

```xml
<build>
    <plugins>
        <!-- Spring Boot Maven插件 -->
        <plugin>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-maven-plugin</artifactId>
            <configuration>
                <excludes>
                    <!-- Lombok不打包到jar -->
                    <exclude>
                        <groupId>org.projectlombok</groupId>
                        <artifactId>lombok</artifactId>
                    </exclude>
                </excludes>
            </configuration>
        </plugin>
        
        <!-- 编译器插件 -->
        <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-compiler-plugin</artifactId>
            <version>3.8.1</version>
            <configuration>
                <source>${java.version}</source>
                <target>${java.version}</target>
                <annotationProcessorPaths>
                    <!-- Lombok注解处理器 -->
                    <path>
                        <groupId>org.projectlombok</groupId>
                        <artifactId>lombok</artifactId>
                        <version>${lombok.version}</version>
                    </path>
                </annotationProcessorPaths>
            </configuration>
        </plugin>
        
        <!-- 测试插件（Java 21模块系统兼容） -->
        <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-surefire-plugin</artifactId>
            <version>3.1.2</version>
            <configuration>
                <argLine>
                    --add-opens java.base/java.lang=ALL-UNNAMED
                    --add-opens java.base/java.lang.invoke=ALL-UNNAMED
                    --add-opens java.base/java.util=ALL-UNNAMED
                    --add-opens java.base/java.io=ALL-UNNAMED
                    --add-opens java.base/java.lang.reflect=ALL-UNNAMED
                    --add-opens java.base/java.text=ALL-UNNAMED
                    --add-opens java.desktop/java.awt.font=ALL-UNNAMED
                    -Dnet.bytebuddy.experimental=true
                </argLine>
            </configuration>
        </plugin>
    </plugins>
</build>
```

---

## 三、前端依赖配置

### 3.1 package.json 依赖

**文件位置**: `frontend/package.json`

#### 3.1.1 生产依赖

```json
{
  "dependencies": {
    "@ant-design/icons-vue": "^7.0.1",  // Ant Design图标
    "ant-design-vue": "^4.2.6",          // UI组件库
    "axios": "^1.7.9",                   // HTTP客户端
    "dayjs": "^1.11.13",                 // 轻量时间处理库
    "echarts": "^5.6.0",                 // 图表库
    "pinia": "^3.0.1",                   // 状态管理（替代Vuex）
    "vue": "^3.5.13",                    // Vue 3框架
    "vue-echarts": "^7.0.3",             // Vue封装的ECharts
    "vue-router": "^4.5.0"               // 路由管理
  }
}
```

#### 3.1.2 开发依赖

```json
{
  "devDependencies": {
    "@commitlint/cli": "^20.4.3",           // Git提交规范检查
    "@commitlint/config-conventional": "^20.4.3",  // 常规提交配置
    "@types/node": "^22.13.5",              // Node.js类型定义
    "@vitejs/plugin-vue": "^5.2.1",         // Vite Vue插件
    "@vue/eslint-config-typescript": "^14.4.0",  // Vue+TS ESLint配置
    "@vue/tsconfig": "^0.7.0",              // Vue TypeScript配置
    "eslint": "^9.20.1",                    // 代码检查工具
    "husky": "^9.1.7",                      // Git钩子管理
    "rollup-plugin-visualizer": "^5.14.0",  // 打包分析插件
    "sass-embedded": "^1.97.3",             // CSS预处理器
    "terser": "^5.46.0",                    // JS压缩工具
    "typescript": "^5.7.3",                 // TypeScript编译器
    "vite": "^6.2.0",                       // 现代构建工具
    "vue-tsc": "^2.2.8"                     // Vue TypeScript检查
  }
}
```

#### 3.1.3 NPM脚本

```json
{
  "scripts": {
    "dev": "vite",                          // 启动开发服务器
    "build": "vue-tsc && vite build",       // 类型检查+构建
    "build:analyze": "ANALYZE=true vue-tsc && vite build",  // 构建分析
    "preview": "vite preview",              // 预览生产构建
    "lint": "eslint . --ext .vue,.ts --fix",  // 代码检查修复
    "type-check": "vue-tsc --noEmit",       // TypeScript类型检查
    "prepare": "husky"                      // 安装Git钩子
  }
}
```

### 3.2 Vite 配置

**文件位置**: `frontend/vite.config.ts`

```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { resolve } from 'path'
import { visualizer } from 'rollup-plugin-visualizer'

export default defineConfig({
  plugins: [
    vue(),
    // 打包分析（可选）
    process.env.ANALYZE && visualizer({
      open: true,
      gzipSize: true,
      brotliSize: true
    })
  ],
  
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src')  // 路径别名
    }
  },
  
  server: {
    port: 3000,                        // 开发服务器端口
    proxy: {
      // API代理配置
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true
      },
      '/ws': {
        target: 'http://localhost:8000',
        ws: true
      }
    }
  },
  
  build: {
    target: 'es2015',                  // 编译目标
    outDir: 'dist',                    // 输出目录
    sourcemap: false,                  // 生产环境不生成sourcemap
    rollupOptions: {
      output: {
        // 手动分包
        manualChunks: {
          vendor: ['vue', 'vue-router', 'pinia'],
          ui: ['ant-design-vue', '@ant-design/icons-vue'],
          charts: ['echarts', 'vue-echarts']
        }
      }
    },
    // 压缩配置
    minify: 'terser',
    terserOptions: {
      compress: {
        drop_console: true,            // 移除console
        drop_debugger: true            // 移除debugger
      }
    }
  }
})
```

### 3.3 TypeScript 配置

**文件位置**: `frontend/tsconfig.json`

```json
{
  "extends": "@vue/tsconfig/tsconfig.dom.json",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    },
    "types": ["vite/client", "node"],
    "strict": true,                    // 严格模式
    "noImplicitAny": true,             // 禁止隐式any
    "strictNullChecks": true,          // 严格null检查
    "noUnusedLocals": true,            // 未使用变量检查
    "noUnusedParameters": true         // 未使用参数检查
  },
  "include": [
    "src/**/*.ts",
    "src/**/*.vue",
    "env.d.ts"
  ],
  "exclude": ["node_modules", "dist"]
}
```

---

## 四、后端配置文件

### 4.1 配置文件结构

```yaml
配置文件说明:
  application.yml:               # 主配置文件（公共配置）
  application-dev.yml:           # 开发环境（H2内存数据库）
  application-local.yml:         # 本地环境（本地PostgreSQL）
  application-prod.yml:          # 生产环境（远程PostgreSQL）
  application-low-memory.yml:    # 低内存环境（2核2G服务器）
  application-dev-full.yml:      # 开发完整环境（启用所有中间件）
```

### 4.2 application.yml 主配置

**文件位置**: `backend/src/main/resources/application.yml`

#### 4.2.1 数据库配置

```yaml
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dorm_power
    username: rongx
    password: 
    driver-class-name: org.postgresql.Driver
    hikari:                              # HikariCP连接池
      minimum-idle: 1                    # 最小空闲连接
      maximum-pool-size: 5               # 最大连接池大小
      idle-timeout: 60000                # 空闲超时（60秒）
      pool-name: DormPowerHikariPool     # 连接池名称
      max-lifetime: 1800000              # 连接最大生命周期（30分钟）
      connection-timeout: 30000          # 连接超时（30秒）
      leak-detection-threshold: 120000   # 连接泄漏检测（120秒）
  
  jpa:
    hibernate:
      ddl-auto: update                   # 自动更新表结构
    show-sql: false                      # 不打印SQL
    properties:
      hibernate:
        format_sql: false                # 不格式化SQL
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          batch_size: 20                 # 批量操作大小
        order_inserts: true              # 优化插入顺序
        order_updates: true              # 优化更新顺序
```

#### 4.2.2 RabbitMQ配置

```yaml
spring:
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USERNAME:guest}
    password: ${RABBITMQ_PASSWORD:guest}
    virtual-host: ${RABBITMQ_VHOST:/}
    enabled: ${RABBITMQ_ENABLED:true}
    queue:
      telemetry: ${RABBITMQ_QUEUE_TELEMETRY:telemetry.data}
      alert: ${RABBITMQ_QUEUE_ALERT:alert.message}
      bill: ${RABBITMQ_QUEUE_BILL:bill.generate}
    exchange:
      direct: ${RABBITMQ_EXCHANGE_DIRECT:dormpower.direct}
    routing:
      telemetry: ${RABBITMQ_ROUTING_TELEMETRY:telemetry}
      alert: ${RABBITMQ_ROUTING_ALERT:alert}
      bill: ${RABBITMQ_ROUTING_BILL:bill}
```

#### 4.2.3 MQTT配置

```yaml
mqtt:
  enabled: ${MQTT_ENABLED:true}
  broker-url: ${MQTT_BROKER_URL:tcp://localhost:1883}
  client-id: ${MQTT_CLIENT_ID:dorm-power-backend}
  username: ${MQTT_USERNAME:admin}
  password: ${MQTT_PASSWORD:admin}
  topic-prefix: ${MQTT_TOPIC_PREFIX:dorm}
  topics:
    device-status: status               # 设备状态主题
    device-telemetry: telemetry         # 遥测数据主题
    device-command: cmd                 # 命令下发主题
    device-ack: ack                     # 命令确认主题
    device-event: event                 # 设备事件主题
```

#### 4.2.4 安全配置

```yaml
security:
  jwt:
    secret: ${JWT_SECRET:your-secret-key-must-be-at-least-256-bits-long-for-jwt}
    expiration: ${JWT_EXPIRATION:86400000}  # 24小时（毫秒）
```

#### 4.2.5 监控配置

```yaml
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
      base-path: /actuator
  endpoint:
    health:
      show-details: always
      enabled: true
    metrics:
      enabled: true
    prometheus:
      enabled: true
  health:
    db:
      enabled: true
    diskspace:
      enabled: true
```

#### 4.2.6 API文档配置

```yaml
springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    tags-sorter: alpha                  # 按字母排序
    operations-sorter: alpha
```

### 4.3 application-dev.yml 开发环境

**文件位置**: `backend/src/main/resources/application-dev.yml`

```yaml
spring:
  # 使用H2内存数据库（开发环境）
  datasource:
    url: jdbc:h2:mem:dorm_power;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: 
    driver-class-name: org.h2.Driver
  
  # H2控制台（浏览器访问 http://localhost:8000/h2-console）
  h2:
    console:
      enabled: true
      path: /h2-console
  
  jpa:
    show-sql: true                      # 打印SQL
    properties:
      hibernate:
        format_sql: true                # 格式化SQL

logging:
  level:
    root: warn
    com.dormpower: debug                # 项目日志DEBUG级别
    org.hibernate.SQL: debug            # 打印SQL
    org.hibernate.type.descriptor.sql.BasicBinder: trace  # 打印参数
```

### 4.4 application-local.yml 本地环境

**文件位置**: `backend/src/main/resources/application-local.yml`

```yaml
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dorm_power
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
    hikari:
      minimum-idle: 2
      maximum-pool-size: 10

logging:
  level:
    com.dormpower: info
```

### 4.5 application-prod.yml 生产环境

**文件位置**: `backend/src/main/resources/application-prod.yml`

```yaml
spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:dorm_power}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 30000
  
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none                    # 生产环境不自动更新

logging:
  level:
    root: warn
    com.dormpower: info
    org.hibernate.SQL: off
```

### 4.6 application-low-memory.yml 低内存环境

**文件位置**: `backend/src/main/resources/application-low-memory.yml`

```yaml
# 针对2核2G服务器的优化配置
spring:
  datasource:
    hikari:
      minimum-idle: 1
      maximum-pool-size: 5              # 减少连接池

  # 禁用不需要的功能
  jpa:
    show-sql: false
    open-in-view: false                 # 关闭Open-in-View

# 减少线程池大小
app:
  thread-pool:
    core-size: 2
    max-size: 4
    queue-capacity: 100

logging:
  level:
    root: warn
    com.dormpower: warn
```

---

## 五、Config配置类详解

### 5.1 Config类总览

```yaml
配置类清单（22个）:
  核心配置:
    - AppConfig.java:                  # 应用基础配置
    - AsyncConfig.java:                # 异步任务配置
    - CorsConfig.java:                 # 跨域配置
  
  安全配置:
    - SecurityConfig.java:             # Spring Security配置
    - JwtAuthenticationFilter.java:    # JWT过滤器
  
  数据库与缓存:
    - CacheConfig.java:                # Caffeine缓存配置
    - RedisCacheConfig.java:           # Redis缓存配置
    - CacheIntegrationConfig.java:     # 多级缓存集成
    - MultiLevelCacheConfig.java:      # 多级缓存配置
    - CacheWarmupRunner.java:          # 缓存预热
  
  消息中间件:
    - MqttConfig.java:                 # MQTT配置
    - MqttStartupRunner.java:          # MQTT启动Runner
    - RabbitMQConfig.java:             # RabbitMQ配置
    - KafkaConfig.java:                # Kafka配置
    - RedisPubSubConfig.java:          # Redis发布订阅
  
  高可用配置:
    - RedisHighAvailabilityConfig.java: # Redis高可用配置
  
  通信配置:
    - WebSocketConfig.java:            # WebSocket配置
  
  API文档:
    - OpenApiConfig.java:              # OpenAPI文档配置
  
  监控配置:
    - MetricsConfig.java:              # Prometheus指标配置
    - ThreadPoolMetricsConfig.java:    # 线程池指标
    - ThreadPoolHealthIndicator.java:  # 线程池健康检查
  
  限流配置:
    - RateLimitConfig.java:            # 限流配置
```

### 5.2 SecurityConfig 安全配置

**文件位置**: `backend/src/main/java/com/dormpower/config/SecurityConfig.java`

```java
package com.dormpower.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.security.config.annotation.web.builders.HttpSecurity;
import org.springframework.security.config.annotation.web.configuration.EnableWebSecurity;
import org.springframework.security.config.http.SessionCreationPolicy;
import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;
import org.springframework.security.crypto.password.PasswordEncoder;
import org.springframework.security.web.SecurityFilterChain;
import org.springframework.security.web.authentication.UsernamePasswordAuthenticationFilter;
import lombok.RequiredArgsConstructor;

/**
 * Spring Security配置类
 * 
 * 配置说明:
 * 1. 禁用CSRF（前后端分离架构，使用JWT）
 * 2. 禁用Session（无状态认证）
 * 3. 配置URL访问权限
 * 4. 添加JWT过滤器
 */
@Configuration
@EnableWebSecurity
@RequiredArgsConstructor
public class SecurityConfig {

    private final JwtAuthenticationFilter jwtAuthenticationFilter;

    /**
     * 配置安全过滤链
     */
    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            // 禁用CSRF（前后端分离）
            .csrf(csrf -> csrf.disable())
            
            // 禁用Session（JWT无状态）
            .sessionManagement(session -> 
                session.sessionCreationPolicy(SessionCreationPolicy.STATELESS))
            
            // 配置URL访问权限
            .authorizeHttpRequests(auth -> auth
                // 公开接口
                .requestMatchers(
                    "/api/auth/login",
                    "/api/auth/register",
                    "/api/auth/forgot-password",
                    "/h2-console/**",
                    "/swagger-ui/**",
                    "/v3/api-docs/**",
                    "/actuator/**",
                    "/ws/**"
                ).permitAll()
                
                // 其他接口需要认证
                .anyRequest().authenticated()
            )
            
            // 添加JWT过滤器
            .addFilterBefore(
                jwtAuthenticationFilter,
                UsernamePasswordAuthenticationFilter.class
            );
        
        return http.build();
    }

    /**
     * 密码加密器
     */
    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }
}
```

### 5.3 CorsConfig 跨域配置

**文件位置**: `backend/src/main/java/com/dormpower/config/CorsConfig.java`

```java
package com.dormpower.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.web.cors.CorsConfiguration;
import org.springframework.web.cors.UrlBasedCorsConfigurationSource;
import org.springframework.web.filter.CorsFilter;

import java.util.List;

/**
 * CORS跨域配置
 * 
 * 配置说明:
 * 1. 允许前端域名访问
 * 2. 允许的HTTP方法
 * 3. 允许的请求头
 * 4. 凭证支持
 */
@Configuration
public class CorsConfig {

    @Bean
    public CorsFilter corsFilter() {
        CorsConfiguration config = new CorsConfiguration();
        
        // 允许的源（开发环境允许localhost:3000）
        config.setAllowedOrigins(List.of(
            "http://localhost:3000",
            "http://localhost:5173"
        ));
        
        // 允许凭证（Cookie、Authorization头）
        config.setAllowCredentials(true);
        
        // 允许的HTTP方法
        config.setAllowedMethods(List.of(
            "GET", "POST", "PUT", "DELETE", "OPTIONS"
        ));
        
        // 允许的请求头
        config.setAllowedHeaders(List.of("*"));
        
        // 预检请求缓存时间（秒）
        config.setMaxAge(3600L);
        
        UrlBasedCorsConfigurationSource source = new UrlBasedCorsConfigurationSource();
        source.registerCorsConfiguration("/**", config);
        
        return new CorsFilter(source);
    }
}
```

### 5.4 AsyncConfig 异步配置

**文件位置**: `backend/src/main/java/com/dormpower/config/AsyncConfig.java`

```java
package com.dormpower.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.scheduling.annotation.EnableAsync;
import org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor;

import java.util.concurrent.Executor;

/**
 * 异步任务配置
 * 
 * 配置说明:
 * 1. 启用异步支持（@EnableAsync）
 * 2. 配置线程池参数
 * 3. 自定义线程名称
 */
@Configuration
@EnableAsync
public class AsyncConfig {

    @Bean(name = "taskExecutor")
    public Executor taskExecutor() {
        ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
        
        // 核心线程数
        executor.setCorePoolSize(4);
        
        // 最大线程数
        executor.setMaxPoolSize(8);
        
        // 队列容量
        executor.setQueueCapacity(100);
        
        // 线程名称前缀
        executor.setThreadNamePrefix("AsyncTask-");
        
        // 等待所有任务结束后再关闭线程池
        executor.setWaitForTasksToCompleteOnShutdown(true);
        
        // 等待时间（秒）
        executor.setAwaitTerminationSeconds(60);
        
        // 拒绝策略：由调用线程处理
        executor.setRejectedExecutionHandler(
            new java.util.concurrent.ThreadPoolExecutor.CallerRunsPolicy()
        );
        
        executor.initialize();
        return executor;
    }
}
```

### 5.5 WebSocketConfig WebSocket配置

**文件位置**: `backend/src/main/java/com/dormpower/config/WebSocketConfig.java`

```java
package com.dormpower.config;

import org.springframework.context.annotation.Configuration;
import org.springframework.web.socket.config.annotation.EnableWebSocket;
import org.springframework.web.socket.config.annotation.WebSocketConfigurer;
import org.springframework.web.socket.config.annotation.WebSocketHandlerRegistry;
import lombok.RequiredArgsConstructor;

/**
 * WebSocket配置
 * 
 * 配置说明:
 * 1. 启用WebSocket支持
 * 2. 注册WebSocket处理器
 * 3. 配置跨域
 */
@Configuration
@EnableWebSocket
@RequiredArgsConstructor
public class WebSocketConfig implements WebSocketConfigurer {

    private final com.dormpower.websocket.DeviceWebSocketHandler deviceWebSocketHandler;

    @Override
    public void registerWebSocketHandlers(WebSocketHandlerRegistry registry) {
        registry.addHandler(deviceWebSocketHandler, "/ws/device")
                .setAllowedOrigins("*");
    }
}
```

### 5.6 CacheConfig 缓存配置

**文件位置**: `backend/src/main/java/com/dormpower/config/CacheConfig.java`

```java
package com.dormpower.config;

import com.github.benmanes.caffeine.cache.Caffeine;
import org.springframework.cache.CacheManager;
import org.springframework.cache.annotation.EnableCaching;
import org.springframework.cache.caffeine.CaffeineCacheManager;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import java.util.concurrent.TimeUnit;

/**
 * Caffeine本地缓存配置
 * 
 * 配置说明:
 * 1. 启用缓存注解支持（@EnableCaching）
 * 2. 配置Caffeine缓存参数
 * 3. 定义缓存策略
 */
@Configuration
@EnableCaching
public class CacheConfig {

    @Bean
    public CacheManager cacheManager() {
        CaffeineCacheManager cacheManager = new CaffeineCacheManager();
        
        cacheManager.setCaffeine(Caffeine.newBuilder()
            // 写入后10分钟过期
            .expireAfterWrite(10, TimeUnit.MINUTES)
            // 最大1000条记录
            .maximumSize(1000)
            // 记录统计信息
            .recordStats()
        );
        
        return cacheManager;
    }
}
```

### 5.7 MqttConfig MQTT配置

**文件位置**: `backend/src/main/java/com/dormpower/config/MqttConfig.java`

```java
package com.dormpower.config;

import lombok.Data;
import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.context.annotation.Configuration;

import java.util.HashMap;
import java.util.Map;

/**
 * MQTT配置属性
 * 
 * 配置说明:
 * 1. 使用@ConfigurationProperties绑定配置
 * 2. 支持环境变量覆盖
 * 3. 定义主题结构
 */
@Data
@Configuration
@ConfigurationProperties(prefix = "mqtt")
public class MqttConfig {

    private boolean enabled = true;
    private String brokerUrl = "tcp://localhost:1883";
    private String clientId = "dorm-power-backend";
    private String username = "admin";
    private String password = "admin";
    private String topicPrefix = "dorm";
    private Map<String, String> topics = new HashMap<>();
    
    /**
     * 获取完整主题
     */
    public String getFullTopic(String topicKey) {
        String topic = topics.getOrDefault(topicKey, topicKey);
        return topicPrefix + "/" + topic;
    }
    
    /**
     * 获取设备特定主题
     */
    public String getDeviceTopic(String topicKey, String deviceId) {
        return getFullTopic(topicKey) + "/" + deviceId;
    }
}
```

### 5.8 OpenApiConfig API文档配置

**文件位置**: `backend/src/main/java/com/dormpower/config/OpenApiConfig.java`

```java
package com.dormpower.config;

import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Contact;
import io.swagger.v3.oas.models.info.Info;
import io.swagger.v3.oas.models.info.License;
import io.swagger.v3.oas.models.security.SecurityRequirement;
import io.swagger.v3.oas.models.security.SecurityScheme;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

/**
 * OpenAPI文档配置
 * 
 * 配置说明:
 * 1. API基本信息
 * 2. 安全认证方案
 * 3. 全局安全要求
 */
@Configuration
public class OpenApiConfig {

    @Bean
    public OpenAPI customOpenAPI() {
        final String securitySchemeName = "bearerAuth";
        
        return new OpenAPI()
            .info(new Info()
                .title("宿舍用电管理系统 API")
                .version("1.0.0")
                .description("宿舍用电管理系统REST API文档")
                .contact(new Contact()
                    .name("DormPower Team")
                    .email("contact@dormpower.com"))
                .license(new License()
                    .name("MIT")
                    .url("https://opensource.org/licenses/MIT")))
            .addSecurityItem(new SecurityRequirement().addList(securitySchemeName))
            .components(new io.swagger.v3.oas.models.Components()
                .addSecuritySchemes(securitySchemeName,
                    new SecurityScheme()
                        .name(securitySchemeName)
                        .type(SecurityScheme.Type.HTTP)
                        .scheme("bearer")
                        .bearerFormat("JWT")));
    }
}
```

### 5.9 MetricsConfig 监控配置

**文件位置**: `backend/src/main/java/com/dormpower/config/MetricsConfig.java`

```java
package com.dormpower.config;

import io.micrometer.core.instrument.MeterRegistry;
import org.springframework.boot.actuate.autoconfigure.metrics.MeterRegistryCustomizer;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

/**
 * Prometheus监控指标配置
 * 
 * 配置说明:
 * 1. 通用标签配置
 * 2. 自定义指标
 */
@Configuration
public class MetricsConfig {

    @Bean
    public MeterRegistryCustomizer<MeterRegistry> metricsCommonTags() {
        return registry -> registry.config()
            .commonTags(
                "application", "dorm-power",
                "version", "1.0.0"
            );
    }
}
```

### 5.10 RateLimitConfig 限流配置

**文件位置**: `backend/src/main/java/com/dormpower/config/RateLimitConfig.java`

```java
package com.dormpower.config;

import com.google.common.util.concurrent.RateLimiter;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import java.util.Map;
import java.util.concurrent.ConcurrentHashMap;

/**
 * 限流配置（基于Guava RateLimiter）
 * 
 * 配置说明:
 * 1. 令牌桶算法限流
 * 2. 支持不同接口不同限流策略
 */
@Configuration
public class RateLimitConfig {

    // 存储各接口的限流器
    private final Map<String, RateLimiter> rateLimiters = new ConcurrentHashMap<>();

    /**
     * 获取或创建限流器
     * 
     * @param key 限流键
     * @param permitsPerSecond 每秒许可数
     */
    public RateLimiter getRateLimiter(String key, double permitsPerSecond) {
        return rateLimiters.computeIfAbsent(key, k -> 
            RateLimiter.create(permitsPerSecond)
        );
    }
}
```

---

## 六、环境配置对比

### 6.1 环境配置对照表

```yaml
环境对比:
  dev（开发环境）:
    数据库: H2内存数据库
    SQL打印: 是
    日志级别: DEBUG
    H2控制台: 启用
    适用场景: 本地开发调试
  
  local（本地环境）:
    数据库: 本地PostgreSQL
    SQL打印: 否
    日志级别: INFO
    适用场景: 本地完整测试
  
  prod（生产环境）:
    数据库: 远程PostgreSQL
    SQL打印: 否
    日志级别: WARN/INFO
    ddl-auto: none
    适用场景: 生产部署
  
  low-memory（低内存）:
    数据库: 远程PostgreSQL
    连接池: 1-5
    线程池: 2-4
    日志级别: WARN
    适用场景: 2核2G服务器
```

### 6.2 如何切换环境

```bash
# 方法1: 命令行参数
mvn spring-boot:run -Dspring-boot.run.profiles=dev

# 方法2: 环境变量
export SPRING_PROFILES_ACTIVE=prod
mvn spring-boot:run

# 方法3: application.properties
# 在 src/main/resources/application.properties 中添加：
# spring.profiles.active=dev

# 方法4: JVM参数
java -jar -Dspring.profiles.active=prod dorm-power-backend.jar
```

---

## 七、开发环境搭建

### 7.1 环境要求

```yaml
必备环境:
  JDK: 21+ (推荐21 LTS)
  Maven: 3.8+
  Node.js: 18+ (推荐20 LTS)
  npm: 9+ 或 pnpm: 8+
  
可选环境:
  PostgreSQL: 14+ (本地开发)
  Docker: 20+ (容器化部署)
  Redis: 7+ (缓存)
  RabbitMQ: 3.12+ (消息队列)
```

### 7.2 后端启动

```bash
# 1. 克隆项目
git clone <repository-url>
cd dorm-power-console/backend

# 2. 安装依赖
mvn clean install

# 3. 开发环境启动（使用H2内存数据库）
mvn spring-boot:run -Dspring-boot.run.profiles=dev

# 4. 访问API文档
# http://localhost:8000/swagger-ui.html

# 5. H2控制台（仅dev环境）
# http://localhost:8000/h2-console
# JDBC URL: jdbc:h2:mem:dorm_power
# 用户名: sa
# 密码: (空)
```

### 7.3 前端启动

```bash
# 1. 进入前端目录
cd dorm-power-console/frontend

# 2. 安装依赖
npm install

# 3. 启动开发服务器
npm run dev

# 4. 访问前端页面
# http://localhost:3000

# 5. 类型检查
npm run type-check

# 6. 代码检查
npm run lint

# 7. 生产构建
npm run build
```

### 7.4 使用Docker启动（推荐）

```bash
# 1. 启动所有服务（PostgreSQL + Redis + RabbitMQ + MQTT）
docker compose -f docker-compose.production.yml --env-file .env.production up -d

# 2. 查看服务状态
docker compose ps

# 3. 查看日志
docker compose logs -f

# 4. 停止服务
docker compose down
```

---

## 八、常见问题

### 8.1 依赖问题

**Q: Maven依赖下载慢**

```bash
# 配置国内镜像（~/.m2/settings.xml）
<mirrors>
    <mirror>
        <id>aliyun</id>
        <mirrorOf>central</mirrorOf>
        <name>Aliyun Maven</name>
        <url>https://maven.aliyun.com/repository/central</url>
    </mirror>
</mirrors>
```

**Q: npm install失败**

```bash
# 配置国内镜像
npm config set registry https://registry.npmmirror.com

# 清除缓存重试
npm cache clean --force
npm install
```

### 8.2 配置问题

**Q: 如何查看当前生效的配置**

```bash
# 启动后访问
http://localhost:8000/actuator/env

# 或查看启动日志中的Active profiles
```

**Q: 数据库连接失败**

```yaml
排查步骤:
  1. 检查数据库是否启动
  2. 检查application.yml中的数据库配置
  3. 检查环境变量是否覆盖配置
  4. 查看启动日志中的错误信息
```

### 8.3 端口冲突

```bash
# 查看端口占用
# Windows
netstat -ano | findstr :8000

# 修改端口
# application.yml
server:
  port: 8001
```

---

## 九、扩展练习

### 练习1：添加自定义配置属性

1. 在`application.yml`中添加自定义配置
2. 创建`@ConfigurationProperties`配置类
3. 在Service中注入并使用

### 练习2：配置多数据源

1. 添加第二个数据源配置
2. 创建对应的`EntityManagerFactory`
3. 配置不同Repository使用不同数据源

### 练习3：集成Redis缓存

1. 安装Redis
2. 配置`RedisCacheConfig`
3. 使用`@Cacheable`注解缓存数据

### 练习4：配置MQTT消息监听

1. 启动MQTT Broker（Mosquitto）
2. 配置`MqttConfig`
3. 创建消息监听器处理遥测数据

---

**最后更新**: 2026-04-22  
**文档版本**: 1.0